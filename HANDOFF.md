# HANDOFF — cross-agent live state

Shared between Claude Code and Codex (and any other agent the user adds). This file is the **source of truth for what to do next**. SUMMARY.md is deep history; RUN_LOG.md is per-batch operational log; this file is the live baton.

## Protocol

**On session start** (before doing anything else):
1. Read this whole file.
2. Read CLAUDE.md if you haven't this session (project knowledge, won't change often).
3. Skim the last 2–3 entries under "Recent sessions" to know what just happened.

**On session end** (or before quota runs out):
1. Update **Status**, **In progress**, **Blocked**, **Next up** to reflect reality *now*.
2. Prepend one new entry to **Recent sessions** — date, agent name, 1–3 bullets on what changed. Keep entries ≤6 lines.
3. If you started something and didn't finish, leave concrete pointers in **In progress** (file paths, line numbers, the exact next command). Assume the next agent has zero memory of this session.
4. Trim **Recent sessions** to the last ~15 entries; older history belongs in SUMMARY.md or git log.

**Editing rules**:
- Never delete another agent's "In progress" notes without confirming the work is done. If unsure, move them to "Blocked / waiting" with a question.
- Concrete > vague. "Run `.venv/bin/python -m scripts.X`" beats "continue the import".
- Mark agent in entries: `(Claude)` or `(Codex)`.

---

## Status

**🧹 2026-08-14 (Claude/Opus) — FOLLOW-UP CLEANUP DONE & DEPLOYED.** Cleared both curation follow-ups the zh-resume surfaced: (1) **merged 66 synonym pairs** + excluded 8 genus-trap/distinct rows (`merge_duplicate_species` now 0/clean); (2) **untangled the `Sinica sinica` moth+bird conflation** (split→ミノウスバ id1101 + Parus major id24750, deleted corrupt row 4657). `dedupe` + `export_static` rebuilt `site/` (24,229 visible spp) → force-pushed gh-pages **`858e5726`→`0c287027`** (live 200; new commit confirmed on remote ref with meta species=24229; GitHub Pages CDN propagation may lag a few min post-push). DB backup `bak_pre_synmerge_20260814`. Details in the two RESOLVED follow-ups below.

**🚀 2026-08-14 (Claude/Opus) — DEPLOYED. zh coverage 49.9%→52.8%.** Finished the zh round-2 resume the 08-13 session left dangling (its ensure_inat_taxon→fetch_inat_zh_names chain completed 22:11 JST *after* that session's last export/deploy, so the +651 sat in DB unexported). Rebuilt `site/` (`export_static`, 24,295 spp) → force-pushed gh-pages **`2fc1828`→`858e5726`** (live 200; round-2 names 美女蛤/小口拟虾虎 confirmed live). zh now **12,822 distinct species = 52.8%** (was 12,123/49.9%). **Pre-deploy DQ:** merge dry-run came back **77 groups (not clean)** — all NEW synonym-collisions from round-2's tid assignment. NULLed **3 confirmed cross-kingdom bad tids** (see Blocked→new follow-up); **deferred** the remaining 74 legit-synonym merges (need per-case review, some have divergent ja). See top Recent-sessions entry.

**🚀 2026-08-13 (Claude/Opus) — DEPLOYED. zh coverage 43.6%→49.9%.** New `scripts/fetch_inat_zh_names.py` (iNat `locale=zh-CN`, fail-closed) added **+1,518 visible-species Chinese names** → gh-pages `2fc1828` (live 200), script on main `543f364`. DB backup `bak_pre_inat_zh`; reversible via `DELETE FROM species_alias WHERE status LIKE 'inat-zh%'`. Also closed the いきものログ TODO-churn (it was DECIDED-SKIP 2026-08-08 but kept getting re-proposed). See top Recent-sessions entry.

**🚀 2026-08-11 (Claude/Opus) — DEPLOYED. gh-pages now current.** Ran `dedupe` (861,239 park_species rows; applied マアナゴ→Haliotis ovina suppression) → `deploy_pages.sh` (export_static rebuilt `site/`, 5058 files, orphan force-push `c1a8738`→`9c211dc` to origin/gh-pages) → live HTTP 200. Also `git push origin main` (backup, `…→f992940`). Live site now carries: this session's ~1050 profiles + recovery track (92.3% named-pool coverage; 17,312/24,298 exported species have a `pr` profile), admin:municipality tier + abundance + rich fields (individualCount/observer/last_year), and the マアナゴ suppression. NOTE discovered during verify: some R1 "recovered" flagship birds (ヒヨドリ etc.) were **already** DB-profiled from an earlier session — my residue-scan keyed on sidecar keys not the `species_profile` table, so it re-flagged already-covered species (harmless; seed keeps existing DB rows). The **tail-158** were correctly DB-filtered, so those are genuinely new.

**✅ 2026-08-10 (Claude/Opus) — PROFILING: weak-gate "exhaustion" was WRONG; STRONGER GATE recovered the commonest species in Japan. + AUTONOMOUS DATA PIPELINE COMPLETED.**
- **Profiling (batches 36→96 + R1/R2 recovery, this multi-day session, ~1050 new 4-lang profiles):** sidecar `data/species_profiles_extra.json` now **17,211 entries**.
- **KEY LESSON — the old `inat_resolve_ja(ja)` gate was too weak.** It matched only iNat's **top-5 `q=<ja>` search hits**; for short common names the *species* gets crowded out by higher taxa sharing the substring (e.g. `ヒヨドリ` → family ヒヨドリ科, genus ヒヨドリバナ属…), so it returned None and the loop **skipped the most-visible species in the whole DB**. The "1,759 residue all fail the gate → exhausted" conclusion was an artifact of that weakness, **not** real.
- **FIX = two stronger gates (scratchpad `verify_sci.py` `confirm_sci_ja` + `verify_v2.py` `confirm_v2`).** Both query iNat **by our scientific_name** (iNat resolves synonym sci→accepted taxon) and confirm the returned species-rank taxon's ja == our ja. v2 also accepts genus-reassignment (ja-match + species-epithet-match). Over the top-500 residue: **46 sci→ja-confirmed + 50 v2-SAFE genus-reassignments**. **Profiled 90 of them (batches R1+R2, `profile_recovered{1,2}.py`)** — incl. ヒヨドリ(2096 parks), スズメ(1991), メジロ(1714), シジュウカラ(1670), ムクドリ, トビ, ウグイス, モズ, ツグミ, コイ, オナガ, アメリカザリガニ, イチモンジチョウ, コルリ, コマドリ, ノゴマ, ウグイ, ハンミョウ, ミルンヤンマ, ウスバカゲロウ… These were keyed under **our** scientific_name (unchanged in DB; sidecar profile is species-content, genus-agnostic).
- **2 traps excluded (do NOT profile):** `マアナゴ→Haliotis ovina` (iNat itself carries this wrong ja-label; マアナゴ is a conger eel, Haliotis is an abalone → should be **suppressed**), and `小羽蟋蟋螽` (a Chinese name; already in profile_skip).
- **RECOVERY TRACK COMPLETE (2026-08-11, batches R1/R2/R3 + tail1/tail2):** profiled all strong-gate-confirmed residue. **R3** = 25 v2-CHECK synonyms (ja-match, epithet-differs: ユウガギク, イヌガヤ, オオミノガ, キマダラモドキ…). **Full-residue v2 rescan** (`verify_v2_full.py`, all ~1700 residue not just top-500) → 158 genuinely-unprofiled tail species (all np=1), profiled in **tail1** (97 animals+fungi: lionfish/moray/fusilier/leopard-cat/echidna…) + **tail2** (61 plants: saffron, pear, mangrove, sea lettuce, epiphytic orchid…). **Coverage now 17,242/18,680 = 92.3%** of the named-profilable pool; `species_profile` covers 17,329 distinct species; sidecar 17,394.
- **REMAINING residue = 1,438**, and these genuinely fail **both** strong gates (sci→ja exact AND v2 genus-reassignment). They are the real human-curation set: iNat returns a *different* species for our sci (true synonymy conflicts / iNat-side errors like マアナゴ→Haliotis ovina), species iNat has no ja vernacular for, or names absent from iNat. **No more auto-recovery is safe** — further profiling needs manual curation (`data/janame_review.json` + case-by-case). Recovery tooling preserved in scratchpad: `verify_sci.py` (sci→ja), `verify_v2.py`/`verify_v2_full.py` (genus-reassignment), caches in `data/cache/inat_scija` + `inat_v2`.
- (earlier this session) Batches 36→70 (~35 batches) established the loop; 71→96 pushed through the alphabetical np=1 long tail (marine inverts / moths / fungi / bryophytes), group-helper functions keeping quality up. Each candidate iNat-verified before profiling; all committed to `main` (sidecar `git add -f`).
- **Autonomous queue FINISHED** (launchd `com.parklife.queue`): all 44× `gbif <pref> --refresh` → `dedupe` → `export_static` completed rc=0 (last `export_static` 2026-08-09T11:26Z). `site/` is **rebuilt locally** with the admin:municipality tier + abundance + rich fields (individualCount/observer_count/last_year). observation tiers now: onsite 762k + admin:municipality 431k. **NOT deployed** — `scripts/deploy_pages.sh` needs the user's OK. NOTE: profiles added *after* 11:26Z aren't in the current `site/` export yet; a fresh `export_static` before deploy will fold them in.
- **Next up:** (a) when the user OKs, `export_static` (refresh) → `deploy_pages.sh` to publish admin-tier + abundance + the session's profiles; (b) profiling loop can keep going down the long tail (self-paced, quality-gated).

**✅ 2026-08-07 (Claude/Opus) — FREE-TOOL TRACK #1–#4 NOW LIVE.** Deployed via `scripts/deploy_pages.sh` (orphan `c1a8738`, 5058 files → `origin/gh-pages`); live `app.js` == latest, `sw.js` == `parklife-v6`. This was the last blocker from the 2026-08-07 #4 entry (Pages outage had held it). Fixed the user-reported "empty species panel on many parks" — it was just the stale old app.js on gh-pages; current code/data are clean (probe: 40 parks, 0 empty). The only genuinely empty parks are 16 with `n=0` (no occurrence records). Next: optional #5 custom domain, or a new track (monetization / いきものログ / resume np=1) — ask the user.

**✅ 2026-08-04 (Claude/Opus) — BOTH operational items from the 2026-07-18 review RESOLVED (user chose "ship" over more grind):** pushed 378 backlog commits to origin/main (backup) + rebuilt `site/` via `export_static` + force-pushed gh-pages (`deploy_pages.sh`). **Live demo is now current** (was last deployed 2026-07-06). See top Recent-sessions entry. The *new-direction* choice (productization / いきものログ / resume np=1) is still open — ask the user.

**⚠️ 2026-07-18 (Claude/Opus) — STATE REVIEW: two operational items need the user's call (grind itself is healthy). [①②RESOLVED 2026-08-04, see above]**
1. **165 local commits UNPUSHED to origin/main** (oldest 2026-07-08). ALL are profile-sidecar + handoff commits (0 code/ingestion) — verified. `git push origin main` is a **backup**, NOT the withheld action (the withheld one is `deploy_pages.sh` force-push to the public `gh-pages` demo). The sidecar edits don't touch `docs/`, so pushing main changes **nothing user-facing**. Recommend pushing to protect 10 days of work.
2. **Live demo is stale (docs/ dated 2026-06-29).** The ~2,900 profiles added since then (grind output) are **not visible to users** — by design (deploy classifier-blocked). Republishing needs the user (re-export `docs/` + `export_static` + `deploy_pages.sh`).
3. **Grind health: GOOD.** merge_duplicate_species dry-run CLEAN (0 groups); np≥10 truly exhausted (8 non-profilable stubs). Remaining profilable backlog: **np5-9=96 spp (~4 batches), np2-4=3,833 spp, np1=3,419 spp** = ~7,350 long-tail species (~300 batches). Diminishing returns: np1 = species in a single park. **Open strategic question for the user: keep grinding the long tail, or pivot to photo-3rd-source / deploy?**

**🎉 2026-07-04 (Claude) — PRODUCTIZATION P2: STATIC-EXPORT STACK (免费上线 + 秒开 + 零服务器). Built + fully Chrome-verified; NOT yet deployed (deploy needs the user).** This resolves the P1 tension: P1's thin-client was fast but needed a running Python server (Docker/fly.io); the old demo was free (GitHub Pages) but shipped a 71 MB blob (~12 MB gzip first paint). P2 pre-bakes the API's access pattern into **static JSON shards** any dumb host serves — free like Pages, fast like the API, no server.
- **What/where:** `scripts/export_static.py` → **`site/`** (gitignored). Reuses `parklife/api.py` for identical shapes. **5,046 files:** `data/parks.json` (index) · `parks/<id>.json` (3,190) · `park-photos/<id>.json` (821) · `species/<bucket>.json` + `species-parks/<bucket>.json` (512 each, bucketed by `id % 512` to stay under Cloudflare's 20k-file cap) · `search-index.json` · `meta.json`. Build: `.venv/bin/python -m scripts.export_static` (~70 s).
- **Client:** `webapp/app.js` converted from `/api/*` fetch to static shards (new `getJSON`+`data*` layer + `SPECIES_BUCKETS=512` — **MUST match** exporter `BUCKETS`). Search reimplemented client-side over `search-index.json`. `sw.js` SHELL made **relative** + data rule `/api/`→`/data/` (the old absolute `/index.html` 404'd under GitHub Pages' `/<repo>/` subpath — real bug, fixed). `manifest` already relative.
- **Verified in real Chrome:** the *existing* `webapp/e2e_test.py` passes **unchanged** against the static site (`BASE=http://127.0.0.1:8091/`, all features, 0 console errors) + extra checks (client search サクラ→18 cards, and a `/parklife/` subpath load) PASS. **First paint 137 KB gz** (was ~12 MB); park click ≤45 KB; species open ≤25 KB; search-index 821 KB (once).
- **Deploy (written, NOT run — needs user):** `scripts/deploy_pages.sh` publishes `site/` as an **orphan** commit force-pushed to `gh-pages` (throwaway index → main's tree/index untouched; parentless → no history accumulation → git-slim stays permanent). Plumbing validated on a throwaway dir. Ran `git gc --prune=now` → local **`.git` 128M→30M** (unreachable cruft only; history intact).
- **➡️ NEXT (all need the user):** (1) run `scripts/deploy_pages.sh` + one-time set GitHub Pages source to `gh-pages` branch/root; (2) THEN `git rm --cached docs/parklife-data.json` (+ retire the whole `docs/` demo) = permanent git-slim, since the static site supersedes it. `serve_api`/`webapp/` + `Dockerfile`/`fly.toml` still work as dev/reference but are no longer the deploy path.

**🎉 2026-06-27 (Claude) — PRODUCTIZATION P1 COMPLETE + git-slim done. The new thin-client stack is production-ready; the legacy 69 MB-blob demo is now superseded (kept live only until the user deploys).** One session delivered, all verified in Chrome (committed regression suite `webapp/e2e_test.py`, 0 console errors), all pushed to main:
- **Stack:** `webapp/` SPA (Leaflet + MarkerCluster) + stdlib read-only API (`parklife/api.py` + `scripts/serve_api.py`) serving data on demand from the DB. Run: `.venv/bin/python -m scripts.serve_api` → <http://127.0.0.1:8787/>.
- **Features:** map clusters · park panel w/ sort+month+group-toggle · species modal (4-lang profile + photo gallery + park-local photos) · parking filter · "show more" pagination · **species→map reverse view** · **URL deep-linking** (`#park/`/`#species/`, shareable + back/forward) · ja/en/zh/zhT.
- **Infra:** **PWA** (manifest + service worker, offline shell / network-first API) · **gzip** (park index 797 KB→142 KB) · **Docker/fly.toml/DEPLOY.md** (stdlib image, `PARKLIFE_DB_RO=1` immutable DB) · **git-slim** (history rewrite, `.git` 2.6 GB→26 MB, force-pushed; backup `/tmp/parklife_dotgit_bak`).
- **Data:** GBIF media = 3rd photo source → visible photo coverage **94.2%→98.0%**. zh names at ceiling (96%). Backups trimmed (4 June `.bak` kept).
- **➡️ GENUINE NEXT STEPS ALL NEED THE USER:** (1) **deploy** the new stack (`fly deploy` / any container host) → THEN retire `docs/parklife-data.json` from tracking = permanent git-slim; (2) **park-coverage expansion** (#1, needs a tier decision like the 地区公園 one + a long enrichment run); (3) **いきものログ** (blocked on manual CSV download). Autonomous polish left is marginal (PNG icon set, bbox viewport loading). **History was force-pushed — a stale clone (Codex) must `git fetch && git reset --hard origin/main`.**

**🏗️ DONE 2026-06-27 (Claude) — productization P1: thin-client SPA + read-only API, FULL STACK WORKING (the git-slim enabler):** Built a stdlib-only JSON backend (`parklife/api.py` + `scripts/serve_api.py`, `ThreadingHTTPServer`) **AND** a thin-client SPA (`webapp/index.html` + `app.js`, Leaflet+MarkerCluster) that fetches data on demand instead of the 69 MB blob. **One command serves both, same origin:** `.venv/bin/python -m scripts.serve_api` → <http://127.0.0.1:8787/>. Static files served from `webapp/` (path-traversal-guarded); `/api/*` endpoints alongside. **VERIFIED end-to-end in real Chrome** (`/tmp/pl_e2e.py`, playwright `channel="chrome"`): map clusters render, park click → 616 species cards in 11 groups, species modal → photo gallery + 3-section profile, ja/en/zh/zhT toggle live-updates, search works — **0 console errors, E2E PASS.** **Proves the blob is unnecessary:** map load 0.8 MB (gzip ~150 KB), park click ~176 KB (now incl zh names on cards), species open ~5 KB lazy. **All work ADDITIVE — `docs/` (old demo) untouched as rollback; only HANDOFF.md is a tracked change. Nothing committed/pushed.** Backups in `/tmp/parklife_webapp_bak/`. **NEXT (the real git-slim, DESTRUCTIVE — needs explicit go-ahead):** host the data artifact out of regular git tracking (LFS / Release asset / let serve_api be the source) + history-rewrite to purge old 60+ MB blobs (`.git` 2.6 GB) + force-push main. Also UI follow-ups not yet ported from old demo: parking filter, group checkbox/sort, per-park gallery photos, seasonal view (data already exposed by the API). See top Recent-sessions entry.

**✅ DONE 2026-06-27 (Claude) — photo backfill (the post-expansion photo track):** Commons P18 (`wikicommons_hero`, hero species 5,705→12,355) + iNat galleries (`ensure_inat_taxon`→`collect_species_photos`) lifted **visible photo coverage 85.4%→93.6%** (7,868/8,408). Caught + fully reverted a `best_match` `results[0]`-fallback bug that injected ~135 wrong tids/photos (see Recent sessions + Photo-gallery follow-up); merge/romaji dry-runs CLEAN. Demo re-exported+pushed (65.9 MB, commit 39522f0). Backup `bak_pre_photo_20260627`. **NEXT photo work needs a GBIF-media/Flickr 3rd source** (~540 obscure GBIF-only visibles have neither iNat nor Commons). Otherwise pivot to productization / いきものログ.

**✅ DONE 2026-06-25 (Claude) — 地区公園 expansion (park-coverage milestone #2):** added park-type **3 地区公園 ≥5ha** to the P13 filter → **+670 new parks, 2,412 → 3,082** (user picked this tier over 運動公園/area-relax). `scripts/p13_seed.py` now env-configurable (`P13_KDP`/`P13_SUFFIX`/`P13_DEDUP_M`/`P13_MIN_AREA_M2`; dedup default bumped 500m→1km per the old dup-bug fix). New seeds in `data/seeds/<pref>-p13ext.json` (47 separate files; originals untouched). **Enrichment queue drained, 0 failed** (144 tasks gbif+inaturalist+ebird ×47 + finishers; cache made existing parks no-ops). **662/670 new parks got species** (8 remote/low-record); overall coverage **3,065/3,082 = 99.4%**. Post-ingest `merge_duplicate_species` (re-introduced by new GBIF synonyms): NULLed 5 bogus tids (38 rows) + merged 96 groups (net −98 species), then re-dedupe. `fix_romaji_ja_names` clean (0). **FINAL: 3,082 parks / 47 prefs / 24,394 species (export shows 23,717 visible) / 428,893 park_species / 645,502 obs.** Demo re-exported (`parklife-data.json` 59.5→**61.6 MB**, under GitHub 100 MB) + copied to `docs/`. DB backups `data/parklife.db.bak_pre_chiku_20260625` + `bak_pre_merge_chiku`. **NEXT:** the expansion REOPENED a profiling backlog — **404 unprofiled visible species** (np≥10=24, np5-9=56, np2-4=153, np1=171; see Active #1). **User decided 2026-06-25: log it, start AFTER a context clear ("我clear后再开始"), before the photo track.** THEN photo image-source expansion (GBIF/Wikimedia-Commons fallback for the ~854 no-iNat-taxon visible species).

Project is in maintenance + enrichment mode. **🌏 NATIONWIDE EXPANSION COMPLETE 2026-06-20 (Claude): 2,412 parks across ALL 47 prefectures** (was 461 Kanto), enrichment queue fully drained (152 tasks, **0 failed**). **FINAL NATIONWIDE NUMBERS:** 2,412 parks / **2,377 with species (98.5%)** / 47 prefectures; **22,694 species** (was 9,761); **354,951 park_species rows** (was 126,808); 487,167 observations. Per source: **GBIF 332,061 · iNaturalist 135,643 · eBird 17,306**. Maintenance (`merge_duplicate_species`/`fix_romaji_ja_names`) dry-runs CLEAN post-ingest. **eBird key now lives in gitignored `data/.ebird_key`** (ebird.py reads env→file fallback, never git/plist). **✅ DEMO REPUBLISHED at national scale 2026-06-22 (Claude) — the "single-JSON demo dies at scale (~150MB+)" assumption was WRONG:** full national `export_html` is only **59.5 MB** (under GitHub's 100 MB hard limit; >50 MB warning is advisory), live at <https://paranoid2droid.github.io/parklife/>. A spatial bbox backend is still nice-to-have for browser perf with 2,412 markers, but is NOT required to ship the demo. **Curation + tooling COMMITTED + PUSHED to main** (680d975 sidecar/code/43 seeds, 865823d docs/demo). DB stays gitignored/local (backups `/tmp/parklife.db.pre_national_pilot` + `pre_national_full`). Pre-expansion baseline was 461 parks / 9,577 visible species. **✅ PROFILING + zh/en + PHOTOS ALL DONE (2026-06-22) — see the top Recent-sessions entry for specifics.** The national-expansion profiling backlog (~1,169 species) is fully cleared (profiles now **8,156**, 4-lang); zh中文名 **99.8%** + en英文名 **99.6%** coverage of visible species; iNat photo backfill lifted visible photo coverage to **88.8%** (7,137/8,041, species_photo 59,062 rows). Residual gaps are all permanent skips / genus stubs / the iNat photo ceiling. **No remaining profiling/curation/iNat-photo fuel** — next work is the productization/spatial-backend track, a GBIF-Commons photo fallback, or いきものログ. **COMMITTED + PUSHED to main 2026-06-22** (680d975 sidecar/code/seeds, 865823d docs/demo); national demo is LIVE at 59.5 MB. The gitignored DB stays local as always. **NATIONAL EXPANSION mechanics (2026-06-20, Claude):** P13 seed generalized to all 47 prefectures (`.venv/bin/python -m scripts.p13_seed <slug…|all>`; no-arg still = original 4); nationwide ≥5ha biodiversity-type parks = **1,930 NEW (ALL 都道府県立, 0 municipal noise — the existing filter cleanly selects flagship prefectural parks)** → total would be ~2,391 (5×). See `scripts/p13_recon.py` for the per-pref table. **Pilot (aichi/osaka/kyoto) ran the full pipeline** seed→load→iNat+GBIF→normalize→dedupe and **validates the productization thesis: 100% park coverage** (197/197 parks got species purely by iNat/GBIF radius, zero website scraping), **+1,999 species** (366 with katakana ja-name = visible/profilable & currently UNPROFILED → national expansion REOPENS the profiling backlog; 1,632 GBIF scientific-name-only = invisible), **+28,196 park_species rows** (126,808→155,004). Extrapolated nationwide ≈ +19k species / +280k ps-rows → demo JSON ~50MB→~150MB+ (**confirms the productization memo: the single-JSON demo dies at national scale — build the spatial bbox backend BEFORE any nationwide demo**). **⚠️ FIXED a destructive bug in `scripts/load_seeds.py`:** its UPDATE overwrote geocoded lat/lon (+ official_url etc.) with seed NULLs on EVERY re-run (first pilot load wiped coords on 171 Kanto parks); now `COALESCE(?, col)` — never clobbers an existing value with a seed NULL. **Mandatory for nationwide (load_seeds re-runs over all parks).** eBird skipped this pass (needs `EBIRD_API_KEY` env; birds already covered by iNat Aves + GBIF). Driver `scripts/enrich_pilot.py`; per-pref CLI now works on any slug (relaxed the 4-pref guards in inaturalist/gbif/ebird). DB change LOCAL (backup `/tmp/parklife.db.pre_national_pilot`); demo NOT rebuilt (user's call). Maintenance (`merge_duplicate_species` / `fix_romaji_ja_names`) ran clean post-ingest. Code + Pages site at <https://github.com/paranoid2droid/parklife>; demo published from `docs/` at <https://paranoid2droid.github.io/parklife/> (export ~48 MB, last republished 2026-06-20). **7,011 species** have curated profiles in ja/en/zh/zhT. **DATA ENRICHMENT (occurrence) DONE — all three external sources ingested:** GBIF 113,129 obs / 9,170 species (now the LARGEST source), iNaturalist 107,121 / 2,632, eBird 8,926 / 203 birds; GBIF vernacular-name pass also ran. Scripts `scripts/{gbif,ebird,gbif_vernacular}.py` (caches ~482 files each). **Only the lowest-priority いきものログ remains un-ingested** (see Active #3). **np≥2 tiers 100% CLEARED**. **🎉🎉 ENTIRE PROFILABLE SPECIES BACKLOG CLEARED (A150–A233 done, 2026-06-16→17).** Every visible species with a real katakana ja-name + scientific_name now has a 4-language profile. **All 8 content deferrals RESOLVED 2026-06-17** (web-verified katakana ja-names, then profiled in A233): Wisteriopsis reticulata→ムラサキナツフジ, Rhombodera basalis→マルムネカマキリ, Ptecticus aurifer→キイロコウカアブ, Terpnosia nigricosta→エゾハルゼミ, Primula vialii→ホザキサクラソウ, Lunella coronata カンギク (verified CORRECT via JAMSTEC BISMaL — homonym of 寒菊, not a mismatch); Latona bicolor + Torularia had junk 動物界/植物界 ja-names NULLed (no verified 和名, kept as invisible species rows); Streptococcus mutans DELETED (out-of-scope oral bacterium). Lithospermum murasaki merged into L. erythrorhizon (2026-06-16). **ONLY irreducible remainder = 7 genus-level stubs (属 names, legitimately un-profilable):** Meretrix [ハマグリ属], Monstera adansonii [モンステラ属], Platambus [モンキマメゲンゴロウ属], Pseudogobio [カマツカ属], Sympetrum [アカネ属], Tenodera [オオカマキリ属], Tetragnatha [アシナガグモ属]. **The per-batch profile loop is DONE — pick up another Active TODO (item 2 maintenance, or productization/expansion track in memory).** **Sidecar data (`data/species_profiles_extra.json`) is pushed & current via the sidecar-only commit workflow.** **🆕 EXPORT SPLIT (2026-06-06, Claude):** the demo dataset is no longer inlined in `docs/index.html`. `export_html` now writes a tiny stable `docs/index.html` (~85 KB) plus a sibling `docs/parklife-data.json` (~46 MB) that the page `fetch()`es at runtime; the two unreferenced dumps (`parklife.json`, `park_species.ndjson`) were dropped from publish. This halves per-commit git bloat and stops index.html churning every batch. **Two consequences:** (1) the 46 MB data file is now the slow-to-push artifact (same throttle caveat that used to apply to index.html — `git add docs/ && commit && push`, retry until a window catches it); (2) `file://` open no longer works — preview via `cd docs && python -m http.server`. No curation data is at risk — all profiles live in the pushed sidecar; the demo just lags until docs/ is pushed. P13 official-URL coverage **443/461 parks (96%)**. **🅿️ PARKING REBUILT 2026-06-20 (Claude):** now 3-state (1/0/NULL) + new `park.parking_source` evidence-tier column; canonical rules in `parklife/parking.py` (SSOT). OSM-absence is no longer a confident 0 — it's NULL (unknown), since OSM under-mapping ≠ no parking. Final: **390 yes / 33 no (all text-backed) / 38 unknown**. See CLAUDE.md "Parking classification: evidence over coverage". DB change LOCAL; demo needs export regen to show the new NULLs.

> **⚠️ PROCESS LESSON (2026-05-31, Claude/Opus session):** Build every batch's species list ONLY from the freshly-run candidate query output you have actually SEEN rendered — never hand-type or reconstruct candidate names from memory. Two batches (A135, A137) were wasted because candidate names were fabricated from a truncated/delayed display: the names didn't exist in `species`, so seed silently skipped them (no profile rows, candidate count didn't drop) and they polluted `species_profiles_extra.json` as orphans (later purged in A136/A138). **Also: when doing per-id DB UPDATEs, never hardcode an `id` you haven't just re-queried** — a fabricated-id fix (`/tmp/fix135.py`) corrupted 4 unrelated species (Fissidens taxifolius, Baeochila horvathi, Aphis gossypii, Entoloma atrum); restored from `data/parklife.db.bak_pre_cleanup_20260529`. Prefer `WHERE scientific_name='…'` over `WHERE id=N`. **If tool output renders with a one-turn delay, run the whole batch via a single `finish*.sh` that appends count/remain/next-candidates to a file, then read that file the NEXT turn before building the following batch.**

**DB integrity (2026-05-29)**: 0 orphan rows across alias/profile/photo/observation; 0 dead parks; 0 dupe scientific_names; 0 iNat-id collisions. 2 NULL-species observations remain (descriptive labels, harmless). 6 ASCII-prefix common_name_ja: 2 with no iNat ja-name, 4 without inat_taxon_id (Sawara homonym resolved 2026-05-27 via kanji disambig `サワラ（椹）` / `サワラ（鰆）`). Remaining NULL-sci visible placeholders (2026-05-29): 23 generic-category words (スイレン属 np=3, ドングリ/ヤエザクラ/コオロギ/トンボ/カエル/バッタ np≤2, rest np=1 — シダ類/タンポポ/カエデ/エリカ/ダリア etc.) — all low np broad categories, not worth bespoke fixes. The two clear-junk non-taxa (コミュニケーション, タケノコ) were deleted 2026-05-29; the resolvable sakura cultivars (カワヅザクラ→558, コブクザクラ, ジンダイアケボノ) were given scientific_names.

**species_profile np≥2 tiers 100% CLEARED (A1–A149, 5,024 profiled / 2026-06-01).** np=2 finished at A149 (verified 0 candidates ≥2 parks remaining). **Next tier = np=1 (the long tail, ~4,000 visible single-park species).** Use the SAME Active #1 query but change `HAVING np >= 2` to `HAVING np = 1` (or `>= 1` to also re-catch any stragglers), restart alphabetically by `s.scientific_name` from A. Reuse the proven `/tmp/finish_generic.sh` + robust-rekey batch template (`/tmp/profile_batchA149.py` is a clean model). **Carry-over watch items still apply:** placeholder ja-names (lowercase-romaji, kanji junk `菌界`/`菌類`), homonyms (verify `taxon_group`), junk en names ("Species code: Xx"/"Algae"/"Animal"/lowercase) — fix in DB before profiling. **Code (A104):** `GENERIC_EN_NAMES` in `scripts/seed_species_profiles.py` includes `animal`/`plant`. **The 4 species corrupted by a bad-id fix (Fissidens taxifolius, Baeochila horvathi, Aphis gossypii, Entoloma atrum) were restored from `bak_pre_cleanup_20260529`.**

## Blocked / waiting

**✅ 2026-08-14 (Claude) — zh round-2 resume DONE & DEPLOYED** (was the 08-13 dangling item; chain finished 22:11 JST after that session ended). +651 zh names exported + force-pushed to gh-pages `858e5726` (live 200). best_match fix already committed `7ce07c3b`. No blockers remain from the zh track. Two NEW curation follow-ups it surfaced ↓ (moved to Next up → Follow-ups).

## Next up

Active TODOs only. Shipped items are pruned to git log + Recent sessions. Pick from the top unless the user redirects.

### Active

1. **species_profile curation — 🎉🎉 COMPLETE (again). Expansion-reopened backlog CLEARED (B1–B19 done).** *(8,509 sidecar entries / 2026-06-26)*
   - **🔄 REOPENED 2026-06-25 by the 地区公園 expansion (+670 parks):** the new parks brought **404 unprofiled visible species**. **✅ ALL 19 batches done 2026-06-26 (Claude), HEAD has B19 + republished demo.** Tiers cleared in order: np≥10 (24) → np5-9 (56) → np2-4 (ALL) → np1 long tail (ALL). sidecar 8133→**8509** (B18 +18, B19 +21 finished the np1 tail, alphabetically Hiatula→Zombrus).
   - **Profilable backlog now = 0.** The np-query (with `AND s.common_name_ja NOT LIKE '%属'`) returns only **3 irreducible non-profilable stubs:** `Hiatula diphos` (ja-name is junk placeholder `旧名ムラサキガイ`), `Phaneropterinae` (亜科 subfamily stub), `Metriogryllacris magna` (garbled Chinese ja-name `小羽蟋蟋螽`, never fixed — a raspy cricket; would need a web-verified 和名 before profiling, low value). **Do NOT keep re-running the np query expecting new candidates** — only fresh iNat/GBIF ingestion reopens it.
   - **Demo REPUBLISHED 2026-06-26 (Claude):** `export_html` → `docs/parklife-data.json` now **62.6 MB** (23,717 visible species / 3,082 parks / 427,015 pairs), pushed to main (commit 93a126c). merge_duplicate_species --dry-run CLEAN (0 groups). The +31 genus-属 stubs in the raw np-query stay as-is (display their Japanese 属 name).
   - **zh-Hans aliases added this round only where standard CN names are unambiguous:** 黄粉虫 (Tenebrio molitor), 剑鱼 (Xiphias gladius), 斑蝠 (Vespertilio murinus), 松江鲈 (Trachidermus fasciatus), 棘鳞蛇鲭 (Ruvettus pretiosus). Obscure moths/beetles/mollusks left zh-text-only (no fabricated `species_alias` rows — see memory warning).
   - **➡️ NEXT WORK = pivot off profiling** (see the "NEXT WORK" bullet below + Active #2/#3 and the productization track).
   - **STATUS (pre-expansion): the original national backlog was DONE.** Every visible species with a real katakana ja-name + scientific_name was profiled in 4 languages as of 2026-06-22. All np tiers (≥2, then =1) cleared, and all 8 content deferrals resolved 2026-06-17 (see Recent sessions for the verified names). **The only remaining np=1 query hits are 7 genus-level stubs** — `Meretrix` (ハマグリ属), `Monstera adansonii` (モンステラ属), `Platambus` (モンキマメゲンゴロウ属), `Pseudogobio` (カマツカ属), `Sympetrum` (アカネ属), `Tenodera` (オオカマキリ属), `Tetragnatha` (アシナガグモ属). These are genus-only records (the source page named only the 属); a species profile is not meaningful, so they stay as-is and display their real Japanese genus name. **Do NOT keep re-running the np query expecting new candidates.**
   - **➡️ NEXT WORK = pivot off profiling. Demo is CURRENT (republished 2026-06-20 with all A150–A234 profiles + parking rebuild).** Remaining tracks: (a) Active #3 いきものログ (last external source); (b) **#1 park-coverage / national expansion** (P13 already at 461; next = other 43 prefectures — coupled with the productization track in memory); (c) Active #2 latent-data maintenance (periodic). The productization probes are done (photo-license ~80% commercial-OK; GIS=P13 confirmed). Ask the user which.
   - **✅ All deferrals resolved (Claude, 2026-06-16→17), DB fixes are LOCAL (gitignored), backups `/tmp/parklife.db.pre_deferrals` + `pre_merge_murasaki`:** Lithospermum murasaki merged into L. erythrorhizon; 5 deferrals web-verified + renamed + profiled in A233 (ムラサキナツフジ / マルムネカマキリ / キイロコウカアブ / エゾハルゼミ / ホザキサクラソウ); Lunella coronata カンギク confirmed CORRECT (JAMSTEC BISMaL — homonym of 寒菊) + profiled; Latona bicolor + Torularia junk 動物界/植物界 ja-names NULLed (kept as invisible rows, no verified 和名); Streptococcus mutans deleted (out-of-scope oral bacterium).
   - **A199–A218 done (Claude/Opus, 2026-06-13→14):** +506 profiles (6,148→6,654), 20 batches. Pattern each batch: backup DB → apply `/tmp/fixesA2NN.py` (Python param-binding UPDATEs — handles cultivar quotes & kanji; ja-kanji/romaji/Chinese→katakana + wrong/lowercase en, always `WHERE scientific_name=…`) → write `/tmp/profile_batchA2NN.py` (robust-rekey template) → run → `seed_species_profiles` → verify table +N & remain −N → commit **sidecar only** → push. DB backups `/tmp/parklife.db.pre_A199…A218` (en/ja-name fixes are LOCAL-only, regenerated into the demo later). Batch sizes 23–26 (taken to a clean genus boundary; some <26 due to skipped stubs/deferrals). ~125 en-name fixes + ~10 ja-name fixes total. **CAUTION:** count entries before running — confirm `added==len(ENTRIES)` (A200 miscount needed a `…b.py` supplement).
   - **WORKFLOW (adopt): use `/tmp/finish_np1_sidecar.sh`** (commits ONLY the sidecar JSON per batch → tiny, pushes instantly). Robust-rekey model = latest `/tmp/profile_batchA177.py`.
   - **🆕 DEMO PUBLISH CHANGED (2026-06-06, Claude):** `export_html` no longer inlines data into `docs/index.html`. It now emits a tiny stable `docs/index.html` (~85 KB) + a sibling `docs/parklife-data.json` (~46 MB, fetched at runtime); the old `parklife.json`/`park_species.ndjson` dumps were dropped from publish (`deploy.py` DEPLOY_FILES). To publish the demo: `git add docs/ && git commit -m "docs: regenerate demo" && git push`. The **46 MB `parklife-data.json`** is now the slow-to-push artifact (the same throttle that used to hit index.html) — **retry the push until a window catches it.** `file://` preview no longer works; use `cd docs && python -m http.server`.
   - **⚠️⚠️ PUSH LESSON (still applies, now to the data file):** committing a ~46 MB docs artifact EVERY batch makes packs huge and the throttled upload disconnects mid-sideband (`Connection to …github.com port 22/443 timed out`; `ls-remote` still works → it's the data upload, not connectivity). Sidecar-only commits (tens of KB) push fine. Keep `docs/` out of per-batch commits; regenerate + push the demo occasionally with retries.
   - **TWO TYPO-DUP MERGES this session** (pattern for future): `Cocccinella septempunctata`(3 c's)→merged into `Coccinella septempunctata`; `Davidia ivolucrata`(missing n, ハンカチノキ)→merged into `Davidia involucrata`(ハトノキ), preserving ハンカチノキ as an alias. Method: repoint observation.species_id, move/clean aliases, delete park_species + species row, re-run `scripts.dedupe`. Backups `/tmp/parklife.db.pre_merge{2499,344}`.
   - **⚠️ ANOMALY to investigate:** A151's robust-rekey DROPPED `Aeluropus littoralis` as "not in DB" even though `SELECT … LIKE 'Aeluropus%'` shows it IS present — so its stored `scientific_name` likely has a hidden/zero-width char or trailing space that `norm()` doesn't strip, OR there are near-dup rows. Same smell: `Allomyrina dichotoma` reappears as an np=1 candidate although it was profiled in A133 (so that profile attached to a DIFFERENT/dup row). **Next session: check for hidden-char / duplicate species rows (`SELECT id, hex(scientific_name) FROM species WHERE scientific_name LIKE 'Aeluropus%' OR scientific_name LIKE 'Allomyrina%';`), merge/clean dups, then these resume normally.**
   - **DB-junk watch (np=1):** kanji ja-names (Abies mariesii 大白檜曽→オオシラビソ), en "Animal"/lowercase/odd ("Bele","convictfish","Sergeants"), HTML-entity & romaji-junk zh aliases — fix in DB before profiling (`/tmp/fixesA150.sql` model; `DELETE FROM species_alias WHERE raw_name LIKE '%&#x%'` purge is handy). **Watch ja-name/identity MISMATCHES** (A151 skipped `Adscita sp.` ja=サホコケムシ[bryozoan on a moth genus] and `Agrilus chujoi` ja=クロビロウドサビカミキリ[longhorn name on a jewel-beetle] — verify identity before profiling; both still np=1 candidates).
   - **ROBUST BATCH METHOD (adopt this — solved the recurring breakage):** In the batch `.py`, after building `ENTRIES`, normalize-rekey every key to the DB's exact `scientific_name` (NFKC + strip zero-width/`\s+`→space + lowercase, map norm→exact via a `SELECT scientific_name FROM species`) and DROP any key whose normalized form has no DB match. This kills hidden-char mismatches (which silently sent A140 entries to "missing") AND auto-removes fabricated orphans. Template lived at `/tmp/profile_batchA142.py`; pair with a `finish_*.sh` that runs batch→`seed_species_profiles`→`export_html`→`cp docs/`→commit→push→print `profile_count` + next-24-candidates + `remain np=2` to a file you read the FOLLOWING turn (tool output rendered with a ~1-turn delay this session). **After each batch verify profile_count rose by ~N AND remain dropped by ~N before trusting it.**
   - **Sidecar workflow** (`data/species_profiles_extra.json`): every entry MUST include `common_name_en` (if DB has NULL or generic) + `aliases.{zh-Hans}` (if DB lacks it) alongside the 4-language profile. zhT auto-derived via OpenCC. See `BATCH_TEMPLATE.md` at repo root.
   - **Query next batch** — change `np >= 5` to `np >= 4` (then 3, 2, …):
     ```sh
     sqlite3 data/parklife.db "SELECT s.scientific_name, s.common_name_ja, COUNT(DISTINCT ps.park_id) AS np FROM species s JOIN park_species ps ON ps.species_id=s.id LEFT JOIN species_profile sp ON sp.species_id=s.id WHERE sp.species_id IS NULL AND s.scientific_name IS NOT NULL AND s.common_name_ja IS NOT NULL AND s.common_name_ja != '' AND SUBSTR(s.common_name_ja,1,1) NOT BETWEEN 'A' AND 'Z' AND s.common_name_ja NOT LIKE '%・%' AND s.common_name_ja NOT LIKE '%（%' GROUP BY s.id HAVING np >= 4 ORDER BY np DESC, s.scientific_name LIMIT 30;"
     ```
   - **Typo guard**: when hand-writing `zh-Hans` aliases, double-check for garbled chars before running — A47/A58 had to be fixed (大阪绿步甲, 近缘牙甲, 蚊子草). A bad alias inserts into `species_alias`; purge with `DELETE FROM species_alias WHERE raw_name='<bad>';` then re-seed.
   - **Per-batch loop**: write `/tmp/profile_batchN.py` → run → `.venv/bin/python -m scripts.seed_species_profiles` → commit the **sidecar only** every batch (tiny, pushes instantly). Regenerate the demo separately and occasionally: `scripts.export_html` → `cp data/export/index.html data/export/parklife-data.json docs/` → `git add docs/ && commit && push` (the 46 MB `parklife-data.json` is the slow one; retry until it lands).
   - **np≥9 cleanup history**: A24 cleared romaji-rescued high-np (ヤハズエンドウ et al). A25–A28 cleared the regular np=9 species (90 entries). A29 closed the last 2 edge cases (Malus toringo had romaji ja `ko-nashi` → fixed to ズミ in DB; Macrogerris is a subgenus treated as ツツジ属 was).
   - **np=8 cleanup history (2026-05-28, A30–A37)**: cleared 144 species across 8 batches; sidecar backfilled ~74 common_name_en + ~40 zh-Hans aliases. Demo 34.8 → 35.2 MB.
   - **np=7 cleanup history (2026-05-29, A38–A44)**: cleared all 158 np=7 species across 7 batches (Acanthosoma→Vibidia); ~75 common_name_en + ~38 zh-Hans aliases backfilled. Demo 35.2 → 35.6 MB.
   - **np=6 cleanup history (2026-05-29, A45–A53)**: cleared all 207 np=6 species across 9 batches (Acanthochitona→Weigela) + 2 stragglers; also fixed placeholder ja-name `Bucephala albeola` カモ→ヒメハジロ in DB. Demo 35.7 → 36.2 MB.
   - **np=5 cleanup history (2026-05-30, A54–A67)**: cleared all 318 np=5 species across 14 batches (Abelia→Zaranga). Demo 36.2 → 37.0 MB. Note `R.arborea`/シャクナゲ has a mangled sci name (treated as genus-level rhododendron profile) — leave as-is.
   - **np=4 cleanup history (2026-05-30, A68–A82)**: cleared the whole np=4 tier across ~15 batches (Abelmoschus→Yucca). 4 ja-name DB fixes (local-only): Acer japonicum, Allium chinense→ラッキョウ, Phedimus aizoon→キリンソウ, Phillipsia domingensis→ニクアツベニサラタケ. Demo 37.0 → 38.1 MB. np≥4 coverage 100%.
   - **Remaining tiers**: np=3 → ~490 (current), np=2 + np=1 → the long tail (all visible ~4,000). Batch of 22–24 costs ~12–16k tokens.

2. **Periodic latent-data maintenance** *(run after every major ingestion; cheap)*
   - `.venv/bin/python -m scripts.merge_duplicate_species` — collapses synonym pairs sharing one `inat_taxon_id`; NULLs bogus tids covering many unrelated species. **Last run 2026-06-17: 0 dups, 0 bogus tids — clean.**
   - `.venv/bin/python -m scripts.fix_romaji_ja_names` — rescues ASCII-prefix `common_name_ja` placeholders via iNat `locale=ja`; preserves romaji form as ja alias. **Last run 2026-06-17:** iNat had no ja-name for the 2 it flagged, but a manual sweep found **5 romaji-prefix unprofiled species** (the np-query had always excluded ASCII-prefix names): renamed+profiled 4 in A234 (Pedinotrichia parallela→オオクロコガネ **np=15**, Polyphylla laticollis→ヒゲコガネ, Crepidiastrum nakaii→ヤクシワダン, Fibularia coffea→コーヒーマメウニ; romaji kept as ja alias), deleted the 5th (Streptococcus agalactiae, Group-B strep, out-of-scope). **0 romaji-prefix species remain.** New iNat ingestion re-introduces these — re-sweep with `SUBSTR(common_name_ja,1,1) BETWEEN 'A' AND 'Z'`.
   - Both have `--dry-run`; run after eBird/GBIF/iNat enrichment passes.

3. **いきものログ ingest — ❌ DECIDED: SKIP (2026-08-08, evaluated & closed). Do NOT re-add as an open option.**
   - **Verdict (see Recent sessions 2026-08-08):** its high-value systematic data (モニタリングサイト1000 = Satoyama Birds 103k / Mammals 25k, with coords + individualCount) is **already on GBIF**, which we ingest by radius (~51% of a sample fell within 3 km of a park). Only pure citizen reports are いきものログ-exclusive; access is a WAF (curl TLS reset) + a multi-step questionnaire that currently errors server-side. License is fine (PDL1.0) but **not worth it**. Instead of scraping, that session added GBIF rich-field ingestion (individualCount/observer/last_year) — that was the better payoff.
   - **This is a closed decision, not a backlog item.** Only re-open if the user explicitly asks, or if いきものログ ships a real API. Don't list it under "NEXT: …" menus.

4. **TMG SPA parking parse via scrapling** *(deferred, low priority)*
   - 32 `tokyo-park.or.jp/park/<slug>/index.html` URLs are JS-rendered SPA shells. Current `scripts/extract_parking.py` + `scripts/reclassify_parking.py` fail on them (stub returns 0 text). Would need `scrapling install` (~200 MB Chromium). Not worth the dependency for 32 parks alone.

### Follow-ups (defer until needed)

- **✅ 74 synonym-collision merge groups from zh round-2's `ensure_inat_taxon` — RESOLVED 2026-08-14.** DB backup `data/parklife.db.bak_pre_synmerge_20260814`. Of the 74 (after NULLing 3 cross-kingdom): **merged 66 genuine synonym pairs** (lossless — same-ja or one-empty; loser's binomial kept as sci alias, e.g. `Trema orientalis`+`orientale` now on one row) via `merge_duplicate_species`; **excluded 8 by NULLing their tid** to keep them separate — **6 genus-vs-species traps** (bare-genus aggregate rows that must NOT collapse into a specific species, else fabricate species-level presence: `Tetragnatha`/アシナガグモ属 np=376, `Agelena`/クサグモ属 np=216, `Tibellus`/シャコグモ属 np=171, `Ulva`/アオサ np=32, `Artemisia`/ヨモギ属 np=5, `Stigmaeopsis` np=1) + **2 divergent-ja distinct-looking species** (`Minolia subangulata`/カドコシタカシタタミ vs `punctata`; `Osteomeles schweriniae`/シラゲテンノウメ vs `schwerinae`). `merge_duplicate_species --dry-run` now **0/clean**. Audit tools: `scratchpad/classify.py` (genus-trap detector), `dump74.py`, `audit_tidgroups.py`.
  - **3 NULLed cross-kingdom bad tids (done 2026-08-14, reversible):** `Volva volva`(id 18577, mollusk)←128074(`Volvariella bombycina`, fungus); `Sinica sinica`(id 4657)←203153(`Parus major`); `Mimachlamys crassicostata`(id 24844, scallop)←1210487(`Iris domestica`, plant). These matched via pre-existing WRONG ja-names in the rows. zh names auto-rejected by fetch_inat_zh's fail-closed gate, so no zh contamination.
  - **✅ `Sinica sinica` (id 4657) corruption — RESOLVED 2026-08-14 (it was a two-organism conflation, not a single bad row).** Aliases revealed a MOTH + a BIRD merged into one row: 97 obs `Sinica sinica` (=`Pryeria sinica`/ミノウスバ moth) + 50 obs `Parus major` (great tit). **Split by raw_name:** moth obs→id 1101 (`Pryeria sinica`/ミノウスバ, np→114); bird obs→id 24750 (`Parus major`, tid 203153 — gave it its missing ja `ヨーロッパシジュウカラ` + zh `欧亚大山雀`, np 41); junk `ミノウスバ`[en] alias deleted; corrupted row 4657 deleted; re-deduped. Fix script `scratchpad/fix_sinica.py`.

- **Chinese name coverage** *(12,822 / 24,295 visible = 52.8% as of 2026-08-14 after zh round-2; was 12,123/49.9% round-1, 43.6% pre-iNat)*
  - **2026-08-14 (Claude): zh round-2 DEPLOYED — +651 zh (12,123→12,822 spp, 52.8%).** The 08-13 chain (`ensure_inat_taxon` gave tids to no-tid visibles → `fetch_inat_zh_names` re-run) finished 22:11 JST *after* that session's last deploy, leaving the +651 unexported. This session rebuilt `site/` + force-pushed gh-pages `858e5726` (live 200). Same fail-closed pipeline; DB backups `bak_pre_ensure_tid`/`bak_pre_inat_zh`, reversible via `DELETE FROM species_alias WHERE status LIKE 'inat-zh%'`. **Next zh lever unchanged:** the ~11.5k still-missing are the no-tid tail iNat can't reach + species iNat has no zh for. Re-run after new iNat/GBIF ingestion (idempotent).
  - **2026-08-13 (Claude): +1,518 zh via NEW `scripts/fetch_inat_zh_names.py` (iNat `locale=zh-CN`).** Mirrors `fetch_inat_ja_names.py`: pulls iNat's curated simplified-Chinese `preferred_common_name` for tid-bearing visible species with no zh alias. **Fail-closed identity gate** (iNat `results[tid].name` must == our `scientific_name`, caught 166 stale/wrong tids), rejects kana (Japanese fallbacks) + Han-echoes of the ja name, collision-guarded, zh-Hant via OpenCC. Inserts `status='inat-zh'`/`'inat-zh-hant'` → reversible in one `DELETE FROM species_alias WHERE status LIKE 'inat-zh%'`. Cached under `data/cache/inat_zh/`. **Sourced from iNat, NOT fabricated** — this cracked the "un-nameable tail" (obscure GBIF mollusks/leafcutter-bees/spiders DO have iNat zh names: 蔷薇切叶蜂 np=401, 红树拟蟹守螺 381, 铜绿金光伪蜻 263…). DB backup `data/parklife.db.bak_pre_inat_zh`. Maintenance dry-runs clean. **Deployed (gh-pages `2fc1828`, live 200) + script pushed to main (`543f364`).** To extend: re-run after new iNat/GBIF ingestion (idempotent), or point it at the 5,453 no-tid candidates via `ensure_inat_taxon` first.
  - **2026-08-13 (Claude): +33 hand-curated zh (zh-Hans+zh-Hant via OpenCC), highest-np confident species only** — 13 birds (喜鹊/大白鹭/攀雀…), 7 butterflies, 13 fish+1 toad. Method: fail-closed ja-verification before INSERT into `species_alias`. `opencc-python-reimplemented` in venv (s2t).
  - **Remaining ~12.2k missing zh** = the no-tid tail (5,453 have no `inat_taxon_id` so the iNat pass can't reach them) + species iNat has no zh for (1,181 skipped this pass). Do NOT fabricate (memory warning). Next lever: run `ensure_inat_taxon` (⚠️ fix the `best_match` `results[0]` bug first, see Photo-gallery follow-up) to give no-tid species a tid, THEN re-run `fetch_inat_zh_names`.
  - Manual curation for high-frequency species still missing zh — `query top --group X` and seed `species_profile` zh fields directly.
  - Try newer sp2000 release when published (current is `chinacol2023`).
  - Keep raw aliases first-class in `species_alias` (lang=`zh-Hans`/`zh-Hant`); never overwrite Japanese names. Avoid English-Wikipedia-title backfill (tested 2026-05-02, unsafe).

- **Park coverage beyond P13** *(post-`e1f93b2`, 461 parks)*
  - P13 is 2011 snapshot — when MLIT ships a 2024+ refresh, re-run `scripts/p13_seed.py` (idempotent on slug hash). **Bump the dedup radius to ≥1 km** to avoid repeating the 2026-05-18 duplicate-park bug (500 m missed 590 m-offset entries).
  - Consider relaxing filters: 街区/近隣公園 ≥ 5 ha exist and were filtered out; 自然観察園 inside larger parks remain invisible.
  - Beyond P13: 国営公園 (~17 nationwide), 区立 nature observation gardens, 都市林 < 5 ha that are genuinely forested — would need manual curation or OSM `leisure=park`/`landuse=forest` cross-reference.

- **Photo gallery** *(visible-species photo coverage **93.6%** = 7,868/8,408, verified 2026-06-27 after the post-地区公園-expansion backfill; species_photo ~74.8k rows, license 100% populated, 83.0% of photo'd species have ≥1 commercial-usable image)*
  - **2026-06-27 backfill DONE (Claude):** post-地区公園 expansion the no-photo gap had reopened to ~1,224 visible (1,174 no-iNat-taxon + 50 iNat-no-photo). Ran `wikicommons_hero` (Commons P18 via Wikidata, re-ran over all 24,371 binomials → picked up expansion adds; Commons hero species **5,705→12,355**, +6,650) → `ensure_inat_taxon` (resolved iNat ids for new GBIF-only visibles; stopped after the visible-resolution rate flattened, ~494 visible resolved) → `collect_species_photos 3000` (+~9.7k iNat gallery rows, 2,156 species) → `backfill_photo_license`. Coverage **85.4%→93.6%**. DB backup `data/parklife.db.bak_pre_photo_20260627`. Demo re-exported (parklife-data.json 62.6→**65.9 MB**) + pushed (commit 39522f0).
  - **⚠️ `ensure_inat_taxon` has a SLOPPY fallback bug** — `best_match` (scripts/ensure_inat_taxon.py:76) returns `results[0]` when no exact/genus/ja match, so obscure GBIF-only species not on iNat get a WRONG taxon_id (+ wrong default photo), and `collect_species_photos` then fetches the wrong taxon's gallery. This injected ~135 bad assignments today (5 bogus tids that fuzzy-matched many unrelated ferns/plants via the ja-name path, + 78 2-member collision groups where a no-ja-name dup landed on a pre-existing species' tid — e.g. Amanita gemmata→muscaria's tid, Rapa rapa→Pieris rapae). **All reverted** by diffing against the pre-run backup: any today-assigned tid that wasn't a strong (exact/genus) match and collided with another species was NULLed, and its wrongly-fetched iNat photos deleted (~630 photo rows). After cleanup `merge_duplicate_species`/`fix_romaji_ja_names` dry-runs are CLEAN (0). **If you re-run ensure_inat_taxon, fix best_match first** (drop the `results[0]` fallback, or require matched_term==query / kingdom-consistency) — otherwise repeat this revert.
  - **Remaining ~540 visible w/o photo:** mostly GBIF-only obscure/marine taxa with NO resolvable iNat taxon AND no Wikidata P18 image, plus the 9 genuinely-distinct species that today wrongly shared one tid (Charybdis bimaculata/subornata, Gaultheria miqueliana/pyroloides, etc. — now correctly tid-less, awaiting a real per-species source). **To go further needs a GBIF-media / Flickr fallback** (the 3rd-hero-source idea below).
  - Truncate verbose Commons `extmetadata.Artist` HTML to first author when "Multiple contributors" lists appear.
  - Add Flickr / GBIF media as a 3rd hero source for taxa with neither iNat nor Commons coverage.

- **Species sort: regional `sp.n` tiebreaker** *(post-`a85c1ba`)*
  - Current `freq` sort: `pair.oc` desc primary, `sp.n` desc tiebreaker. When two species both have 1 record at the selected park, global spread tiebreaker favors 関東-wide commons over locally-clustered species. Precomputed `sp.regional_n` per (species, prefecture) would fix this; defer until a user complaint surfaces.

- **`data/parklife.db.bak*` cleanup** — 7 backups, ~738 MB total local-only. Safe to keep 1 recent good snapshot; older ones (`.bak`, `.bak2`, `.bak3` from 5/18–5/23) can go. Not gitignored issue since `data/parklife.db*` is excluded.

### Long-term direction — productization & national expansion *(new north star, discussed 2026-06-08; build not started, both feasibility probes DONE 2026-06-17)*

Strategic goal: turn the dataset into a **shareable, possibly monetized PWA** for **JP + international** users, with **nationwide park coverage** (denser map). Full rationale in memory `project_parklife_productization.md`. This is the long-horizon plan. **Profiling is COMPLETE** (no longer a competing grind), so this is now a primary candidate for next work. **Both cheap probes already ran** (2026-06-17): photo-licensing gate is mild (~80% of species keep ≥1 commercial-usable photo via Wikimedia Commons; `species_photo.license` column added), and the nationwide-GIS path is confirmed (MLIT 都市公園 = P13, already ingested). Key decisions already made:

- **Center of gravity flips** at scale from "scrape park sites" → "park polygon/coords → iNat/GBIF aggregation". Expansion = mostly running data, not new scrapers.
- **Form factor: PWA first** (one codebase JP+intl, 0 platform cut on Stripe, fast from existing SPA). Native (Capacitor wrap) only after validation.
- **Photo licensing is a HARD gate once monetized**: many iNat photos are CC-BY-NC. Add `photo_license`/`photo_attribution`, filter to CC0/CC-BY, add attribution/ToS/privacy. Moat = the integrated park×species×season data, not photos.
- **Data volume forces a spatial backend**: nationwide DB → GB scale; the 44 MB JSON fetch dies. Need bbox query API (SpatiaLite/Turso or PostGIS), marker-clustering + viewport loading, polygon storage (national parks are areas). Strip the 44 MB data from git first (`.git` already 2.2 GB).
- **Scale by AREA threshold, not admin level.** Sources: 国土数値情報 都市公園データ (official name+polygon+area GIS), 自然公園地域データ, OSM. Priority 国営→国立/国定→残り43都道府県立→大型市立. Taxonomy → GBIF Backbone / iNat taxon (drop manual_species at scale).
- **Phases:** P0 compliance+git-slim · P1 architecture (spatial backend, PWA, clustering) · P2 geographic expansion · P3 monetization validation (Stripe + 1 paid feature; B2B) · P4 native/UGC if proven.
- **Cheap feasibility probes to run first:** (a) iNat photo license distribution (how many survive a commercial filter); (b) 国土数値情報 都市公園 schema check.

## Recent sessions

### 2026-08-14 (Claude/Opus) — 🇨🇳 finished the dangling zh round-2 resume → DEPLOYED, coverage 49.9%→52.8%
- **Root cause:** the 08-13 zh round-2 chain (`ensure_inat_taxon`→`fetch_inat_zh_names`) finished 22:11 JST, *after* that session's last export/deploy, so **+651 zh names sat in DB unexported**. best_match fix was already committed (`7ce07c3b`). This session ran the resume: `export_static` (24,295 spp) → force-push gh-pages **`2fc1828`→`858e5726`** (live 200; 美女蛤/小口拟虾虎 confirmed live). **zh now 12,822 spp = 52.8%** (was 12,123/49.9%).
- **Pre-deploy DQ catch:** `merge_duplicate_species --dry-run` was **77 groups, NOT clean** — all NEW synonym-collisions from round-2's tid assignment (round-1 was 0). NULLed **3 confirmed cross-kingdom bad tids** (Volva volva/Sinica sinica/Mimachlamys crassicostata — matched via pre-existing wrong ja-names; no zh contamination thanks to the fail-closed gate). **Deferred** the 74 legit-synonym merges (some have divergent ja → need per-case review). Two new Follow-ups filed (74-group merge + the pre-existing `Sinica sinica` np=134 corrupted row). Audit tool: `scratchpad/audit_tidgroups.py`.
- kana/echo leak checks 0. No DB backup taken this session (only NULLed 3 tids; reversible from values in the merge follow-up).

### 2026-08-13 (Claude/Opus) — 🇨🇳 zh coverage 43.6%→49.9% via iNat locale=zh-CN + closed the いきものログ churn
- **NEW `scripts/fetch_inat_zh_names.py`** (iNat `locale=zh-CN`, fail-closed identity gate, kana/echo reject, collision-guard, zh-Hant via OpenCC, `status='inat-zh'` reversible). **+1,518 visible species got Chinese names** (12,123/24,295 = 49.9%). Cracked the supposed "un-nameable tail" — iNat curators DO have zh for the obscure GBIF mollusks/bees/spiders (蔷薇切叶蜂 np=401…). Non-fabricated. DB backup `data/parklife.db.bak_pre_inat_zh`; maintenance dry-runs clean. **Deployed gh-pages `2fc1828` (live 200); script pushed to main `543f364`.** Full detail in the "Chinese name coverage" follow-up.
- **Process fix (user flagged inefficiency):** いきものログ was **already evaluated & DECIDED-SKIP on 2026-08-08** (its high-value data is on GBIF) but stayed listed as an open "NEXT:" option, so a fresh /clear re-proposed it. Rewrote Active #3 → **❌ CLOSED-SKIP, not a backlog item** + purged it from the productization memo / MEMORY.md / todos so it stops resurfacing.
- **Next zh lever:** fix `ensure_inat_taxon` `best_match` bug → assign tids to the 5,453 no-tid candidates → re-run `fetch_inat_zh_names`.

### 2026-08-12 (Claude/Opus) — ✅ hand-curated the 3 janame mismatches + relaunched gbif_admin animalia full
- **Closed the `data/janame_review.json` hand-curation** the 08-09 session deferred (annotated in-file with per-row `resolution`). DB backup `data/parklife.db.bak_pre_janame_20260812`. Used `scripts.merge_species_pair`:
  - **ヤマガラ**: merged 3 synonym rows → `Sittiparus varius`(7139, accepted). `Poecile varius`(3111) was mislabeled **オーストンヤマガラ** (that 和名 = *Sittiparus owstoni*); purged its owstoni-specific aliases first, then merged 3111+`Parus varius`(14456) in. obs 2765.
  - **カルガモ**: merged `Anas poecilorhyncha`(3042, インドカルガモ s.s., not wild in JP) → `Anas zonorhyncha`(579, accepted JP カルガモ); the 1048 records were old-broad-concept eastern カルガモ. obs 5447.
  - **ニイニイゼミ**: NO CHANGE — false positive (`Platypleura kaempferi` is correct; iNat's `Planopleura` is a genus-spelling variant).
- **`gbif_admin --kingdom animalia` FULL — DONE** via `scratch_run_admin.sh` (nohup, direct — NOT the queue, so no 4h TASK_TIMEOUT that killed the earlier 922/14835 partial). Chain rc=0: gbif_admin(14942 spp) → dedupe → export_static (12:25–12:27 UTC). **park_species 861,239 → 926,929 (+65,690); admin:municipality tier now 474,022 pairs** (huge coordinate-less animal-museum-specimen recovery, now > onsite 452,907; still behind the webapp opt-in toggle, headline counts onsite only). Merges folded in & stable post-dedupe.
- **DEPLOY auto-running** via `scratch_deploy_after.sh` (nohup detached — user opted into auto-deploy-on-completion). It waited for `ALL DONE` then ran `SKIP_BUILD=1 scripts/deploy_pages.sh`; built orphan commit `0ee16e7` (5058 files), force-push to origin/gh-pages **in progress** (large push, throttle-prone per past notes — may retry). **Verify: `tail data/deploy_after.log` for `DEPLOY OK`, then curl gh-pages for HTTP 200.** If the push timed out, re-run `SKIP_BUILD=1 scripts/deploy_pages.sh`.
- **NOTE:** `data/janame_review.json` only had the top-np 3 flagged (the 08-09 scan was np-ordered/limited); the long tail was not fully scanned. A full `scripts.fix_janame_mismatches.py` re-scan (iNat call per visible species, ~hours, cached) could surface more — deferred, low yield.
- **Scratch files left (untracked, safe to delete):** `scratch_run_admin.sh`, `scratch_wait_admin.sh`, `scratch_deploy_after.sh`, `data/gbif_admin_full.log`, `data/deploy_after.log`. DB backup `data/parklife.db.bak_pre_janame_20260812`.

### 2026-08-09 (Claude/Opus) — 🧬 profiling resume → found backlog is contaminated; mismatch auto-fix proven unviable
- **Resumed profiling with a quality gate** (iNat-ja verify before profiling): +14 verified species (sidecar 15632, batches 1-2 committed). But discovered the remaining ~3130-species backlog is **~60-85% mis-maps/unconfirmable** (top-50-by-np: 19 confirmed; next np=1 block: ~1/7) — the residue prior passes correctly skipped. Profiling it is low-value; flagged to user.
- **User pivoted to fixing the mismatches. Investigated exhaustively → NOT safely automatable.** The mismatches are two hard classes: (1) real GBIF species with a wrong 和名 mis-attached (Polistes canadensis→オオスズメバチ; the 0-GBIF-JP gate fails — it has 30 JP recs); (2) mostly **taxonomic synonyms/splits**, not errors. `scripts/fix_janame_mismatches.py` (bidirectional iNat + GBIF-backbone synonym guard) still flags カルガモ (Anas poecilorhyncha vs zonorhyncha split), subspecies (オーストンヤマガラ), spelling variants (Platypleura/Planopleura) as false positives — auto-NULL would delete correct 和名 from common species. Made it **REVIEW-ONLY** (`data/janame_review.json`); genuine errors are rare + buried, need hand-curation.
- **Conclusion / recommendation:** the session's safely-automatable data-quality work is DONE (abundance, noise-kingdoms, scrape re-resolution, rich fields, ナナフシ). Remaining issues are low-value (long-tail profiling) or not safely automatable (name mismatches ≈ synonyms). Recommend NOT burning more tokens here. If continuing anyway, only profile iNat-ja-verified species (safe but low-value).

### 2026-08-08 (Claude/Opus) — 🧬 DATA-QUALITY #6: いきものログ evaluated → GBIF rich-field ingestion instead
- **いきものログ verdict: skip the scrape.** Its high-value systematic-survey data (モニタリングサイト1000) is ALREADY on GBIF — datasets "Monitoring Sites 1000 Satoyama Birds" (103k recs, coords, `individualCount`) / "Satoyama Mammals" (25k) — which we already ingest by radius (~51% of a sample fell within 3km of a park). Only pure citizen reports are いきものログ-exclusive, and its access is a WAF (curl TLS reset) + multi-step questionnaire that currently errors server-side. License is fine (PDL1.0, commercial OK) but not worth it. Site pattern saved to the scrapling skill.
- **Did instead — GBIF rich-field ingestion** (the real win): schema `observation.{last_year,observer_count,individual_count}` + `park_species.{last_year,observer_count}`. `eventDate`/`recordedBy` were already in the slim GBIF cache → `scripts/backfill_gbif_richfields.py` extracted last_year (recency) + observer_count (confidence) with NO network — **402,095 (park,species) pairs** (403k/417k GBIF obs have last_year; 135k are pre-2000 museum specimens = staleness we can now flag; 228k have observer_count). `individualCount` added to `GBIF_OCC_KEYS` + `gbif.py` so survey counts are captured on the next GBIF pull; `dedupe` prefers `individual_count` as the abundance signal else obs_count; `api.park_detail` emits `ly`/`ob`. Populates park_species on the queued dedupe.
- **QUEUED continuous task — full GBIF `--refresh`.** `gbif.py --refresh` (bypass cache, re-fetch, UPDATE rich fields incl individualCount on existing rows; verified 1 park → 27 rows gained individual_count max 50). Queued **47 `pending: gbif <pref> --refresh`** (park-density order: hokkaido/tokyo/hyogo/… first) + dedupe + export, AFTER the admin ingest, in `data/run_queue.txt`. Per-prefecture tasks are naturally bounded (~10-40min each, won't hit the 4h TASK_TIMEOUT like gbif_admin did). launchd-autonomous, no Claude tokens, runs over days. This retro-captures individualCount everywhere + refreshes recency/coverage (~2 months of new obs since the 2026-06 pull).
- **➡️ Remaining follow-ups:** (a) webapp use of `ly`/`ob` — flag stale (old-only) species + observer-confidence (perception). (b) EFFICIENT alt to the full refresh (not built): targeted pull of just the moni1000 GBIF datasets by datasetKey (~128k recs) — the full refresh re-fetches ~95% iNat records that have no individualCount, so it's wasteful-but-comprehensive; the targeted pull gets the survey abundance cheaply if the full refresh proves too slow.

### 2026-08-08 (Claude/Opus) — 🧬 DATA-QUALITY #5 SYSTEMIC: scrape-normalization error class (audit + re-resolve)
- The ナナフシ→Myronides mis-map was one instance of a CLASS — website-scrape katakana that the Wikipedia normalizer resolved to a wrong/obscure species. Fixed the class, not the name. **`scripts/audit_scrape_only.py`**: 122 scrape-only species (no iNat/GBIF/eBird corroboration); GBIF-Japan-count gates them → 44 suspect (0 JP records). **`scripts/reresolve_scrape_species.py`**: re-resolves each by JA name via iNat's ja-vernacular index (fuzzy, strips 「(広義)/属」). **Applied: 13 RE-MAP** (fix real mislabeled natives — ツワブキ Farfugium grande→japonicum, ヒカゲツツジ→Rhododendron keiskei, シロタエギク→Jacobaea maritima…) **+ 20 SUPPRESS** (bare category words, no sci name — コオロギ/タンポポ/ドングリ… → `data/suppressed_species.json`, read by `dedupe`). **10 REVIEW** left for a human call (real rare cultivars ホザキサクラソウ/ラベンダー mixed with noise ビスカッチャ/モンステラ属 — NOT auto-touched). DB backup `bak_pre_reresolve_20260808`. Reflects on the queued dedupe. **Open: the 10 REVIEW items** (recommend suppress ビスカッチャ/シャクナゲ/バラ/モンステラ属/ルピナス属, keep テマリカンボク/ホザキサクラソウ/ミューレンベルギア/ラベンダー/ナンバンサイカチ).

### 2026-08-08 (Claude/Opus) — 🧬 DATA-QUALITY #3+#4 during the admin-ingest wait (both ship with the queued dedupe→export)
- **Noise-species exclusion** (`scripts/dedupe.py`): park_species now drops non-perceivable microbes — bacteria/viruses/archaea always; protozoa/chromista only when they lack a JA vernacular (so slime molds ススホコリ + seaweeds ワカメ/ヒジキ stay). 542 species / 1057 rows; 0 animalia/plantae/fungi affected; raw rows kept in `observation`. Applies on the next dedupe.
- **ナナフシ/竹节虫 disambiguation**: the bare name had mis-normalized to tropical Myronides glaucus (1 park); re-pointed the sighting + ナナフシ/竹节虫 aliases to Ramulus mikado (ナナフシモドキ, id 842, 203 parks; now carries the 竹节虫 zh alias), NULLed Myronides' bogus 和名 (orphaned → drops from park_species). `manual_species.json` guards regression. DB fix applied locally; reflects on the queued dedupe.
- **Admin-ingest sped up** (was ~844 spp/4h → 60h ETA): `gbif_admin` MAX_REC 3000→600 (~5×, also drops 1-record-municipality noise) + sci→taxonKey disk cache (resume skips done species without live calls); killed the slow in-flight process so the fast one took over; queue padded to 6 `gbif_admin` lines + dedupe + export so it finishes autonomously. Watch `data/run_queue.log`.

### 2026-08-07 (Claude/Opus) — 🧬 DATA-QUALITY #2: abundance ranking (perception match) — DONE, ships with the admin-tier export
- **Problem:** park lists didn't match a visitor's experience — 76% of park_species were single-record (oc=1) and the "freq" sort used our source-ROW count, not the real per-source sighting count each ingestion already fetched but buried in `characteristics` text. Source-direction fix (user steered here): promote the buried count to a numeric abundance signal.
- **DONE & verified:** `observation.obs_count` + `park_species.abundance` (MAX onsite obs_count per pair — MAX not SUM so iNat monthly rows don't multiply; admin tier excluded). `scripts/backfill_obs_count.py` ran (ALTER + parsed 751k rows + set 443k pairs, batched alongside the live ingest, 0 lock errors). gbif/inaturalist/ebird.py store obs_count going forward; `dedupe.py` recomputes abundance; `api.park_detail` emits `ab` + ORDER BY abundance; webapp freq-sort by `ab`; sw **v8**. Verified in Chrome: 深大寺 bird group leads カルガモ/ヒヨドリ/カワラバト/スズメ, insects セイヨウミツバチ/ホシホウジャク, long tail sinks; 0 errors. Committed. **Ships automatically** — the queue's trailing `dedupe`→`export_static` (after the admin ingest) rebuilds park_species with abundance and emits `ab`.
- **➡️ Follow-ups (source-direction, from the assessment):** (2) GBIF `recordedBy`+`eventDate` are ALREADY in the slim cache (`GBIF_OCC_KEYS`) — re-read to add observer-diversity + recency, no re-fetch. (3) GBIF re-pull with `basisOfRecord` filter (drop FOSSIL/LIVING_SPECIMEN=captive → fixes キリン/コアラ leakage) + kingdom scope (drop bacteria/virus/protozoa ~700 noise species) + individualCount. (4) point-in-polygon attribution via P13 geometry (kills marine-at-inland leakage). (5) eBird frequency/abundance endpoints for birds.

### 2026-08-07 (Claude/Opus) — 🧬 DATA-QUALITY: 'admin:municipality' evidence tier — steps 1-5 DONE, full ingest + deploy pending
- **Problem (user):** very common insects (e.g. ナナフシ/竹节虫, common beetles) missing from most parks. **Root cause = data-source ceiling, not tuning:** (a) open point-occurrence data for JP insects is thin in absolute terms — ナナホシテントウ has only ~2,671 georeferenced GBIF records in ALL Japan; (b) **we discard ~66% of GBIF's JP beetle records** (601k/916k) because `hasCoordinate=true` drops museum specimens that carry only romaji stateProvince+county. Radius/pagination/quality-grade tuning (RADIUS_KM=1.5→3 ≈3.8× insect richness; but per_page=100 top-N-by-count cap crowds out low-abundance species at rich parks — a separate real bug) only redistributes the thin georeferenced layer.
- **Solution = new evidence tier (mirrors parking's tiered confidence).** Recover coord-less specimens by matching their 市区町村 to parks. Validated: A1 (geocode fine locality→point, full precision) is LOW yield (~11-20% Nominatim hit on romaji micro-localities, no kanji verbatim in GBIF) → abandoned. **A2 (municipality match) ~doubles coverage** and fails *safe* (misses, not wrong-attaches; prefecture-scoped → no same-name collisions).
- **BUILT & verified end-to-end this session:** (1) schema — `observation.evidence_tier` + `park_species.evidence_tier` (DEFAULT 'onsite'; migrated live DB, 758k obs backfilled; backup `data/parklife.db.bak_pre_evidtier_20260807`). (2) `parklife/admin_match.py` — romaji(state,county)→kanji-muni via cached Nominatim + **3 fail-closed gates** (pref-slug known / muni exists as a park муни / geocoder-pref consistency); strips 〇〇郡 prefix. (3) `scripts/gbif_admin.py` — pull GBIF coord-less per species → resolve → insert obs `location_hint='GBIF-admin'`, `evidence_tier='admin:municipality'` (idempotent, caches `data/cache/gbif_admin/`). (4) `scripts/dedupe.py` — aggregates strongest tier per pair (onsite > admin). **Proof:** ナナホシテントウ 459→740 parks, カブトムシ 297→415; global park_species tiers `admin:municipality=399, onsite=446687`; matches correct (Tateyama-machi→中新川郡立山町, gun-strip OK).
- **(5) DONE — tier surfaced end-to-end, verified in Chrome + e2e PASS.** `parklife/api.py`: `park_index.n` and `park_detail.n` count **onsite only** (honest headline, unchanged for pre-admin data); admin cards flagged `a:1` in the species array. `webapp/app.js`: default hides `a:1`; opt-in checkbox "含周边区域记录 (+N)" (`showRegional`, localStorage `pl_regional`) reveals them; admin cards get a `📍<municipality> 区域` badge + grayed/dimmed (`.card.regional`), 4-lang (`regional`/`regionalBadge`/`regionalNote`). `webapp/sw.js` VERSION **v7**. `webapp/index.html` CSS for `.reg-toggle`/`.reg-badge`. Re-exported `site/`. Verified: park 97 default n=130 (0 regional shown), toggle ON → +1 card "📍奥多摩町 region", 0 console errors; full `e2e_test.py` PASS.
- **(6) RESUMING under launchd queue — 1st pass hit the 4h TASK_TIMEOUT at ~1188/14835 species (rc=124); dedupe+export ran on partial data (site now has abundance ranking + ~8% of admin tier, 126,324 admin park_species rows).** Fixed the resume-cost bug (`taxon_key` now disk-cached) and re-queued `gbif_admin ×3 → dedupe → export_static` (kickstarted); each 4h firing advances, caches warm (9158 geocodes, 1293 species pulls), a finished pass is a fast no-op. Watch `tail data/run_queue.log` for the trailing `done(...): export_static` after 3 gbif_admin lines clear. **Original run details:** user chose ALL ANIMALS. In-session `nohup`/background gets reaped at turn boundaries, so this runs via the **autonomous queue** instead: 3 `pending:` lines in `data/run_queue.txt` — `gbif_admin --kingdom animalia` → `dedupe` → `export_static`. launchd (`com.parklife.queue`, fires every 30 min, 4h/task, fcntl-locked, resumable) kickstarted 2026-08-07 12:16Z; progress in `data/run_queue.log` + per-species in `data/gbif_admin_full.log`. 14,835 animal species (~922 done in-session; caches `data/cache/gbif_admin/` + `data/cache/nominatim_muni/` make resume cheap). **To check status:** `tail data/run_queue.log` (look for `done(...): export_static`), or `SELECT COUNT(*),COUNT(DISTINCT species_id) FROM observation WHERE location_hint='GBIF-admin'`. When export_static shows done, **(7) deploy** `scripts/deploy_pages.sh` — now SAFE (tier handled, sw v7); verify `curl -s https://paranoid2droid.github.io/parklife/data/parks/97.json` shows an admin `a:1` species. (8) quality upgrade: replace ad-hoc Nominatim in `admin_match.py` with an authoritative 市区町村 romaji↔kanji + Heisei-merger-alias gazetteer (lifts ~54% pair hit-rate + kills residual mis-geocode). Also still-open cheap wins on the georeference layer: bump RADIUS_KM 1.5→3 + paginate species_counts past the per_page=100 top-N cap.

### 2026-08-07 (Claude/Opus) — 🚀 DEPLOYED free-tool track #1–#4 to gh-pages (was the last blocker)
- **User reported "many parks show an empty species panel."** Root cause: **gh-pages was stale** — deployed `app.js` predated the #1–#4 commits and `sw.js` was still `parklife-v5`; local `site/app.js` (latest) was fine. Verified data was identical on both sides (3190 parks, same 16 truly-empty `n=0` parks) and a real-Chrome probe over 40 random parks with the current code found **0 empty, 0 console errors** — so it was purely a not-yet-deployed problem, not a code/data bug.
- **Ran `scripts/deploy_pages.sh`** (Pages/Actions outage had cleared): rebuilt static export (3190 parks · 24690 species · 512 buckets, ~84s) → orphan commit `c1a8738`, force-pushed **5058 files** to `origin/gh-pages`. Verified live `app.js` now == local latest and `sw.js` == `parklife-v6` (returning PWA users' cache-first shell auto-replaces). Allow a few min for Pages CDN.
- **➡️ NEXT:** live demo is now current with #1–#4. Remaining: optional #5 custom domain (needs user), or a new track (monetization gate, いきものログ, resume species grind). The 16 `n=0` parks are a genuine data gap (no occurrence records), unrelated to this fix.

### 2026-08-07 (Claude/Opus) — 🏗️ PRODUCTIZATION (free-tool track) #4: mobile + PWA polish — FREE-TOOL TRACK #1–#4 COMPLETE
- **#4 DONE & committed (`0b39525`).** Safe-area insets (iOS notch/home-indicator) on header+panel; iOS meta (status-bar-style, app title) + android mobile-web-app-capable; **bigger touch targets** on ≤760 (≈34px buttons, roomier chips) + **modal → bottom sheet** on phones; **PWA install**: `beforeinstallprompt` → '📲 Install app' button in About (Android/Chrome), iOS Safari gets an A2HS hint, hidden when standalone, `appinstalled` clears it, 4-lang; **sw.js VERSION v5→v6** so returning PWA users get the new shell (added a "bump on every shell change" comment — **remember this on future shell edits**). Verified at phone viewport + iOS UA + android install-event; all 5 e2e suites pass, 0 console errors.
- **✅ FREE-TOOL PRODUCTIZATION TRACK #1–#4 ALL DONE** (credits · Parklife naming+About · near-me · season · UX polish · mobile/PWA). Everything is committed to main and built into local `site/`. **ONE thing remains: the deploy**, still blocked on the GitHub Pages/Actions outage. When it clears: `SKIP_BUILD=1 scripts/deploy_pages.sh` ships #1–#4 together; verify `curl -s https://paranoid2droid.github.io/parklife/data/meta.json` shows `generated` ≥ 1786041646 and `seasonCounts` present.
- **➡️ NEXT after deploy:** optional #5 custom domain (needs user), or a new track (revisit monetization gate, いきものログ, or resume species grind). Ask the user.

### 2026-08-07 (Claude/Opus) — ✨ discovery UX polish (user feedback): season checkbox filter + reachable/labelled buttons
- Three fixes from live local review: (1) **discovery buttons were unreachable after opening a park** (only in empty-state) → promoted 📍/🌸 to **persistent header buttons** (`587c7f8`); (2) **season view was a flat wall of ~700 cards** → now **grouped by taxon with a checkbox filter row** (All + per-group chips w/ counts; each group capped 24 + show-more; `seasonGroups`/`toggleSeasonGroup`/`toggleSeasonAll`); (3) **header buttons relied on slow native `title` tooltips** (useless on touch) → now show **text labels** (📍 Near me / 🌸 In season, 4-lang) with full phrase as title; tagline hides ≤760px. Commits `f9e64b2` (+`587c7f8`). All 5 e2e suites pass, 0 console errors.

### 2026-08-07 (Claude/Opus) — 🏗️ PRODUCTIZATION (free-tool track) #3b: "in season this month" discovery (auto-loads current month)
- **#3b DONE & committed (`be986cf`) — this is the export-dependent half deferred by #3a.** Empty-state now has a **🌸 in-season button**; `discoverSeason()` defaults to the **current month** (`new Date().getMonth()+1`, per user's "根据当前日期自动加载") and renders a prevalence-ranked species grid + a month `<select>` to browse; cards open the species modal. 4-lang, re-localizes on lang switch (`inSeasonView` flag).
- **Export change** (`scripts/export_static.py`, section 8): new **`season/<m>.json`** shards (m=1..12) = `[[species_id, in_season_park_count], ...]` ranked desc, from `park_species.months_bitmap` (visible+reachable only; NULL/0 = year-round/unknown → excluded; tiebreak overall np). Client joins to `search-index` so **no name/photo data duplicated**. `meta.json` gains `seasonCounts[12]`. Data reality: **2,371 visible species (12.6%) carry month data**; per-month in-season counts `[317,338,552,816,882,884,767,706,697,729,606,353]` (Jan→Dec) — plants(花の見ごろ)+insects+birds dominate.
- **Tests:** new `webapp/e2e_season_test.py` (auto-loads current month, show-more, month-switch, card→modal, i18n re-render). **All 5 e2e suites** pass in real Chrome, 0 console errors.
- **⚠️ Deploy:** #3b touches the exporter, so `site/` was rebuilt locally (season shards present). The pending deploy (still blocked on the **GitHub Pages/Actions outage** — `githubstatus.com` still shows Actions major_outage as of this entry) will carry #1+#2+#3a+#3b together. When Pages recovers: `SKIP_BUILD=1 scripts/deploy_pages.sh` (site/ already built) — OR rebuild first if the DB changed since.
- **➡️ NEXT free-tool item: #4 mobile/PWA polish** (or wait for the deploy to unblock and verify live).

### 2026-08-07 (Claude/Opus) — 🏗️ PRODUCTIZATION (free-tool track) #3a: "parks near me" discovery list
- **#3a DONE & committed (`3725853`, `webapp/` shell only).** Empty-state panel now has a **📍 near-me button**; `discoverNearby()` geolocates and renders the **12 nearest parks ranked by distance** (haversine via existing `distanceKm`, new `nearestParks()`/`fmtKm()`), each a click-through row with distance + parking pill. Geo-denied/unsupported → fallback message + retry button. 4-lang strings. `renderEmpty()` centralizes the empty-state (also fixed a latent null-deref: `setLang` did `$('ph').innerHTML=` when `#ph` was gone in search/nearby views). Reuses existing geolocation infra (`geolocate`/`autoLocate`/`nearestPark` were already there — only the *list* UI was missing).
- **Tests:** new `webapp/e2e_nearby_test.py` (mocked geolocation → sorted list + click-through; denied-permission → fallback). All **4** e2e suites (`e2e_test`, `e2e_about_test`, `e2e_credits_test`, `e2e_nearby_test`) PASS in real Chrome, 0 console errors.
- **➡️ #3b (deferred, needs export change): global "in season this month" discovery.** `parks.json` has no per-species months (they live only in per-park detail shards), so a global season view needs a NEW precomputed shard in `scripts/export_static.py` (e.g. `season/<month>.json` = in-season species / per-park in-season counts) + rebuild. Per-park month filter already exists (`setMonth`/`monthFilter`/`s.mb`). Left for a follow-up since it's an export+deploy change, not self-contained.
- **⚠️ Deploy note:** #3a is NOT in the currently-deployed gh-pages tree (`47b308b` carries only #1+#2). **The next deploy must rebuild `site/` (`export_static`) so it includes #3a** — still blocked on the GitHub Pages outage (see #2 entry).

### 2026-08-07 (Claude/Opus) — 🏗️ PRODUCTIZATION (free-tool track) #2: named "Parklife" + first-visit About/landing page
- **#2 DONE & committed (`d863e8a3`, `webapp/` shell only).** Product named **Parklife** (was internal パークライフ) across `<title>`, header brand, `manifest.webmanifest`. Clickable header brand (🌿 Parklife) opens a 4-lang (ja/en/zh/zhT) **About overlay**: tagline · what-it-is blurb · **live park/species counts + last-updated** (lazy-fetched from `data/meta.json`, new `dataMeta()` helper + `_meta` cache) · 4-step how-to · a link into the credits overlay. Auto-shown once on first visit (`localStorage pl_seen_about`); suppressed when arriving via a shared `#park`/`#species` link.
- **Tests:** added canonical `webapp/e2e_about_test.py` + `webapp/e2e_credits_test.py`; **`webapp/e2e_test.py` now seeds `pl_seen_about`** via `add_init_script` so the greeting overlay doesn't intercept its panel clicks (real product behavior, tested separately). All 3 suites PASS in real Chrome, 0 console errors.
- **✅ DEPLOYED to gh-pages (user said "上线") but ⛔ BLOCKED ON A GITHUB PAGES OUTAGE — live URL still shows the OLD (2026-08-04) build.** Force-push succeeded twice (`edeeac76` then re-triggered `47b308b`); `git cat-file -p 47b308b:index.html` confirms the pushed tree carries the Parklife shell + today's data (`meta.json generated=1786041646`). But `githubstatus.com` shows **Pages = major_outage, Actions = major_outage** — Pages isn't building, so the live site is stuck on `generated=1785808452`. **NOTHING MORE TO DO but wait for GitHub to recover; the correct build is already on gh-pages and should auto-publish once Pages builds resume.** If it doesn't auto-publish after recovery, a trivial re-push (`SKIP_BUILD=1 scripts/deploy_pages.sh`) re-triggers it. Verify with `curl -s https://paranoid2droid.github.io/parklife/data/meta.json` → expect `generated:1786041646`. Live URL <https://paranoid2droid.github.io/parklife/>.
- **➡️ NEXT free-tool item: #3 discovery (season view / near-me).** Data is in the DB/shards; needs UI surfacing.

### 2026-08-07 (Claude/Opus) — 🏗️ PRODUCTIZATION (free-tool track) #1: data-source credits/attribution overlay
- User picked the **productization direction, positioned as a FREE public tool** (not monetized) → this drops the heavy monetization-compliance (photo CC-BY-NC filtering, Stripe) and leaves a light path. Agreed free-tool backlog order: **#1 credits/attribution (done)** → #2 name+about/landing page → #3 discovery (season view / near-me) → #4 mobile+PWA polish → #5 optional custom domain. Only #2's product name and #5's domain need the user.
- **#1 DONE & committed (`f17509fb`, `webapp/` shell only — NOT yet deployed).** Header **ⓘ** opens a 4-lang (ja/en/zh/zhT) credits overlay: iNaturalist/GBIF/eBird (occurrence) · iNaturalist/Wikimedia Commons/GBIF (photos) · Wikipedia/CoL/GBIF-backbone (names) · MLIT 国土数値情報 P13 + park official sites + OpenStreetMap/Nominatim (parks/map/geo), each with a license chip, a photo-CC note, a GitHub-issues feedback link, and a non-affiliation disclaimer. Discovery from investigation: **per-photo CC attribution was ALREADY rendered** in the species modal (`app.js` ~L415), so #1 only needed the aggregate source page. Implementation: `CREDITS`+`SOURCE_GROUPS` i18n in `app.js`, `openCredits`/`closeCredits`, re-renders on lang change, Escape/backdrop close.
- **Verified in real Chrome:** new `scratchpad/credits_e2e.py` (8 sources present, 4 sections, license chips, GitHub link, all 4 langs re-render, Escape+backdrop close) PASS + existing `webapp/e2e_test.py` PASS — 0 console errors both.
- **➡️ NEXT: #2 name + about/landing page.** Needs the user to pick a product name (current internal name パークライフ / "Parklife"). **Deploy of #1 is PENDING** — recommend bundling the gh-pages redeploy with #2 (both touch the shell) to avoid a force-push per small feature. Deploy = rebuild `site/` (`export_static`) + `scripts/deploy_pages.sh`.

### 2026-08-04 (Claude/Opus) — 🚀 SHIP: pushed 378 backlog commits + redeployed live demo (was 4 wks stale)
- User asked "which direction next"; state review showed **378 unpushed commits** (oldest 2026-07-08, all sidecar/handoff, 0 code) and the **live gh-pages demo last deployed 2026-07-06** — a month of np=2/np=1 grind (sidecar → 15,618) was invisible to users AND unbacked. User chose **落地已有成果** over more grind.
- **Done, all 3 steps:** (1) `git push origin main` → 378 commits backed up (1cc48c33..9df281fb). (2) `.venv/bin/python -m scripts.export_static` → `site/` rebuilt (5,046 files, 24,690 species / 3,190 parks; verified new profiles present — e.g. Aster scaber シラヤマギク 4-lang in shard). (3) `SKIP_BUILD=1 scripts/deploy_pages.sh` → orphan force-push to gh-pages (f305693a→e6df5866). **Live demo now current.** `git gc` reclaimed deploy objects.
- **Note:** local `.git` still **194M** (main history carries old `docs/parklife-data.json` blobs) — the permanent git-slim (retire `docs/` from tracking, see P2 Status entry ➡️NEXT) is still an open follow-up, unrelated to this deploy.
- **➡️ NEXT direction still open** (user deferred choosing a *new* track): productization/spatial backend · いきものログ · or resume np=1 grind (~3,096 clean remaining, diminishing returns). Ask the user.

### 2026-08-03 (Claude/Opus) — np=1 grind (np1-bo..by, +302), sidecar 15,618 [IN PROGRESS]
- Grinding the **np=1 long tail** (single-park species). Query = same as np=2 with `HAVING COUNT(DISTINCT ps.park_id)=1` + skip filters (`NOT LIKE '%属'/%科/%亜科/%の一種/%近似%/%?%/%？%`). **np=1 clean remaining now 3,113.** Alphabetical from A; 12 batches done: bo–by covering A through Assara (bv Althaea–Antennatus, bw Anisostira–Arachniodes, bx Amichrotus–Arisaema, by Anomoia–Assara). **NEXT frontier: continue from `Aster`/`Asterina`/`Astomella`... onward (rest of A → As/At/Au...).** Heavy repeat taxa → type-helpers built inline each batch (geometers, ウワバ Plusiinae, ハマキ tortrix, マダラメイガ, ナガタマムシ Agrilus, ヒラタゴミムシ/マルガタ Amara, マルハキバガ, ハネカクシ Aleochara/Anotylus, ミドリイシ coral, ザルガイ cockle, snails, サビキコリ/コメツキ, ヌカボ/Agrostis, ミミズ Amynthas, サルボウ Anadara, ノメイガ, ヒメハナバチ Andrena, シシウド Angelica, ベッコウ Anoplius, スジコガネ Anomala, ゴケ Anzia lichen, カエルアンコウ frogfish, ヨトウ Apamea, ボヤ Aplidium, アオカスミカメ Apolygus, カナワラビ Arachniodes).
- **Skipped (leave unprofiled):** `Antechinus leo` (Australian marsupial, spurious record + transliteration name); plant-disease-fungus names (`…の白サビ病菌`, `…のクロボ菌病`); `Amblyglyphidodon leucogaster` (ja=バンダイイシモチ may mismatch — verify before profiling).
- Workflow per batch: scratchpad `batch_np1_*.py` (NFKC robust-rekey; pre-run `grep -nP '[\x{AC00}-\x{D7A3}\x{0400}-\x{04FF}]'` hangul/cyrillic check) → seed → sidecar-only commit. DB backup `data/parklife.db.bak_pre_np1_20260803`. Minor DB fix inline: Agonopterix pallidior typo `…ヒラタヒラタ…`→`…ヒラタ…`. Commits bo `44f11d41` → bw `0932dad0`. **Grind continues until token budget exhausted (user directive).**

### 2026-08-02 (Claude/Opus) — 🧹 DB-wide ja-name-quality pass (44 fixed) + 1 synonym merge
- Scanned ALL 18.8k visible species for problematic `common_name_ja` (romaji, ・-transliterations, `属の一種`/`科の一種`/`亜科の一種` stubs, trailing `?`/`？`, `近似種`, mismatches, kingdom-junk). **~110 flagged → 2 acceptable remain** (`Albugo candida` アブラナ・シロサビビョウキン = profiled descriptive name; `Lucinoma minima` ケシツブツキガイの近似種 = honest np=1 near-ID). All fixes DB-local (gitignored; regenerated into demo on next export). Backups `data/parklife.db.bak_pre_bm_20260802` + `bak_pre_namefix_20260802`. Script: scratchpad `namefix_20260802.py`.
- **Romaji→katakana, high-spread trees (Tier A, romaji was literally the reading):** ツルウメモドキ (Celastrus orbiculatus np=186), オオジシバリ (Ixeris japonica np=153), イヌシデ (Carpinus tschonoskii np=134), クマシデ (Carpinus japonica np=112), ケヤマハンノキ (Alnus hirsuta np=106), ヤシャブシ (Alnus firma np=63), アカクラゲ (Chrysaora pacifica, stripped romaji), ヒナイシドジョウ (Cobitis shikokuensis, stripped bracket).
- **Web-verified real 和名 (was ・-transliteration or mismatch):** ツガサルノコシカケ (Fomitopsis pinicola np=34, +profiled), マツゲハゼ (Oxyurichthys ophthalmonema — was `マツゲハゼ近似種`; IS マツゲハゼ), コウシンバラ (Rosa chinensis), アメリカボダイジュ (Tilia americana), メガロドン (Carcharias megalodon np=6, +profiled), オトメフウロ (Geranium dissectum np=7), モンツキウマゴヤシ (Medicago arabica np=6), メランポジウム (Melampodium divaricatum), レモンベルガモット (Monarda citriodora), クロモモドキ (Lagarosiphon major).
- **`…の一種` stubs → clean 属/科/亜科 form** (per kept-stub policy; excluded from future np-queries): ヒメムキタケ属, クモノスカビ属, ネクトリア属, ヌマスギ属, カエデ属, セイヨウショウロ属, ナガタマムシ属, カワモズク属, コバンハゼ属, ヒメムシロ属, マクラガイ科, ヒメハマキガ亜科, カラムシ属 (Boehmeria siamensis was `カラムシsp.`). **Trailing `?` stripped:** シロホソスジナミシャク, ツヤマメシジミ, サンジョウダケシギアブ, オドリコソウチビケシキスイ, キララホコリ, クロゲシメジ（ササクレシメジ）.
- **NULLed (transliteration/mismatch, no verified 和名 → row kept, invisible):** Coccoidea quercicola, Erioscyphella sclerotii, Mollisia ventosa, Pachydiscus haradai + subcompressus, Baculites yokoyamai, Diplomoceras notabile (ammonite/fungi translit), Sardinella aurita (ホソウケクチヒイラギ = mismatched ponyfish name, Atlantic fish), Viola hiyamae (was ペチュニア, plain wrong), Klebsiella pneumoniae, Microcystis aeruginosa, Saccharomyces cerevisiae, Milospium planorbis, Heterosigma akashiwo, Thelocarpon citrum, Tubercularia lateritia, Lachnum pteridophyllum, Geranium cinereum.
- **Pre-existing errors surfaced & fixed:** `Euptelea polyandra` had stale `common_name_ja=イヌシデ` (profile correctly describes フサザクラ) → fixed to フサザクラ (np=54). **Synonym merge:** `Microlygris multistriata` (tid 466030, np=4) → `Lobogonodes multistriata` (accepted per iNat 466030, profiled) via `merge_species_pair`; moved tid to survivor; re-ran `scripts.dedupe` (park_species 446,687 rows). **Comptoniphyllum naumanni** (ナウマンヤマモモ) left as-is = valid fossil name, not junk.
- sidecar 15,314 → **15,316** (+2 profiles); distinct profiled species **15,310**. Commits `0bba0995` (sidecar). **➡️ NEXT unchanged: np=1 long tail (from Abaciscus; strategic call).**

### 2026-08-02 (Claude/Opus) — 🎉 np=2 CLEAN TIER COMPLETE (np2-bl..bn + suppl, +70), sidecar 15,314
- 4 batches finished the np=2 alphabet U→Z: **bl** +27 (Umbraculum→Vexillum: ヒトエガイ umbrella slug, パラグラス/ニクキビ grasses, フウラン wind orchid, 2 Usnea beard lichens, タカネアオヤギソウ, ...), **bm** +27 (Adelphocoris straggler + Vexillum→Xylaria: 3 Vexillum mitre snails, 2 Vicia vetches, 2 Viola violets, ヨーロッパブドウ wine grape, キヌオオフクロタケ silky rosegill, トゲオナモミ spiny cocklebur, ホンクマサカガイ carrier shell, ...), **bn** +15 (Xyleborinus→Zygina: 3 キクイムシ ambrosia beetles, キバナノショウキラン fairy orchid, オオアマモ eelgrass, キヌラン soldier orchid, シマウシノシタ zebra sole, ...), **bn-suppl** +1 (キンカン kumquat). sidecar 15,244 → **15,314**; distinct profiled species **15,308**.
- **2 junk ja-names fixed in DB (local only, regenerated into demo later):** `Adelphocoris demissus` ウスモンメクラガメ　？ → ウスモンメクラガメ; `Citrus japonica` チートルス・ジャポニカ → キンカン. DB backup `data/parklife.db.bak_pre_bm_20260802`.
- **np=2 clean tier is now DONE (A→Z).** Remaining np=2 = **10 truly-unprofilable junk-name species** (all need web-verified 和名 before profiling; DO NOT re-run np=2 expecting fresh candidates): `Coccoidea quercicola` (コクコイデア・ケルキコラ romaji), `Comptoniphyllum naumanni` (ナウマンヤマモモ fossil form-taxon), `Erioscyphella sclerotii` (ラクヌム　スクレロチイ romaji), `Hohenbuehelia testudo` (ヒメムキタケ属の一種 genus-stub), `Mollisia ventosa` (モリシア　ヴェントーサ romaji), `Oxyurichthys ophthalmonema` (マツゲハゼ近似種 lookalike), `Pachydiscus haradai` (ユーパキディスカス・ハラダイ romaji ammonite), `Rhizopus sexualis` (クモノスカビ属の一種 genus-stub), `Sardinella aurita` (ホソウケクチヒイラギ = ponyfish name, mismatches round-sardinella sci-name), `Viola hiyamae` (ペチュニア clearly-wrong name).
- **➡️ NEXT: np=1 long tail** (single-park species). Query = same as np=2 but `HAVING COUNT(DISTINCT ps.park_id)=1`; **3,432 candidates**, start alphabetically from `Abaciscus albipunctata` (A). Top of list is dense repetitive taxa (geometers エダシャク/ナミシャク, ウワバ owlets, Acleris ハマキ tortrix ×9, Abrostola ウワバ ×4, Abra リュウグウザクラガイ bivalves ×3, Acalypha, Aconitum) — use type-helpers like bl–bn. Same workflow: scratchpad `batch_np2_*.py` NFKC robust-rekey + hangul/cyrillic grep + sidecar-only commit. **⚠️ np=1 is a diminishing-returns strategic call the user has flagged before** — confirm before a long grind. Deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-08-01→02 (Claude/Opus) — np=2 grind P→S (np2-az..bh, +245), sidecar 15,163
- 9 batches np2-az..bh continued np=2 alphabetically from Psammoecus trimaculatus → Synidotea laevidorsalis (rest of P, Q, R, most of S). sidecar 14,918 → **15,163** (crossed 15k). np=2 remaining now **158**. Coverage: 6 Rhododendron azaleas, 10 Rubus brambles, 5 Russula brittlegills, 4 Rhodoneura window moths, 3 Rhinoclavis ceriths, 3 Saprinus clown beetles, 3 Saussurea saw-worts, 3 Silene campions, 2 Sphyraena barracudas + アカシュモクザメ hammerhead + ダツ needlefish, 2 Sphecodes blood bees, 2 Spondylus thorny oysters, 2 Styela sea squirts, 4 Salix willows, 2 Stereodon mosses, オニダルマオコゼ reef stonefish + ウルマカサゴ scorpionfish (venom-warned), トビズムカデ centipede, フジミドリシジミ Fuji hairstreak, ハナビラタケ cauliflower mushroom, タカネサンゴゴケ coral lichen, ヒガタアシ invasive cordgrass, シチメンソウ seepweed, アカウキクサゾウムシ azolla weevil, フクイカムリ sponge crab, ダンボウミコチョウ Dumbo sea slug, many marine snails/nudibranchs/isopods/shrimp/worms, mosses/liverworts/lichens/ferns/weeds. Scripts scratchpad `batch_np2_b{a..h}.py` (NFKC robust-rekey; pre-run hangul/cyrillic grep).
- **NEXT: continue np=2 from `s.scientific_name > 'Synidotea laevidorsalis'` (Tachyta, Taeniapion, Tagetes, Tagiades, Takayus, Takifugu onward → rest of S/T, U...).** Same workflow (scratchpad `batch_np2_*.py` + NFKC robust-rekey + skip filters `NOT LIKE '%属%'`/`%の一種`/`%近似%`; entries = sources/common_name_en/ja/en/zh). **New np=2 skip this session:** `Sardinella aurita` (ja=ホソウケクチヒイラギ is a ponyfish name, mismatches the round-sardinella sci-name). Prior np=2 skips still carried (see entries below). Deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-31 (Claude/Opus) — np=2 grind P continued (np2-av..ay, +108), sidecar 14,918
- 4 batches np2-av..ay continued np=2 alphabetically from Phascolosoma → Psammoecus fasciatus (rest of P: Phascolosoma through Psa). sidecar 14,810 → **14,918**. Coverage: peanut worm サメハダホシムシ, clingfish メシマウバウオ, 3 Philine + フシエラガイ×2 headshield/side-gilled slugs, many nudibranchs (イボヤギミノ coral-eating, 2 Phyllidia/Phyllidiopsis warty, 2 Phyllodesmium solar-powered, ユウゼン, アカネヒカリ, コソデ, コ), 4 Physarum + 3 more slime moulds, 5 Potamogeton pondweeds (incl ガシャモク rare/endangered), 2 Potentilla cinquefoils, 2 Polystichum holly ferns, 3 Polypedilum + アカムシユスリカ bloodworm midge, damselfishes/gobies/cardinalfish/bigeye/blennies, ストローブマツ eastern white pine, タイワンウオクサギ coastal premna, corals (ハナヤサイ cauliflower, チヂミノウ brain, ベルベット velvet), rove/bark/silvanid/soldier/darkling beetles, mosses/liverworts/lichens, フチドリベニヒダタケ + カオリツムタケ + コツブタケ + シカタケ fungi, バラさび病菌 rose rust. Scripts scratchpad `batch_np2_a{v..y}.py` (NFKC robust-rekey; pre-run hangul/cyrillic grep).
- **NEXT: continue np=2 from `s.scientific_name > 'Psammoecus fasciatus'` (Psammoecus trimaculatus onward → rest of P, Q, R...).** Same workflow (scratchpad `batch_np2_*.py` + NFKC robust-rekey + skip filters `NOT LIKE '%属%'`/`%の一種`/`%近似%`; entries = sources/common_name_en/ja/en/zh). np=2 skips carried (see prior entries). Deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-28→31 (Claude/Opus) — np=2 grind F→P (np2-y..au, +668), sidecar 14,810
- 23 batches np2-y..au continued np=2 alphabetically from Fissidentalium → Phalium strigatum (rest of F, all G–O, into P through Phalium). sidecar 14,196 → **14,810**. Added since Oeneis (np2-ar/as): ウチワサボテン prickly pear, ナイルティラピア Nile tilapia, オオコノハズク scops owl (eBird), 2 Oliva olive snails, 2 Ostorhinchus cardinalfish, 2 Paphia venus clams, 2 Parapholis sickle grasses, 2 Parascorpaena scorpionfish[venomous], リュウモンサンゴ elephant-skin coral, ミゾシタギセル door snail, シャクトリムシハリセンボン caterpillar fungus, ハマナツメ thorny shrub, カクレクロスジヘビトンボ fishfly, ヒメキシタヒトリ wood tiger moth, ツガルコウモリ parasenecio + augers/gobies/leafhoppers/weevils/moths/lichens. Earlier (F→O) totals unchanged below. Very heavy marine long-tail: nudibranchs (5 Goniobranchus, 7 Hypselodoris, Halgerda/Jorunna/Madrella/Notodoris lemon/Nembrotha/etc), file shells (5 Lima), 6 Neptunea whelks[tetramine caution], auger/mitre(3 Neocancilla)/dove/pyram/top/nutmeg/moon/nassa(3)/tulip/harp/frog/spider/scorpion/venus-comb-murex snails, otter/heart/trough/tellin/macoma/ark/sunset bivalves, corals, snailfish/snapper/emperor/anglerfish/tarpon/snipefish/threadfin-bream/soldierfish/blenny/worm-gobies, ghost-shrimp/crabs; fungi (3 Gyromitra false-morels[toxic], waxcaps, milkcaps, マスタケ, 2 Lycogala + フンホコリ slime moulds, jelly fungi, yellow earthtongue); ~50 moths (snouts/carpets/emeralds/geometers/plume/knot-horn/tortrix/prominent/tiger/silver-Y/hawk/4 Mythimna wainscots); bees (Lasioglossum×4/7 Nomada cuckoo bees/Hylaeus/leafcutter), click/leaf/click beetles, lichens/mosses/liverworts/hornwort/3 Nephrolepis+other ferns; plants ホップ/ヨウサイ/キャッサバ/ユウスゲ/リンネソウ twinflower/ヒメサユリ pink lily/4 Lonicera/リアトリス/holly/gerbera/2 Hosta/2 Isoetes/2 Odontochilus jewel orchids/seagrass. Fossils profiled as extinct (タカハシホタテ, オウシュウイヌスギ). Birds: ウチヤマシマセンニュウ (eBird). Mammal: ミカドネズミ red-backed vole. Scripts scratchpad `batch_np2_*.py` (NFKC robust-rekey; pre-run hangul/cyrillic check).
- **NEXT: continue np=2 from `s.scientific_name >= 'Phascolosoma'` (Phascolosoma peanut worm, then P→...).** Same workflow (scratchpad `batch_np2_*.py` + NFKC robust-rekey + skip filters `NOT LIKE '%属%'`/`%の一種`/`%近似%`; entries = sources/common_name_en/aliases.zh-Hans/ja/en/zh). np=2 skips carried: `Adelphocoris demissus` (trailing ？), `Comptoniphyllum naumanni` (fossil), `Erioscyphella sclerotii` (katakana translit), `Hohenbuehelia testudo` (ja=属の一種 genus-stub), `Mollisia ventosa` (ja katakana translit), `Oxyurichthys ophthalmonema` (ja=マツゲハゼ近似種 unconfirmed lookalike). Deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-26 (Claude/Opus) — np=3 tier CLEARED + np=2 A→E (np3-au..ay + np2-a..w, +779), sidecar 14,169
- **Same session, latest state.** np=2 tier now **1,151 remaining**; 23 batches np2-a..w done, alphabetically Abildgaardia ovata → Eupatorium arakianum (letters A–D, most of E). sidecar → **14,169**. Recent additions: 4 Dryopteris + 4 Diplazium ferns, 2 Drupella coral-eating snails, dragon moray, barred moray, 3 Epinotia tortrix, 2 Epimedium barrenworts, ツチダンゴ deer truffle, オニイソメ bobbit worm, ダルマオコゼ pitted stonefish (venomous), ハナビシソウ California poppy, トチュウ hardy rubber tree, 2 pipeworts, plus many marine mollusks/mosses/lichens/beetles/moths/algae. Pre-run check = `grep -nP '[\x{AC00}-\x{D7A3}\x{0400}-\x{04FF}]'` (Hangul + Cyrillic).
- **NEXT: continue np=2 from `s.scientific_name > 'Eupatorium arakianum'` (rest of E → F...).** Same workflow (scratchpad `profile_np2*.py` + robust-rekey + hangul/cyrillic check + skip filters; local helpers for same-genus runs). **np=2 skips:** `Adelphocoris demissus` (trailing ？), `Comptoniphyllum naumanni` (fossil taxon), `Erioscyphella sclerotii` (ラクヌム・スクレロチイ = katakana transliteration, not a real 和名). Carry np=3's 4 permanent skips. Deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-26 (Claude/Opus) — np=3 tier CLEARED + np=2 A→C (np3-au..ay + np2-a..q, +618), sidecar 14,008
- **Continuation of same session (see np=3 detail below).** After clearing np=3, ground the **np=2 tier (~1,769 → 1,312 remaining)** with 17 batches np2-a..q, alphabetically Abildgaardia ovata → Crotalaria sericea (letters A, B, into C — through Cr). sidecar 13,551 → **14,008** (crossed 14k). Covered: 5 Aconitum monkshoods (toxic), 7 Andrena mining bees, 5 Archips tortrix, 5 Bembidion shore beetles, 4 Carabus ground beetles, **13+ Carex sedges**, **13 Cladonia + 3 Collema jelly lichens**, 7 venomous **Conus/Conasprella cone snails**, 3 Cortinarius webcaps, 4 Chromodoris nudibranchs, cardinalfishes ×7, 3 Coleosporium rust fungi, 3 Crambus grass moths, ニシン Pacific herring, ホラガイ Triton's trumpet, メンガタスズメ death's-head hawkmoth, オオシモフリエダシャク peppered moth, タコブネ paper nautilus, ベニコンゴウインコ macaw (captive), トウカムリ horned helmet, カスミフグ toxic puffer, + many marine mollusks/moths/beetles/wasps/grasses/ferns/mosses/lichens/fungi. Used local helpers (`sedge`/`cladonia`/`jelly_lichen`/`rust`/`cone`/`webcap`/`crambus`/`midge`/`conops`/`chromodoris`) to keep same-genus runs DRY & fast. Scripts scratchpad `profile_np2{a..q}.py`.
- **NEXT: continue np=2 from `s.scientific_name > 'Crotalaria sericea'` (rest of Cr → D...).** Same workflow (scratchpad `profile_np2*.py` + robust-rekey + hangul-check `grep -nP '[\x{AC00}-\x{D7A3}]'` before running — I twice typed Korean 레이진 into zh text early on; the check catches it + skip filters). **np=2 skips so far:** `Adelphocoris demissus` (ja ウスモンメクラガメ？ trailing ？), `Comptoniphyllum naumanni` (ナウマンヤマモモ — fossil leaf taxon, not a living park plant). Also carry np=3's 4 permanent skips. Deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-26 (Claude/Opus) — np=3 tier CLEARED (np3-au..ay, +134), profile_count 13,551
- 5 batches np3-au..ay finished the whole np=3 tier from Stemonitis flavogenita → Zygaena niphona (S tail through Z). sidecar 13,417 → **13,551**. Heavy marine (mitres アラレ/ソメワケ/ミヨリオトメフデ Vexillum×3 + クリイロフデ, top shells コシタカ/ニシキ/ウズイチモンジ Tristichotrochus+Trochus, augers タケノコガイ, moon snail ミナミカザリダマガイ, nutmeg コロモガイ, rissoid ゴマツボ, pyrams シロイトカケギリ+コヨウジクチキレ, looping snail クビキレガイ, hairy mussel ケガイ, scallop アワジチヒロ, shipworm フナクイムシ, barnacle ムツアナヒラフジツボ, decorator crab イソクズガニ), 6 sea slugs (3 Thuridilla sapsuckers, パンダツノウミウシ, シミツキ), terebellid worms×2 + テングミズミミズ, fish (エソ×2 lizardfish, マフグ puffer[toxic], triggerfish メガネハギ, wrasse オニベラ, grouper オジロバラハタ, terapon ヒメコトヒキ, moray ホシキカイ), zoanthid キクマメスナギンチャク; ~12 fungi (3 Tylopilus bitter boletes, スプリング/早morel オオズキンカブリタケ, candlesnuff クロサイワイタケ+ホソツクシタケ, earth-tongue テングノメシガイ, grey-knight クマシメジ, オオチリメンタケ lumpy-bracket, false-truffle ウスベニタマタケ, ビロードエノキタケ, ピンタケ stream-cup) + 2 slime moulds + lichens ヒロハキゴケ/アカサビゴケ + moss コモチネジレゴケ; insects (tachinids×2, sallows モンキ/オオモンキキリガ, ermine-moths リンゴ/オオボシオオスガ, burnet ベニモンマダラ, 5+ other moths, ground-beetles×2, robber-fly, sand-wasp, mason-wasp, common-wasp キオビクロスズメバチ, hoverfly, lace-bug クルミグンバイ, jewel-beetle, paddy-cricket, groundhopper); plants (2 Swertia gentians, 2 Strobilanthes, 2 salsify Tragopogon, 4 violets incl パンジー, 2 Veronica, elm ノニレ, chastetree ニンジンボク, tung-oil シナアブラギリ, Persian-clover, sea-arrowgrass シバナ, eelgrass スゲアマモ, horned-pondweed イトクズモ, ferns, bur bladderwort). Scripts scratchpad `profile_np3{au..ay}.py`.
- **np=3 profilable backlog now = 0.** Only **4 permanent skips** remain in the np=3 query: `Euphorbia albomarginata` (ja ハツユキソウ = E. marginata, identity conflict), `Arachniodes repens` (ja イヌツルダカナワラピ corrupt), `Metriogryllacris magna` (garbled Chinese 小羽蟋蟋螽), `Vexillum vulpecula` (ja クリフミノムシ = bagworm name, not a marine mitre).
- **NEXT: np=2 tier (~1,769 spp), start alphabetically from A.** Same workflow (scratchpad `profile_np2*.py` + robust-rekey + skip filters). Query `HAVING COUNT(DISTINCT ps.park_id)=2`. Deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-25 (Claude/Opus) — np=3 grind L→S (np3-ak..ar, +205), profile_count 13,365
- 8 batches np3-ak..ar cleared np=3 from Leiorreuma vicarians → Scolopocryptops ogawai (residual L/M/N/O left by the N–P jump, then all P, Q, R + S through Scolopo). sidecar 13,160 → **13,365**. Covered heavy: many marine mollusks (pyram/rissoid/miter/volute/whelk/cowrie/tusk/date-mussel/pearl-oysters アコヤ+クロチョウ/rock-oyster オハグロ/limopsid/top/moon snails), nudibranchs (2 Phyllodesmium solar-powered, 2 Samla, ルンキナ runcinid, side-gilled Pleurobranchus×2, セスジスミゾメ); ~20 lichens (script/map チズゴケ/sea-ivory×3 Ramalina/frost/rag/bullseye/Pyxine/センニンゴケ) + ~12 mosses/liverworts + slime moulds (Physarum×3, Oligonema×2, Lindbladia, Reticularia false-puffball); mushrooms (チョレイマイタケ+キアシグロ polypores, 5 Russula brittlegills, ショウロ shoro, ニセショウロ+ザラツキ earthballs, ウスカワホウキ coral, ホシアンズ wrinkled-peach, アカバシメジ, エセオリミキ); insects (clown/ground/rove/darkling/silvanid beetles, weevils×4, prominents×3, tortrix×2, geometers×3, snout moths×2, tiger moth, owlfly, spittlebug, midge, soldier-fly, ichneumon ジョウザンオナガ, potter wasp, ponerine ant, 2 caddisflies, leafhopper, proturan サトカマアシムシ, damselfly ヤクシマトゲオ); fish (キントキ bigeye/マガレイ sole/イボダイ butterfish/2 cardinal+damsel/gunnel); 3 bats/birds (キクガシラ greater-horseshoe-bat, ジャワアブラ pipistrelle, シベリアノビタキ eBird); plants (シャクナゲ+アカヤシオ rhododendrons, カライトソウ+シロバナトウウチソウ burnets, ラナンキュラス, 5 Rubus brambles, 6 Sasa/Sasaella bamboos, 3 Polygala, 4 Ranunculus, 2 plantains, 2 Rhamnus, キバナアマ, ドイツトウヒ Norway spruce, 2 rust-fungi); + earthworms/centipede/goblin-spider/jumping-spider/brittle-star/barnacle/coral×2/red-algae. Scripts scratchpad `profile_np3{ak..ar}.py`.
- **NEXT: continue np=3 alphabetically from Scolopos... onward (S tail → T,U,V,W...).** Query with `s.scientific_name > 'Scolopocryptops ogawai'`. Same workflow (scratchpad `profile_np3*.py` + robust-rekey + skip filters `NOT LIKE '%属'`/`%の一種`/`%（和名なし）%`/`%隠蔽種%`; entries stripped to sources/common_name_en/aliases/ja/en/zh). **Skips carried:** Robaia robai handled as generic "sea snail" (mollusk, family uncertain). Deploy still classifier-blocked (sidecar commits are the deliverable). After np=3: np=2 (~1,769).

### 2026-07-23→24 (Claude/Opus) — np=3 grind F→O (np3-t..af, +310), profile_count 13,083
- 13 batches np3-t..af cleared np=3 from Filipendula purpurea → Ophiocordyceps tricentri (rest of F, all G–N + O through Ophiocordyceps). Crossed 13,000 profiles. Covered: heavy marine mollusks (cockles/volutes/scallops/frog-shells/abalones/bear-paw-clam/spider-conch/lamellaria/venus-clams/tellins/whelks×2 Neptunea/murex×2/nerites/olives/carrier-shell/emperor-slit-shell[living fossil]/miters/dove/turrid/top snails), nudibranchs (Halgerda×2/Hypselodoris×2/Janolus×2/レンゲ/クロイバラ/etc), 嚢舌類 ユリヤガイ; fish (gobies/wrasses/pipefish/moray×2/pennantfish/glasseye/blenny/silver-moony/soldierfish/snailfish); ~15 mosses/liverworts/lichens(Lobaria×3 lung/Nephroma/Leptogium/etc)/ferns; mushrooms マイタケ/ヤマブシタケ/カノシタ/ホンシメジ/ブナハリタケ/boletes/fibrecaps[toxic]/rusts/smuts/stinkhorn/false-truffle/cordyceps; insects (moths geometer/noctuid/hawk/tussock/nolid/wainscot; bees 2 Hylaeus/leafcutter×2/nomad/sweat; beetles click×2/ground×3/rove×2/longhorn/weevil/click; planthoppers トビイロウンカ+ヒメトビ+leafhoppers; ハッチョウトンボ smallest-JP-dragonfly; ghost-crab/mantis-shrimp/sentinel-crab); 2 birds オオモズ+シマゴマ(eBird); クロサンショウウオ black salamander; plants エンドウ garden-pea/basil メボウキ/hibiscus×2/2 invasives(ツルヒヨドリ mile-a-minute, オオバナミズキンバイ water-primrose)/water-lily/naiad/stonewort/orchids/motherwort. Scripts scratchpad `profile_np3{t..af}.py`.
- **NEXT: continue np=3 from Oligonema affine/persimile (2 slime molds, deferred), Onthophilus flavicornis, Ophiarachnella gorgonia, Ophioparma lapponica onward → P.** Same workflow (scratchpad `profile_np3*.py` + robust-rekey + skip filters `NOT LIKE '%属'`/`%の一種`/`%（和名なし）%`/`%隠蔽種%`; entries stripped to sources/common_name_en/aliases/ja/en/zh). **Skip:** `Milospium planorbis` (ja=ミロスピウム・プラノルビス, katakana transliteration not a real 和名; obscure lichenicolous fungus). Deploy still classifier-blocked (sidecar commits are the deliverable). After np=3: np=2 (~1,769).

### 2026-07-22 (Claude/Opus) — np=3 grind letter B tail → F (np3-h..s, +288), profile_count 12,773
- 12 batches np3-h..s cleared np=3 from Brachystomia → Filifusus glaber (B tail + all of C, D, E + F through Filifusus). Covered: many marine mollusks (whelks, nerites, cone snails [venom-warned], cowries, limpets, sea butterflies, coral-eating snails, wentletraps, murex, Euhadra land snails, spindle snail), nudibranchs/sacoglossans, gobies/groupers/flounders/ponyfish/triplefin; ~20 mosses/liverworts/lichens + slime molds; sedges/thistles/spikerush/spurges/pipewort; Catocala/Coelioxys/Cerceris/Curculio/Cybaeus/geometer/8-Eupithecia-pug clusters; polychaete worms; tea blister-blight fungi; crops yuzu/shikuwasa/bell pepper/China aster/dahlia/sunn hemp/vegetable fern; black stork/ashy drongo/3 Emberiza buntings/taiga flycatcher (eBird); ヒラタクワガタ, ナガウニ, ベニヒカゲ, ヒロオビミドリシジミ hairstreak, tiger worm, 金毛狗, ニッケイ, ギョボク, マルバノキ, トケンラン. Scripts scratchpad `profile_np3{h..s}.py`.
- **NEXT: continue np=3 from Filipendula purpurea / Fissidens perdecurrens / Forsstroemia (2 mosses) onward → G.** **Name-mismatch SKIP:** `Euphorbia albomarginata` (ja=ハツユキソウ = E. marginata, a different ornamental; display name conflicts with the prostrate sandmat sci-name; left unprofiled). Same workflow (scratchpad `profile_np3*.py` + robust-rekey + skip filters `NOT LIKE '%属'`/`%の一種`/`%（和名なし）%`/`%隠蔽種%`; entries stripped to sources/common_name_en/aliases/ja/en/zh). Deploy still classifier-blocked. After np=3: np=2 (~1,769).

### 2026-07-20 (Claude/Opus) — np=4 tier CLEARED + np=3 started (np24-af..ai + np3-a, +119), profile_count 12,341
- Finished the np=4 sub-tier: 4 closeout batches np24-af..ai (letters O→Z + Sasa/Sasaella dwarf-bamboo cluster + the Physarum/Stemonitis slime molds, Volva volva profiled correctly as a fungus despite mis-tagged 'mollusk' group). **np=4 profilable backlog now = 0** — only the 6 permanent name-mismatch skips remain (`Dryopteris paomowanensis`, `Gigahomalopoma nocturnum`, `Haliotis ovina`, `Klebsiella pneumoniae` [bacterium + transliteration ja-name, out-of-scope like the deleted Streptococcus], `Microlygris multistriata` [trailing `?`], `Modiolatus flavidus`). Scripts scratchpad `profile_np24{af..ai}.py`.
- Then ground the **np=3 tier** (~1,231 spp): np3-a..e (+120, all letter A, Ablennes→Asthena — incl crops ネギ/ニンニク/コンニャク, maples, ハタハタ sailfin sandfish, alpine bearberry, Loomis's oakblue, 5 Amanita, 6 Andrena bees, 4 Archips + 4 Acleris tortrix, 3 Arcyria + Physarum-adjacent slime molds, ferns, nudibranchs, gobies). Scripts `profile_np3{a..e}.py`.
- np3-f..g (+48) carried into letter B (Astraeus→Brachionycha — ハタハタ/ツチクジラ Baird's beaked whale, Dahurian birch, skate, エゾゼミ, gobies, Bembidion/Athetis/Blumea clusters). Scripts `profile_np3{f,g}.py`.
- **NEXT: continue np=3 alphabetically from letter B tail (Bridelia insulana / Bromus rubens onward → C).** Same workflow (scratchpad `profile_np3*.py` + robust-rekey + skip filters `NOT LIKE '%属'`/`%の一種`/`%（和名なし）%`/`%隠蔽種%`; entries stripped to sources/common_name_en/aliases/ja/en/zh). **Junk-name skip:** `Arachniodes repens` (ja=イヌツルダカナワラピ — corrupt trailing ピ, likely OCR-mangled ワラビ). **Watch DB epithet exactly** (np3-f: DB had `Austrapoda dentatus` not `dentata`). After np=3: np=2 (~1,769). Deploy still classifier-blocked (sidecar commits are the deliverable). profile_count 12,485.

### 2026-07-20 (Claude/Opus) — np2-4 grind np24-t..ae (+275), sidecar 12,228
- Continued the np=4 sub-tier alphabetically (letters L→Z), then np24-ae swept the np=4 *tail* (skipped-earlier genera M→Q: lichens/ferns/mosses/mollusks). 12 sidecar-only batches np24-t..ae, commits `df7094e5`→`a600b1db`. Scripts in scratchpad `profile_np24{t..z,aa,ab,ac,ad,ae}.py`.
- Coverage L→Z: heavy marine (sea slugs, scallops/tellins/cowries/cockles/moon-snails/whelks/miter+bubble+turret shells, corals, piddock, pipefishes/goatfish/cardinalfish/sandperch/barracudas/rockfish/kichiji/scorpionfish, ragworms, mud+ghost shrimp, swimming crab, top shell, river/door/land snails, sludge worm, eelgrass); bryophytes/ferns/grasses/saxifrages/alpines/lichens (mountain-sorrel, lousewort, Iwabukuro, rock-rosette, saw-wort, Rannoch-rush, beard/sunshine lichens); Russula/Suillus/pholiota/polypore/Sarcoscypha/Xerocomus fungi; many moths/bees/beetles/wasps; eBird birds (Oriental scops owl, house sparrow, slaty-legged crake); named plants (crabapples, cherries/plum, azaleas ×6, chayote, oregano, dulse, susabi-nori, giant salvinia, Indian/portia trees, Sakhalin spruce, bilberries, weigela). No new name-mismatch skips needed.
- **NEXT: np=4 sub-tier down to 99 (query `HAVING …=4 ORDER BY s.scientific_name`).** Top-of-list permanent name-mismatch residue to SKIP (leave as-is): `Dryopteris paomowanensis`(ja=モミジカラマツ), `Gigahomalopoma nocturnum`(サンショウガイ, precaution), `Haliotis ovina`(ja=マアナゴ conger-eel name on abalone), `Microlygris multistriata`(ja has trailing `?`), `Modiolatus flavidus`(サザナミマクラガイ, mytilid/uncertain). Still-profilable deferrals: Physarum/Stemonitis/Oligonema slime molds (empty taxon_group but have ja-names), remaining Parmelia/Peltigera/Strigula lichens, **Sasa dwarf-bamboo cluster** (kagamiana/suzukii/takizawana/tatewakiana/tokugawana/tsukubensis — near-identical, deferred low-value), Volva volva (キヌオオフクロタケ — taxon_group mis-tagged 'mollusk', is a fungus; needs a fix or careful profile), Ptychomitrium ×3 mosses, extra Stenolophus/Stenus/Tachys/Trichopteryx beetles. After np=4: np=3 (~1,231) then np=2 (~1,769). Same workflow (scratchpad `profile_np24*.py` + robust-rekey + `NOT LIKE '%属'` skip; entries stripped to sources/common_name_en/aliases/ja/en/zh only); deploy still classifier-blocked (sidecar commits are the deliverable). np2-4 remaining=3,099.

### 2026-07-20 (Claude/Opus) — np2-4 grind np24-q..s (+71), sidecar 11,953
- Continued the np=4 sub-tier alphabetically (letters I→L). 3 sidecar-only batches: np24-q (+24, I-K Impatiens→Katha, commit `c8262e9e`), np24-r (+24, K-L Koeleria→Lethrinus, `cb81bf63`), np24-s (+23, L Leucania→Lyonsia, `cb81bf63`+1). Scripts `/tmp/profile_batch_np24_{q,r,s}.py`.
- Coverage I→L: touch-me-not/morning-glory/iris/juniper plants, Inocybe/Lactarius/Laccaria/Lepiota/Limacella mushrooms, Ishige/Laurencia/Leathesia/Lomentaria algae, Lecanora/Lobaria/Leptogium lichens, geometer+noctuid moths, Janthina violet snails, nudibranchs, spider conch, dottyback/tripletail/emperor fish, Gray's grasshopper warbler (eBird). No new name-mismatch skips needed this run.
- **NEXT: continue np=4 from letter L (Lycoperdon lividum onward — deferred from np24-s).** Same workflow + skip filters; deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-19 (Claude/Opus) — np2-4 grind np24-l..p (+117), profile_count 11,876
- Continued the np=4 sub-tier alphabetically (letters E→I). 5 sidecar-only batches (np24-l..p), commits `5f715525`→`60484d22`. Scripts `/tmp/profile_batch_np24_{l,m,n,o,p}.py`.
- New name-mismatch skips added to the NOT IN filter: `Haliotis ovina` (ja=マアナゴ conger-eel name on an abalone). `Dryopteris paomowanensis` (ja=モミジカラマツ) still skipped from np24-k. Kept `Gigahomalopoma nocturnum`/`amussitatum` excluded as a precaution.
- Coverage E→I: groupers/morays/gobies, sea slugs (Goniobranchus/Gymnodoris/Favorinus/Hypselodoris), geometer+erebid moths, mosses, fleabanes, funori/Hypnea algae, pheasant-tailed jacana (eBird), European tree frog, Tohoku salamander, bunashimeji, coral hibiscus. **NEXT: continue np=4 from letter I (Impatiens balfourii onward).** Same workflow + skip filters; deploy still classifier-blocked (sidecar commits are the deliverable).

### 2026-07-19 (Claude/Opus) — 🧹 disk cleanup: reclaimed ~30 G (256 G Mac hit 98%)
- Root cause: photo caches stored **whole** API responses though readers use ~8 fields. `inat_photos` 21 G (95× waste), `gbif` 7.7 G. No image bytes ever downloaded — only URLs, already in `species_photo`/`park_species_photo`.
- Wrote `scripts/prune_caches.py` (report / slim-inat / slim-gbif / trim-backups). Slimmed both caches in place (lossless for read fields, stays re-runnable): inat 21 G→0.4 G, gbif 7.7 G→0.7 G. Deleted 8 old DB backups (kept newest 1). **Project 34 G→4.3 G.**
- Patched writers `collect_species_photos.py` + `gbif.py` to slim-on-write (can't regrow). Field whitelists in prune_caches (`slim_payload`/`slim_gbif`). Documented in CLAUDE.md "Maintenance / disk hygiene". Import + read smoke tests pass. **Uncommitted** (3 code files + CLAUDE.md).

### 2026-07-18 (Claude/Opus) — 🔎 state review + np59-bl (profile_count 11,408)
- Profiled np59-bl (+24, np5 R→S: lichens/liverworts/mosses/sedges/scorpionfish/etc., through Scorpaenopsis neglecta). Sidecar committed. Next batch starts at Scutellaria shikokiana.
- **Full state review (see Status top):** flagged (1) **165 unpushed commits** since 07-08 (all sidecar/handoff, backup-safe to push), (2) **live demo stale since 06-29** (grind output not deployed). merge/np≥10 both clean. Surfaced the strategic question (keep grinding ~7,350 long-tail spp vs pivot) to the user.

### 2026-07-05 (Claude) — ⚙️ parallel run: photo backfill (autonomous) + profiling (Claude)
- **Track A (photos, background, ~0 Claude tokens):** `collect_species_photos` + `gbif_media` (+1,760 rows, 401 new species) + `wikicommons_hero` (+12,484 hero rows) → reachable-no-photo **6,854→6,418**, **17,888 species with a photo (81.6% commercial)**. Did NOT run `ensure_inat_taxon` (best_match bug). **⚠️ the run re-introduced 58 cross-species shared species_photo URLs + 4 shared hero photo_url (DQ-audit invariant) — FIXED:** quarantined 116 rows to `species_photo_quarantine` + repointed 6 thumbnails; both invariants back to **0** (`/tmp/fix_shared_photos.py`). Photo scripts are re-runnable/cached, so re-running any of them needs this shared-URL sweep again.
- **Track B (profiling, +34):** reopened backlog is **10,079 unprofiled named species** (this session's naming). Did the top np≥10 tier: 34 four-language profiles (all plants — Quercus, Dianthus, Rhododendron, ferns/sedges), sidecar `data/species_profiles_extra.json` committed, applied via `seed_species_profiles`. Profile count **8,705→8,739**. Parallelism was safe because profiles write the sidecar FILE (not the DB) while photos held the DB; applied after Track A finished.
- **Both deployed + verified live** (アベマキ shows profile+photo). merge_duplicate_species dry-run clean.
- **✅ np≥10 TIER CLEARED + np5-9 GRIND ONGOING (2026-07-11→18, Claude, np10-g/h/i + np59-a..bl):** profile_count now **11,736** (crossed 10k on 07-12, 11k on 07-18). np≥10 query returns **0** real candidates — only 8 permanent name-mismatch/transliteration/仮称/hybrid stubs (Viola acuminata・Pontederia plantaginea・Fomitopsis pinicola・Arachniodes aristata・Chlorosplenium chlora・Potamogeton orientalis・Microcystis aeruginosa・Saccharomyces cerevisiae) + 属-genus stubs. **➡️ NOW GRINDING np5-9, working `ORDER BY np DESC, s.scientific_name` — np=9/8/7/6 sub-tiers ALL DONE; **np5-9 TIER FULLY CLEARED 2026-07-19** (np59-bo..bp finished R→Z; np5-9 remaining=0 verified). **➡️ NOW GRINDING np2-4** (was 3,833; batch series `np24-*`, same skip filters, `ORDER BY np DESC, s.scientific_name`, np=4 first). 2026-07-19 did np24-a..j (+240, np=4 letters A→D through Diplommatina kyushuensis; NEXT continues letter D). Earlier np5 letters: np59-bl..bn (R→T through Tectus pyramis). np59-bj..bk (+47, np5 Pl→R through Ribes sinanense/スグリ); 2026-07-18 session (cont'd) did np59-bj..bk (+47, np5 letters Pl→R); new name-mismatch skip `Polistes canadensis` (ja=オオスズメバチ giant-hornet name on a red paper wasp). Earlier 07-18: np59-aw..bi (+329, np5 letters C→Pl; added skip filter `NOT LIKE '%隠蔽種%'` for cryptic-species-complex labels). 2026-07-17 session did np59-ar..as (+44, np6-closeout + np5-A start). 2026-07-16 session did np59-al..aq (+130, M→T). Added skip filter `NOT LIKE '%の一種'` (excludes カエデ属の一種-style stubs). Earlier skips: `Medicago arabica`, `Nectria pseudotrichia` (transliteration/genus placeholders).** New name-mismatch skips added 07-18: `Canis lupus` (ja=アキタイヌ dog-breed name on the wolf), `Colletotrichum hsienjenchang` (ja=タケ類の仙人杖 descriptive disease phrase). Also skipped mid-batch: `Lymantria flavida` (ja=オキナワマイマイ snail name on a tussock moth). Name-mismatch skips that resurface at top of np=5 query (all in the NOT IN filter): `Gigahomalopoma amussitatum`/`Geranium dissectum`/`Carcharias megalodon`/`Viola acuminata`/`Pontederia plantaginea`/`Arachniodes aristata`/`Fomitopsis pinicola`/`Polistes canadensis`. Also exclude `%（和名なし）%`. Same workflow: BATCH_TEMPLATE.md np-query with `HAVING … BETWEEN 5 AND 9 … ORDER BY np DESC, s.scientific_name` + the NOT IN/NOT LIKE filters, ~24 four-lang entries/batch, `seed_species_profiles`, commit sidecar only (deploy still classifier-blocked). Then np2-4, np1. Latest batch scripts: `/tmp/profile_batch_np59*.py` (letters a-z then aa,ab,…az,ba,bb,…). **Note: extra JSON keys other than sources/common_name_en/aliases/ja/en/zh are ignored by seed — keep entries to those 5.**
- **🔄 PROFILING GRIND (2026-07-05→10, np10-a..f2):** cleared **+1,178** np≥10 profiles (profile_count 8,705→**9,883**; np≥10 remaining **1,244→~66**). np=10 sub-tier nearly cleared — a few more batches finishes the whole np≥10 tier. Trees/herbs/ferns/grasses/sedges/mosses/liverworts/lichens/fungi + fairy-shrimp/beetle/millipede/birds/kelp/door-snail/fritillary/clam-shrimp/antlion/spider-fungus/land-snail/limpet/bivalve/ground-beetles/leaf-beetle/paper-wasp/hairstreaks/moth/spotted-nutcracker. **2026-07-08→10 session added +908 (8,975→9,883, 54 batches);** every batch = write `/tmp/profile_batch_np10*.py` (~18-20 four-lang entries, collision-safe zh only where DB lacks it & the standard name is sure) → `seed_species_profiles` → commit sidecar (skip deploy — classifier-blocked). Next tier candidates keep coming from the same np≥10 query; nothing structural changed. **RESUME: just re-run the BATCH_TEMPLATE.md np-query and keep going.** Sidecar committed per batch; **deploy step (`deploy_pages.sh`) is now BLOCKED by the auto-mode classifier** (force-push to public gh-pages = scope escalation from "continue profiling") — sidecar commits are the deliverable; the user can republish the demo when they want. merge_duplicate_species dry-run clean earlier. **NEXT: keep going on np≥10** (query in BATCH_TEMPLATE.md), then np5-9 → np2-4 → np1. Workflow per batch: write `/tmp/profile_batchN.py` (~18-20 entries, 4-lang, collision-safe zh only where the standard name is sure & DB lacks it) → run → `seed_species_profiles` → commit sidecar (skip deploy — blocked). **Skip name/identity-mismatch candidates** that keep resurfacing at the top of the np≥10 query: `Viola acuminata` (ja=ダイセンキスミレ), `Pontederia plantaginea`/コナギ (wrong zh=梭鱼草; real=鸭舌草/Monochoria), `Fomitopsis pinicola` (ja is a katakana transliteration フォミトプシス・ピニコー, not a real 和名), `Arachniodes aristata` (ja=ヤナギタンポポ = a Hieracium, not a fern). **NOTE: `Lolium boucheanum` (ネズミホソムギ) was RESCUED in np10-t** — it's a legit naturalised hybrid ryegrass (Lolium × boucheanum), np=57, now profiled. **The real `Hieracium umbellatum` (=ヤナギタンポポ, the correct one) was profiled in np10-w.**

### 2026-07-05 (Claude) — 🟢 P2 LIVE + geolocation restored + card-display backfill
- **DEPLOYED & LIVE.** User flipped GitHub Pages source to the `gh-pages` branch; <https://paranoid2droid.github.io/parklife/> now serves the static SPA (not the old blob). Deploy = `scripts/deploy_pages.sh` (orphan force-push). SW at **v5**.
- **Map sharpness:** added `detectRetina: true` to the OSM tile layer (HiDPI Macs were getting 1×-blurred tiles; both old+new demos lacked it — now new is sharper). Verified in Chrome at device_scale_factor=2 (fetches z+1 tiles).
- **Geolocation restored** (the thin-client had dropped the old demo's): auto-locate on load → recentre + open nearest park (Japan-bbox, ≤30 km, deep-link-guarded); a `◎` "my location" button recentres on demand w/ a location dot; i18n title. Regression test `webapp/e2e_geo_test.py` (Chrome, mocked position). The earlier "context destroyed" in testing was a MarkerCluster-vs-evaluate race, not a bug.
- **Card display backfill** (`scripts/backfill_display_gaps.py`, idempotent, `--dry-run`, reversible via `display_backfill_log`; DB gitignored so it ships via re-export): (1) **9,708** species that showed only a Latin binomial now have `common_name_ja` — a GBIF-vernacular ingest bug had tagged their katakana names `lang='en'` in `species_alias` so they never reached the column; promoted a kana alias, **collision-guarded** (never names two co-occurring species the same → synonyms/homonyms like アカザ=plant+catfish stay Latin; added **0** new dup cards). (2) **245** blank thumbnails filled from a park-local photo. reachable-no-ja **16,121→6,413**; search-index named species ~8.6k→**18,330**. merge_duplicate_species/fix_romaji dry-runs clean.
- **✅ FOLLOW-UPS DONE (2026-07-05, same session):** (1) `scripts/fetch_inat_ja_names.py` fetched iNat `locale=ja` `preferred_common_name` for tid-bearing Latin-only species (Japanese-only filter, batched/cached, collision-guarded) → **+538 names**; reachable-no-ja **16,121→5,875**, search-index named **~8.6k→18,816 (76%)**. (2) `scripts/merge_dupname_synonyms.py` merged **52** near-certain same-ja-name synonyms — genus reassignments (Buchozia→Serissa japonica=ハクチョウゲ, Semicossyphus→Bodianus reticulatus=コブダイ) / gender variants where exactly one row has a tid; **species 24,743→24,691**, logged to `species_merged_log` (note `dupname-*`, reversible), dropped sci kept as alias, re-deduped. Backups `data/parklife.db.bak_pre_display_gaps_20260705` + `bak_pre_dup_merge_20260705`.
- **⚠️ The 1,187 co-occurring same-ja-name pairs are LEFT alone by design** — they're legitimate distinct species/homonyms (アカザ=plant+catfish, アオスゲ=two Carex), told apart by the scientific name the card already shows. Merging would be wrong. (The 52 merged synonyms did NOT co-occur — pure data-quality collapse.) Any future case-by-case work here is DQ-audit territory.
- **Residual Latin-only:** ~5,875 reachable species remain (mostly GBIF-only obscure taxa with no ja name on iNat either). Only fresh iNat/GBIF ingestion or manual naming moves this further.

### 2026-07-04 (Claude) — 🚀 productization P2: static-export stack (free static hosting, ~137 KB first paint, no server)
- **New `scripts/export_static.py` → `site/`** (gitignored, 5,046 files): reuses `parklife/api.py` shapes; `parks.json`+`parks/<id>.json`+`park-photos/<id>.json`, species detail & species→park-ids **bucketed `id % 512`** (`species/`, `species-parks/`), `search-index.json`, `meta.json`. Build ~70 s.
- **`webapp/app.js`** switched from `/api/*` to static shards (new `getJSON`/`data*` layer, `SPECIES_BUCKETS=512` must match exporter). Client-side search over `search-index.json`. **`sw.js` SHELL → relative + `/api/`→`/data/`** (fixed a real subpath 404 bug: absolute `/index.html` breaks on GitHub Pages' `/<repo>/`).
- **Chrome-verified:** existing `webapp/e2e_test.py` passes **unchanged** vs the static site + extra search/subpath checks, 0 console errors. First paint **~12 MB→137 KB gz**; park click ≤45 KB; species open ≤25 KB.
- **`scripts/deploy_pages.sh`** (orphan `gh-pages` force-push, throwaway index → main untouched; **NOT run** — deploy needs the user). Plumbing validated; `git gc` reclaimed local `.git` 128M→30M.
- **NEXT (user):** run the deploy + set Pages source to `gh-pages`; then `git rm --cached docs/parklife-data.json` + retire `docs/` = permanent git-slim. `serve_api`/`Dockerfile`/`fly.toml` demoted to dev/reference.

### 2026-06-29 (Claude/Opus) — 🧹 data-quality audit & remediation (wrong photos / dup species / placeholder names)
- Full read-only scan (`/tmp/dq_audit.py` → `data/audit/findings_20260629.md`) + remediation report `data/audit/REPORT_20260629.md`. DB backup `data/parklife.db.bak_pre_dq_audit_20260629`. **No git push.**
- **🔴 Wrong photos (biggest):** a since-reverted wrong-tid bug had left `species_photo` rows + heroes where ONE photo was attached to up to **36 unrelated species**. Fixed in 4 passes (`/tmp/fix_orphan_photos.py`, `fix_orphan_hero.py`, `fix4a_shared_gallery.py` + collect refill): quarantined the contaminated rows and **refilled galleries strictly from each species' OWN inat_taxon_id**. **shared species_photo URL groups 169→0, shared hero 21→0.**
- **🟠 Duplicate species:** merged **13** same-tid synonym pairs (`/tmp/merge_dups.py`) + **21** genus-reclassification synonyms (`/tmp/merge_ja_syn.py`, strict criterion: identical 和名 + same epithet + exactly one NULL tid → e.g. Charadrius→Anarhynchus, Egretta→Ardea intermedia). **tid collisions 13→0.** A post-cleanup legit photo re-acquire (gbif_media +37 spp, iNat galleries) exposed **3 more** gender-variant synonyms (Oceanodroma→Hydrobates furcata etc.) → merged too. **species 24786→24749 (−37 total merges).** **Deliberately did NOT merge** same-和名 SPLITS / homonyms (ウミスズメ = a bird AND a boxfish; アマサギ Ardea coromanda≠Bubulcus ibis) — 77 such pairs documented as REVIEW, untouched.
- **🟠 Placeholder names:** GBIF higher-taxon vernacular pollution (`Onychium japonicum`→`維管束植物門`, np=220; Buddleja/Zinnia/Kalmia got genus names) → corrected to iNat species 和名 (`/tmp/fix_genus_ja.py`, 5 fixes). Cleaned 2 orphan aliases.
- **🟠 Park-local galleries:** `park_species_photo` had attached one GBIF occurrence image to several co-located taxa (smut fungus + host plant; co-surveyed fish) — quarantined **1,672** cross-species-shared rows (`/tmp/fix7_psp_shared.py` → `park_species_photo_quarantine`; regenerable; pre-existing tool behavior).
- **Reversibility (nothing hard-deleted):** `species_photo_quarantine` (680), `park_species_photo_quarantine` (1672), `species_merged_log` (37), `name_fix_log` (5), `species_alias_removed` (2). Visible photo coverage 99.31% (inflated by wrong photos) → **98.56%** (honest, after legit re-acquire). **Final invariants ALL green at every layer:** shared photo URLs 0 (hero/species_photo/park_species_photo), tid collisions 0, dup scientific_name 0, orphan FKs 0, months_bitmap in-range. **REVIEW ITEMS RESOLVED (2nd pass):** id 74 fixed (`C. acuminata`/カリッサ属/tid 64128=eucalyptus → it's オオバナカリッサ = *Carissa macrocarpa* iNat 130215; corrected all fields). The 77 same-和名 pairs adjudicated (`/tmp/classify_ja_review.py`): **6 more same-epithet synonyms merged** (incl. マメヅタ flipped to the correctly-named row; `メダイチドリ` left as a likely 2022 *atrifrons* SPLIT), **70 are genuinely different species / homonyms** (ウミスズメ=鳥+魚, キハダ=木+鮪, splits like オオクチバス/アマサギ) → correctly KEPT. species → **24743**, `species_merged_log` **43**. All invariants still green.

### 2026-06-28 (Claude/Opus) — 📷 photo backfill (expansion-reopened gap): visible coverage 97.75% → 99.31%
- The +108-park expansion (entry below) reopened a photo gap: **195 visible species (in parks, katakana ja-name) had no photo.** Ran the 3 safe automated sources — `collect_species_photos` (iNat galleries), `gbif_media`, `wikicommons_hero` — all already exhausted (0 net new) for the tail.
- **Key win = a SAFE exact-only iNat resolver** (`/tmp/exact_inat.py`): for the 173 gap species lacking `inat_taxon_id`, look up iNat `/taxa` by scientific_name and set the tid ONLY when an iNat result's `name` EXACTLY equals our binomial (sidesteps the `best_match` genus-prefix/`results[0]` fuzzy bug). **136/173 exact matches applied** → +136 hero photos; `collect_species_photos` then added **+176 gallery rows**. `/tmp/taxon_default_photo.py` (fetch default_photo by trusted tid) found 0 more (those 20 tids genuinely photo-less on iNat).
- **Result: gap 195 → 60 (99.31%).** Remaining 60 = irreducible (hybrids マガモ×カルガモ / cultivars カワズサクラ・コブクザクラ / ferns Polystichum・Dryopteris / obscure molluscs — no iNat exact taxon, no GBIF StillImage, no Wikidata P18). `species_photo` 76,463 → 76,639. DB backup `data/parklife.db.bak_pre_photo_20260628`. **No re-dedupe needed** (photos don't touch park_species). New stack serves photos live from DB; legacy `docs/parklife-data.json` regen+push is the user's call.

### 2026-06-28 (Claude/Opus) — 🌳 park expansion: high-value tier (+108 parks → 3,190), 100% species coverage
- User picked the **high-value small-tier mix** over sports/max-coverage. Added P13 types **8 国営公園 + 10 緩衝緑地 + 12 広場公園 + 1 街区公園 + 7 レクリエーション都市 + 13 緑道, ≥5 ha** → `P13_KDP=8,10,12,1,7,13 P13_SUFFIX=p13hv .venv/bin/python -m scripts.p13_seed all`. **108 new parks** (緩衝46 / 国営27 / 広場17 / 街区7 / レク7 / 緑道4), deduped 1 km vs existing. Seeds in `data/seeds/<pref>-p13hv.json` (47 files, originals untouched).
- **Pre-recon finding:** the blessed ≥5 ha types (総合/地区/特殊/都市林/広域/都市緑地) are **100% ingested already** (verified by direct-distance dedup vs Tokyo 49/49 sanity) — so expansion = a NEW tier, not completing old ones. The remaining un-ingested ≥5 ha tiers: 運動568 / 近隣56 / 緩衝46 / 国営27 / misc35.
- **Pipeline:** load_seeds (3082→3190) → GBIF+iNat+eBird global run (cache made existing parks no-ops; **108 new parks fetched, ~113k new obs: GBIF 14,738 / iNat 97,550 / eBird 650, 0 errors**) → normalize (0 err) → backfill → dedupe. **All 108 new parks got species (100%)** — thesis holds (radius-only, no scraping). Totals: **3,190 parks / 3,174 w/ species / 24,786 species / 447,037 park_species / 758,440 obs.** DB backup `data/parklife.db.bak_pre_hvtier_20260628`.
- **Photo backfill** (expansion reopened 292 no-photo): `gbif_media` +96 species, `wikicommons_hero` re-run → visible photo coverage **97.8%**. Maintenance dry-runs CLEAN (merge 0, romaji 0). New API verified serving a new national park (Okinawa Ocean Expo, 306 spp). Legacy demo re-exported (`docs/parklife-data.json` → **70.1 MB**).
- **✅✅ PROFILING BACKLOG FULLY CLEARED (batches H1–H8, +218 profiles, sidecar 8499→8711, profile_count→8732):** the expansion's entire 218-species reopened backlog (133 np≥2 + 85 np=1) is done in 4 languages. **Only 2 irreducible stubs remain** (both non-profilable, skip): `Metriogryllacris magna` (garbled Chinese placeholder ja-name `小羽蟋蟋螽`) + `Phaneropterinae` (亜科 subfamily). Covered: birds/fish/odonata/butterflies/longhorns/mollusks/moths/ants/bees/salamanders/the Ryukyu long-furred rat etc., with en-names + zh-Hans aliases where standard. **Demo re-exported (`docs/parklife-data.json` → 70.7 MB).** **REMAINING (non-profiling):** ~192 obscure species photo-less (the floor — no accessible source). **Do NOT re-run the np query expecting candidates; only fresh iNat/GBIF ingestion reopens it.**

### 2026-06-27 (Claude/Opus) — ✨ webapp feature-complete: pagination + species→map reverse view + deep-linking + e2e suite
- Closed every old-demo gap in the new SPA and went beyond it. New `webapp/` features (all verified in Chrome, 0 console errors):
  - **Per-group "show more"** pagination (caps big parks at 48 cards/group → mobile-light). `ca5d815`.
  - **Species → map reverse view** — modal "🗺 show parks with this species" plots/highlights its parks via `/api/species/<id>/parks`, fits bounds, reset banner. `b70ab84`.
  - **URL deep-linking** — shareable `#park/<id>` / `#species/<id>` (open on load) + browser back/forward via `pushState`+`hashchange`.
  - **PWA** — installable (`manifest.webmanifest` + `icon.svg`, standalone) + service worker (`sw.js`: app-shell cache-first/offline, `/api/*` network-first w/ cache fallback). serve_api sends `application/manifest+json`. SW verified active + manifest installable in Chrome; full e2e suite still PASS with SW intercepting. (Follow-up: PNG icon set for older browsers.)
- **Committed regression suite `webapp/e2e_test.py`** (playwright `channel="chrome"`): map, panel, modal, i18n, all filters, park-local photos, pagination, reverse view, deep-linking. Run: server up → `.venv/bin/python webapp/e2e_test.py` → "E2E PASS".
- The new stack now **matches + exceeds the legacy 69 MB-blob demo**, served from the DB on demand. Combined with the deploy artifacts, the only thing left to retire the legacy blob (permanent git-slim) is the user deploying (`fly deploy` / any container host).

### 2026-06-27 (Claude/Opus) — 🚀 productization P1→P2: deploy artifacts for the new stack (unblocks permanent git-slim)
- Made the thin-client stack **deployable**: `Dockerfile` + `.dockerignore` + `fly.toml` (example, Tokyo/scale-to-zero) + `DEPLOY.md`. The API path is **100% stdlib** (verified: bare `python3.13`, no venv/pip, imports `parklife.api` + queries DB), so the image = Python base + code + `data/parklife.db`, **zero deps to install**.
- Added **immutable read-only DB mode** to `parklife/api._connect` (`PARKLIFE_DB_RO=1`): opens `file:…?mode=ro&immutable=1` — no write-lock, no `-wal`/`-shm`, works on a read-only mount. Set in the Dockerfile/fly env. **Verified natively** (bare python + RO env → healthz/stats/species all green, no `-wal` created).
- zh-name coverage checked: 96.0% (333 missing are obscure Japan-centric moths/beetles/molluscs; iNat zh-Hans hit only ~1/8; existing gbif_vernacular+wikidata_zh+wikipedia_zh already exhausted easy sources — **at practical ceiling, not grinding it / no fabrication**).
- **NEXT once user deploys** (needs their host account): `fly deploy` (or Render/Railway/Cloud Run from the Dockerfile) → then retire `docs/parklife-data.json` from tracking → `.git` stays permanently tiny. Files are additive; nothing deployed.

### 2026-06-27 (Claude/Opus) — 📷 GBIF media = 3rd hero-photo source: visible photo coverage 94.2%→98.0%
- New `scripts/gbif_media.py` — fetches GBIF occurrence `StillImage` media for visible species with NO photo (no iNat taxon + no Commons P18). **+304 species newly photo'd, 1,384 photo rows** (`source='GBIF'`); coverage **94.2%→98.0%** (8,237/8,408). Mix of museum/herbarium/iNat-via-GBIF images (Smithsonian, MBG, Academia Sinica, MSU…).
- **⚠️ Built WITH the wrong-taxon guard the old ensure_inat_taxon lacked:** accepts ONLY GBIF `species/match` results that are `EXACT`/`FUZZY` AND `rank=SPECIES` AND whose canonical binomial still equals the input — **rejects `HIGHERRANK`** (genus-only matches whose images are for the whole genus). 113 candidates correctly skipped; spot-check showed **0 guard leaks**. Cached: `data/cache/gbif_match/`, `data/cache/gbif_media/`. Idempotent (skips already-photo'd).
- **Remaining 171 no-photo visibles are the honest floor:** ~113 cultivar/synonym names with no species-rank GBIF taxon (`Cerasus 'Kobuku-zakura'`, `Iris germanica`…) + ~58 species GBIF has no image for. License of new rows: 1,026 CC BY-NC / 195 CC BY / 84 CC0 / rest SA/ND/none.
- DB backup `data/parklife.db.bak_pre_gbifmedia_20260627`. Maintenance dry-runs CLEAN (photos create no species). **New webapp/API serves them live from the DB** (verified). **Legacy demo re-exported** (`docs/parklife-data.json` 69.1→69.4 MB) — committing the regen (one ~+0.3 MB blob; re-bloat acceptable per the git-slim follow-up note).

### 2026-06-27 (Claude/Opus) — 🧹 git-slim: rewrote history, `.git` 2.6 GB → ~30 MB (⚠️ FORCE-PUSHED main, all SHAs changed)
- **⚠️ HISTORY REWRITTEN + FORCE-PUSHED to `origin/main`. Anyone with a clone (Codex etc.) MUST re-clone or `git fetch && git reset --hard origin/main` — old commit SHAs no longer exist.** Pre-rewrite HEAD was `c189442`.
- **Cause of the 2.6 GB:** history carried every regenerated demo artifact — `docs/index.html` (old *inlined-data* form, ~43 MB × ~215 commits = **9.3 GB uncompressed**), `data/species_profiles_extra.json` (~14 MB × ~140 batches = 1.9 GB), `docs/parklife-data.json` (390 MB), dropped `docs/parklife.json` (204 MB) + `park_species.ndjson` (70 MB).
- **Method:** `git filter-repo --strip-blobs-bigger-than 5M` (installed in venv). Stripped every >5 MB blob from all 605 commits; commit graph preserved but **173 commits that ONLY touched stripped artifacts ("docs: regenerate demo") were pruned → 605→432 commits.** All code/data/HANDOFF commits survived. Then **restored the two current source-of-truth files** (filter-repo had reverted HEAD to stale versions): `data/species_profiles_extra.json` (8,509 entries) + `docs/parklife-data.json` (23,717 species — live demo still needs it) from `/tmp/parklife_keep/`, re-committed, `git gc --prune=now`.
- **Result: `.git` 2.6 GB → ~30 MB.** Live GitHub Pages demo unaffected (current `docs/` preserved). **Full rollback backup: `/tmp/parklife_dotgit_bak` (the original 2.6 GB `.git`)** — restore by replacing `.git` + force-pushing it back if needed.
- **NEXT / follow-up to fully prevent re-bloat:** `docs/parklife-data.json` (69 MB) is tracked again so the legacy Pages demo keeps working; every future `regenerate demo` re-bloats. Once the NEW webapp/serve_api stack is deployed (needs a Python host — Fly/Render), retire the legacy blob demo: drop `docs/parklife-data.json` from tracking (.gitignore it) or host it as a GitHub Release asset. THEN `.git` stays permanently tiny.

### 2026-06-27 (Claude/Opus) — 🏗️ P1 cont'd: committed SPA + ported old-demo UI (sort/month/group/parking/park-photos)
- **Committed + pushed the SPA+API baseline to main** (`bf28523`, additive; `data/parklife.db` stays gitignored, `docs/` blob untouched).
- **Ported the remaining old-demo UI into `webapp/`** (all frontend except one new endpoint): per-park **sort** (record/name/sci), **month** seasonal soft-filter, **group toggle** (click header to collapse), map **parking-only filter**, and **park-local photos** in the species modal (📍-badged, before the global hero gallery) via new `GET /api/parks/<pid>/photos/<sid>` + `api.pair_photos()`.
- **Verified in Chrome** (`/tmp/pl_e2e2.py`): parking filter 15→2 markers, April filter 616→277 species, sort/group-toggle work, park-local photo badge + nav present — **0 console errors, E2E2 PASS**; v1 e2e still PASS (no regression). Backups in `/tmp/parklife_webapp_bak/`. **About to commit this UI batch.** **NEXT:** the real git-slim (destructive, needs go-ahead) or deploy-the-new-stack decision.

### 2026-06-27 (Claude/Opus) — 🏗️ productization P1: thin-client SPA + read-only API, full stack working
- User set goal "build the thin client until the new front+back end is usable; back up each step." **DONE & verified end-to-end.** Two layers, both new + additive:
  - **Backend** (stdlib only, no FastAPI dep): `parklife/api.py` (query layer over `data/parklife.db`, mirrors `export_html.collect_data` shapes but keyed by real DB ids; zh/zhT aliases batched into park-detail + search cards) + `scripts/serve_api.py` (`ThreadingHTTPServer`, also serves `webapp/` static files at `/`, path-traversal-guarded, CORS-open). Endpoints: `/api/stats` · `/api/parks` (+`?bbox=minLon,minLat,maxLon,maxLat&limit=`) · `/api/parks/<id>` · `/api/species/<id>` · `/api/species/<id>/parks` · `/api/search?q=` · `/healthz`.
  - **Frontend** `webapp/index.html` + `app.js` (Leaflet + MarkerCluster from unpkg): clustered park map → click park → grouped species cards → click species → photo-gallery modal + 4-lang profile; ja/en/zh/zhT toggle; debounced search. API base = same-origin `/api` (override `?api=`). See `webapp/README.md`.
- **Run it:** `.venv/bin/python -m scripts.serve_api` → <http://127.0.0.1:8787/> (one process = app + API, same origin).
- **VERIFIED in real Chrome** via `/tmp/pl_e2e.py` (playwright `channel="chrome"`, no browser download): map clusters render, 代々木 park = 616 cards/11 groups, modal photo + 3 profile sections, zh title 大山雀, search サクラ→24 cards, **0 console errors → E2E PASS.** Screenshot looked right.
- **Payload proof of the git-slim thesis:** map 0.8 MB (gzip ~150 KB) / park ~176 KB / species ~5 KB lazy — vs the old 69 MB single fetch. The blob is in git ONLY because the old SPA needs it; now it doesn't.
- **Safety:** purely additive — `docs/` (old live demo) untouched as rollback; only HANDOFF.md is a tracked edit; nothing committed/pushed. Backups in `/tmp/parklife_webapp_bak/`.
- **NEXT:** real git-slim (DESTRUCTIVE, needs go-ahead) = move data artifact out of tracking + history rewrite + force-push; or port remaining old-demo UI (parking filter / group checkbox+sort / per-park gallery photos / seasonal view — API already exposes the data).

### 2026-06-27 (Claude/Opus) — 📷 photo backfill after 地区公園 expansion: visible coverage 85.4%→93.6%
- Expansion had reopened the photo gap to ~1,224 visible no-photo. Ran `wikicommons_hero` (Commons hero species 5,705→**12,355**, +6,650) → `ensure_inat_taxon` (stopped once visible-resolution rate flattened, ~494 visible resolved) → `collect_species_photos` (+~9.7k iNat rows, 2,156 species) → `backfill_photo_license`. **Coverage 85.4%→93.6%** (7,868/8,408). Demo re-exported (62.6→**65.9 MB**) + pushed (39522f0). DB backup `bak_pre_photo_20260627`.
- **⚠️ Caught + reverted a data-quality bug:** `ensure_inat_taxon`'s `best_match` `results[0]` fallback mis-assigned tids to ~135 obscure GBIF-only species (5 bogus tids hitting many ferns/plants via the ja-name path; 78 collision groups like Amanita gemmata→muscaria, Rapa rapa→Pieris rapae), and `collect_species_photos` fetched their wrong-taxon photos. Diffed against the pre-run backup, NULLed every non-strong colliding today-assignment, deleted ~630 wrong photo rows. **merge_duplicate_species + fix_romaji_ja_names dry-runs now CLEAN.** Don't re-run ensure_inat without fixing best_match (see Photo-gallery follow-up). **NEXT = GBIF-media/Flickr 3rd source for the ~540 still-no-photo, or pivot to productization / いきものログ.**

### 2026-06-26 (Claude/Opus) — 🎉 reopened-backlog FINISHED B13–B19 + demo republish (8,509 sidecar)
- Picked up from B17 (B18 was drafted in /tmp but uncommitted). Ran B18 (+18) + B19 (+21) → cleared the entire **np1 long tail** (Hiatula→Zombrus). **Expansion-reopened 404-species backlog now 0 profilable.** Only 3 irreducible stubs left (Hiatula diphos junk placeholder, Phaneropterinae 亜科, Metriogryllacris magna garbled ja-name).
- merge_duplicate_species --dry-run CLEAN. **Re-exported + pushed demo** (`docs/parklife-data.json` 61.6→**62.6 MB**, commit 93a126c). zh-Hans aliases added only where standard CN unambiguous (黄粉虫/剑鱼/斑蝠/松江鲈/棘鳞蛇鲭). **NEXT = pivot off profiling** (Active #2 maintenance / #3 いきものログ / productization).

### 2026-06-26 (Claude/Opus) — 🐛 reopened-backlog curation B1–B8: +158 profiles (8,133 → 8,291)
- Worked the 地区公園 +670-park profiling backlog (was 404). **Cleared 158 species, 8 sidecar-only batches B1–B8** (each committed separately; tiny/instant push). Tiers: **np≥10 (24) + np5-9 (56) done; np2-4 78 of ~153 done.** **Remaining PROFILABLE = 215** (raw np-query is higher: +31 genus-level 属 stubs that are un-profilable — exclude with `NOT LIKE '%属'`). np1 (171) not started.
- Workflow (resume-ready in Active #1): `/tmp/q_tier2b.py <lo> <hi>` pulls next candidates (excludes romaji/・/（/属); `/tmp/profile_B<N>.py` robust-rekey template → `seed_species_profiles` → verify +N/−N → commit sidecar. zh-Hans aliases only where confident (reef fish/birds/mammals); obscure moths/beetles/cowries left zh-text-only (no fabricated alias). Profiled 2 prior "permanent skips" with defensible content (ハマダラハルカ kept family-agnostic; **Bodianus reticulatus IS a valid current name** = Asian sheepshead wrasse コブダイ). **Still skipped: `Metriogryllacris magna` ja='小羽蟋蟋螽' (Chinese/garbled — needs 和名 fix first).** merge dry-run CLEAN. **Demo NOT yet re-exported** — do once after all 404 cleared, then push docs/.

### 2026-06-25 (Claude/Opus) — 🏞 park-coverage milestone #2: 2,412 → 3,082 (+地区公園)
- Added P13 type-3 **地区公園 ≥5ha** (user chose this over 運動公園/area-relax). +670 parks. `p13_seed.py` made env-configurable (`P13_KDP`/`P13_SUFFIX`/`P13_DEDUP_M`/`P13_MIN_AREA_M2`, dedup 500m→1km); seeds in 47 new `*-p13ext.json` (originals intact).
- Enrichment queue (gbif+iNat+eBird ×47 + finishers) drained **0-fail**; 662/670 new parks got species, coverage **99.4%**. `merge_duplicate_species` cleaned GBIF-reintroduced synonyms (−98 sp, 5 bogus tids NULLed). **Final: 3,082 parks / 24,394 species / 428,893 ps-rows.**
- Demo re-exported (61.6 MB) + **committed & pushed** (60c1272); Pages republishing. **NEXT = photo image-source expansion** (GBIF/Wikimedia-Commons fallback for ~854 no-iNat species), per user.

### 2026-06-22 (Claude/Opus) — 🎉 curation + photos all wrapped: profiles 8,156 · zh 99.8% · en 99.6% · photos 88.8%
- **Profiling backlog cleared again** (A269–A290, +496 → **8,156** profiled species; 4-lang). Only 6 permanent skips remain (domestic dog/budgie, コブダイ junk sci, ハマダラハルカ unverifiable family, 小羽蟋蟋螽 Chinese-kanji name, 旧名ムラサキガイ). Insect/arachnid family-level-accurate descriptions; ⚠ warnings on toxic/biting/protected/endangered taxa.
- **zh中文名 + en英文名 coverage COMPLETE** (user: "1和2都进行"). Visible-species zh **517→20 missing (99.8%)**, en **409→29 missing (99.6%)** — the residual are the same permanent skips + genus/亜属 stubs. Method: MERGE-safe sidecar batches (`/tmp/zh_batchZ1..Z6.py`, `/tmp/en_batchE1.py`) + a **taxon-group-gated suffix→generic auto-mapper** (`/tmp/zh_automap.py`,`/tmp/en_automap.py`) for the np≤2 tail. Confident real names where known (中白鹭/日本大蠊/野荸荠/低斑蜻/枯叶蛱蝶/Large brown cicada/Japanese five-lined skink…), genus-level generics otherwise — **no invented specific names**. `apply_extra_aliases` in `seed_species_profiles` writes them to DB.
- **Photo backfill DONE:** `ensure_inat_taxon 1500`→`collect_species_photos 3000`→`backfill_photo_license`. **+5,943 photo rows**, 1,344 species gained photos, visible coverage **85.3%→88.8%** (7,137/8,041). species_photo=59,062, license 100% populated, 72.7% commercial-usable. Remaining 904 are at the iNat ceiling (854 no iNat match, 50 no iNat photos) → need GBIF/Commons media fallback to go higher.
- **Maintenance dry-runs CLEAN** (`merge_duplicate_species` 0 dups). **COMMITTED + PUSHED to main** (680d975 sidecar/code/43 seeds; 865823d docs/demo). **DEMO REPUBLISHED at national scale & LIVE** (<https://paranoid2droid.github.io/parklife/>): full export is only **59.5 MB** (under GitHub's 100 MB limit) — the long-held "single-JSON demo dies at national scale" assumption was wrong; no spatial backend needed to ship. The gitignored DB (194 MB, holds photos/aliases) stays local. `/tmp/*.py` batch scripts are disposable.
- **NEXT (pick one):** (a) GBIF/Wikimedia-Commons photo fallback for the 854 no-iNat species; (b) productization / spatial-backend track (memory [[productization]]); (c) いきものログ ingest (last external source). Profiling/zh/en/iNat-photos have no remaining fuel.

### 2026-06-20 (Claude/Opus) — 🌏 #1 NATIONAL EXPANSION COMPLETE: queue drained, 2,412 parks / 22,694 species
- **Enrichment queue fully drained, 0 failed.** Final: 2,412 parks (47 prefs, 2,377 w/ species = 98.5%), **22,694 species** (from 9,761), **354,951 park_species rows**, 487,167 obs. Sources GBIF 332k / iNat 136k / eBird 17k. Maintenance dry-runs clean.
- **eBird wired into the queue** after the user supplied a key: stored in gitignored `data/.ebird_key` (ebird.py env→file fallback; never git/plist), then queued `ebird <pref>` for all 43 new prefs + a 2nd dedupe (`scripts/queue_ebird_national.py`).
- **National expansion + the reopened profiling backlog are both DONE** (see the 2026-06-22 entry at the top for the full curation/photo wrap-up). This 2026-06-20 entry originally tracked the A-batch profiling loop (A235→A290); that loop is finished — do not re-run the np query expecting candidates.
  **▶▶ RESUME-AFTER-CLEAR (this is the live loop):** the national-expansion data work is 100% DONE (2,412 parks / 47 prefs / 22,694 species). The ONLY ongoing task is clearing the species_profile insect tail. Each batch:
  1. `sqlite3` / the np-desc query from Active #1 **WITH `AND s.taxon_group IN ('insect','arachnid')`**, `ORDER BY np DESC` LIMIT ~22, top-np first.
  2. Write `/tmp/profile_batchA2NN.py` — copy the robust-rekey structure of the last one (the file may be gone after clear; the structure is: `ENTRIES` dict keyed by scientific_name, each value `{sources, common_name_en, aliases:{zh-Hans}, ja/en/zh each with summary/habitat_hint/finding_tips}`; then NFKC-normalize-rekey every key to the DB's exact scientific_name and drop non-matches; write JSON back). ~18 entries. Family-level-accurate descriptions OK for obscure micro-moths/beetles (note family / habitat / comes-to-lights / larval foodplant if known); VERIFY ja-name↔scientific identity, don't invent specifics.
  3. `PYTHONPATH=/Users/zhe/ClaudeCode/parklife .venv/bin/python /tmp/profile_batchA2NN.py` then `.venv/bin/python -m scripts.seed_species_profiles`; confirm `added==len(ENTRIES)` and profiled rose ~+18 / backlog dropped ~-18.
  4. Update this PROGRESS line; keep going.
  **SKIP** 属 genus stubs + ASCII/romaji-prefix ja-names + these known-junk: `Bodianus reticulatus`(コブダイ), `Hiatula diphos`(旧名ムラサキガイ), domestic イエイヌ/セキセイインコ. **NOT committing/pushing — all changes (DB national expansion + sidecar `data/species_profiles_extra.json`) persist on disk, uncommitted, awaiting user OK to push.** Old per-batch templates were at /tmp/profile_batchA268.py (gone after a machine reboot; reconstruct from the structure above). (Skip the 2 domestic-animal rows イエイヌ Canis familiaris / セキセイインコ Melopsittacus undulatus + genus stub Andrias オオサンショウウオ属 — not profilable.) A235-A240 top-np→np≈11 (regionals + family-level moths/beetles); **A241–A244 cleared most of the high-value non-insect tail** (fish/birds/herps/mammals/mollusks: stickleback, lionfish, chum salmon, habu pit-viper, Asian black bear, Japanese giant salamander, mountain hare, mahi-mahi, Indian peafowl, Ryukyu scops owl, sword-tailed newt, Kuroiwa's gecko, Ryukyu flying fox, sea hares/nudibranchs/wrasses/angelfish/groupers, freshwater mussels/bitterlings, etc.). Batch template = `/tmp/profile_batchA244.py` (robust-rekey; run `PYTHONPATH=<repo> .venv/bin/python /tmp/profile_batchA2NN.py` then `seed_species_profiles`). **Next = A245: ~209 non-insect left** (query adds `AND s.taxon_group NOT IN ('insect','arachnid')`), then the ~680 insect tail with family-level descriptions. Verify ja-name/identity before profiling. **NOT committed/pushed (await user OK); sidecar persists on disk.** NOT committing/pushing — sidecar/seeds/code/`.ebird_key`/docs all uncommitted locally (await user OK on push); work persists on disk. Demo still NOT rebuilt (needs spatial backend).

### 2026-06-20 (Claude/Opus) — 🌏 #1 NATIONAL EXPANSION: nationwide seed+load done (461→2,412 parks), enrichment queued
- **Decision (user): queue the full nationwide rollout.** Seeded + loaded P13 都道府県立 parks for all 40 remaining prefectures (`p13_seed <40 slugs>` → 1,733 new) on top of the validated pilot. **DB now 2,412 parks / 47 prefectures / 2,394 with coords.** Backups `/tmp/parklife.db.pre_national_pilot` + `pre_national_full`.
- **Enrichment QUEUED, not run inline:** `scripts/queue_national_enrichment.py` appended 80 per-pref `inaturalist`/`gbif` tasks + 3 finishers (normalize/backfill/dedupe) to `data/run_queue.txt` (83 pending). Kicked `run_pending.py` (launchd `com.parklife.queue` also loaded → resumes every 30 min if my process dies). **Running now**, started at `inaturalist hokkaido`; ETA several hours. eBird skipped (no key).
- **Watch on resume:** when queue drains, verify with the pilot pattern (species total jump, park_species rows, `merge_duplicate_species`/`fix_romaji_ja_names` dry-runs). Then the profiling backlog reopens (~thousands of new katakana species). Demo still NOT rebuildable at this scale — spatial backend first (see [[productization]] / memory). **Code + 43 seed JSONs uncommitted.**
- (See prior entry below for recon + pilot + the load_seeds COALESCE bugfix.)

### 2026-06-20 (Claude/Opus) — 🌏 #1 NATIONAL EXPANSION kicked off: recon + pilot (461→679 parks)
- **Generalized P13 seeding to all 47 prefectures** (`scripts/p13_seed.py` now takes `<slug…|all>`, full code→slug map; no-arg = original 4). Recon (`scripts/p13_recon.py`, downloaded+cached all 47 zips): nationwide ≥5ha biodiversity-type parks = **1,930 NEW, ALL 都道府県立** (0 municipal noise) → 5× growth.
- **Ran a 3-pref pilot (aichi/osaka/kyoto = 197 parks)** end-to-end: seed→load→iNat+GBIF→normalize→dedupe. **100% park coverage** (197/197 got species by radius, no scraping); **+1,999 species** (366 visible/profilable+unprofiled, 1,632 GBIF sci-only), **+28,196 park_species rows**. Validates the "geo→aggregation, not scrapers" thesis. eBird skipped (no `EBIRD_API_KEY`).
- **🐛 Fixed a destructive `load_seeds.py` bug** (UPDATE clobbered geocoded lat/lon with seed NULLs → wiped coords on 171 Kanto parks on first run). Now `COALESCE(?, col)`; restored from backup, re-ran clean (661/679 keep coords). **Critical for nationwide.**
- Decisions THIS session (user): staged pilot first (not all-1,930-at-once); **demo NOT rebuilt** (single-JSON can't hold national scale — needs spatial backend first). DB LOCAL, backup `/tmp/parklife.db.pre_national_pilot`. **NEXT:** decide whether to roll out remaining 40 prefs via `run_queue`/`p13_seed all` (long, rate-limited) — and that the profiling backlog reopens (~366/pilot of new katakana species). Code+seeds uncommitted.

### 2026-06-20 (Claude/Opus) — 🅿️ parking classifier rebuilt (3-state + evidence tiers); demo TODOs found already-done
- **Verified the two "demo improvement" TODOs (#3 multilang, #5 checkbox+sort) were ALREADY shipped** — docs were stale. Updated memory `project_parklife_todos`.
- **Solved the parking-classification TODO (#2) at the root.** The real bug wasn't "too many NULL" — it was `osm_parking` writing OSM-absence as a CONFIDENT `has_parking=0` (>half of all negatives were just unmapped-in-OSM false negatives). Rebuilt as a 3-state model (1/0/NULL) + new `park.parking_source` evidence-tier column. **Canonical rules now in `parklife/parking.py` (SSOT)**; `extract_parking`/`reclassify_parking`/`osm_parking` delegate to it. New invariant: **absence≠negation** → OSM miss / no-text = NULL (re-checkable); only TEXT (駐車場なし/ありません) or restricted-only (団体/障害者/観光バス専用 = `text:restricted`) = 0.
- Migration `scripts/migrate_parking_source.py` (idempotent) backfilled source + demoted 39 false negatives. Final **390 yes / 33 no (all text-backed) / 38 unknown**. Documented in CLAUDE.md ("Parking classification: evidence over coverage") + Commands. DB change LOCAL (backup `/tmp/parklife.db.pre_parking_source`). **Demo not yet republished** — regen `export_html`+push to surface the new NULLs as "未知/unknown". When expanding to new prefectures, just run the 3 parking passes; NULL rows are honestly unknown.

### 2026-06-17 (Claude/Opus) — 🎉🎉 PROFILE CURATION COMPLETE: all deferrals resolved (A233 +6 →7,007)
- **Web-verified the 8 content deferrals one-by-one** and fixed DB ja-names, then profiled (A233): Wisteriopsis reticulata→ムラサキナツフジ (evergreen wisteria), Rhombodera basalis→マルムネカマキリ (giant shield mantis, exotic), Ptecticus aurifer→キイロコウカアブ (yellow soldier fly), Terpnosia nigricosta→エゾハルゼミ (Ezo spring cicada), Primula vialii→ホザキサクラソウ (orchid primrose). **Lunella coronata カンギク confirmed CORRECT** (JAMSTEC BISMaL — it's a real homonym of 寒菊, the earlier "mismatch" flag was wrong) → profiled.
- Cleanup: NULLed junk 動物界/植物界 ja-names on Latona bicolor + Torularia (no verified 和名, kept as invisible rows); **deleted** out-of-scope oral bacterium Streptococcus mutans (re-ran dedupe).
- **Result: every profilable visible species is done. Only 7 genus-level 属 stubs remain (irreducible).** DB fixes LOCAL (backup /tmp/parklife.db.pre_deferrals). Sidecar pushed.
- **Then did all of "next pivot" (1+2+3) same session:** **(2) maintenance** — `merge_duplicate_species` clean (0 dups); `fix_romaji_ja_names` sweep rescued 4 romaji-hidden species incl. **オオクロコガネ np=15** (A234, +4 →7,011), deleted 1 more bacterium; 0 romaji-prefix species remain. **(1) demo REPUBLISHED** — `export_html` → `cp docs/` → pushed the 48 MB `docs/parklife-data.json` (landed first try); public site now reflects all A150–A234 profiles + every local DB fix/merge/delete. **(3) productization probes** — ran both feasibility probes (results in memory `parklife-productization…`): photo-licensing gate is MILDER than feared (**~79–81% of species keep ≥1 commercial-usable photo**, thanks to 100%-free Wikimedia Commons; iNat alone only 13%), and the nationwide-GIS path is CONFIRMED (MLIT 都市公園 = dataset **P13**, already ingested — gives name/polygon/area/type free).

### 2026-06-16 (Claude/Opus) — np=1 FINISHED: A231–A232 (+35 →7,001) + murasaki merge
- **🎉 np=1 profilable tier CLEARED.** A231 (Viola diffusa→Ylistrum, +23) + A232 (Zaitzevia→Zonotrichia, +12, final). Profile count crossed **7,000**. Only 16 np=1 left — all documented deferrals/genus-stubs (see Active #1). Per-batch profiling has no more easy fuel.
- **Lithospermum murasaki RESOLVED** — merged into L. erythrorhizon (id 9023, profiled A198), kept synonym sci as alias, re-ran dedupe. Backup /tmp/parklife.db.pre_merge_murasaki.
- New deferral: `Wisteriopsis reticulata` (ja=サクコウ unverified/suspect). Highlights profiled: ⚠Sweet Violet (escaped), Bonin gampi (endemic, washi fibre), silky wisteria (twines opposite Noda), hornet twisted-wing parasite (Strepsiptera), ⚠invasive Taiwan bamboo carpenter bee, John Dory + mirror dory, ⚠Golden-crowned Sparrow (very rare vagrant), jujube (kanpo 大棗), 2 clean-water riffle beetles.
- ~18 en + 1 ja fix (Viola odorata romaji→ニオイスミレ). All sidecar-only, pushed each batch. DB fixes + merge LOCAL (gitignored DB; backups /tmp/parklife.db.pre_A231/A232). `docs/` demo still behind — regen when desired.

### 2026-06-14→15 (Claude/Opus) — np=1: A225–A230 (+156 →6,966)
- 6 batches continuously (Stethojulis bandanensis → Viola biflora). **52 np=1 remain** (V→Z tail; finish in sight). **Next A231 from Viola diffusa** — ⚠ fix Viola odorata romaji ja `nioi-sumire`→ニオイスミレ first; skip deferrals + 7 genus stubs in Active #1.
- New permanent skips added: Streptococcus mutans (oral bacterium), Terpnosia nigricosta (ja=セミ placeholder), Torularia (ja=植物界 junk), genus stubs Sympetrum/Tenodera/Tetragnatha.
- Highlights: emerald cuckoo wasp, ⚠Mikado pheasant + ⚠Temminck's tragopan (zoo birds), ⚠⚠3 toxic Takifugu puffers, ⚠⚠Tokyo bitterling (Natural Monument), cherry witches'-broom, ⚠whitetip reef shark, Japanese flying squid (surume) + jack mackerel (aji) + largehead hairtail (tachiuo), ⚠CITES giant clam, ⚠Asian tadpole shrimp (living fossil), Chinese black truffle, ⚠Common Murre (very scarce in JP), ⚠⚠white false hellebore (mistaken for wild greens), Amami whip scorpion (vinegar spray), Manchurian elm (Ainu attus fibre).
- ~63 en + 5 ja fixes (incl. romaji yoshi-susuki→ヨシススキ, kanji 米栂→コメツガ; Strelitzia/Taxus genus-name→species). All sidecar-only, pushed each batch. DB fixes LOCAL (backups /tmp/parklife.db.pre_A225…A230). `docs/` demo still far behind.

### 2026-06-14 (Claude/Opus) — np=1: A223–A224 (+52 →~6,810)
- 2 more batches (Sinanodonta calipygos → Sternaspis costata). 208 np=1 remain. **Next A225 from Stethojulis bandanensis** (skip deferrals + 4 genus stubs in Active #1).
- Highlights: tomato/eggplant (⚠leaves toxic), ⚠⚠invasive creeping oxeye (world's-100-worst), ⚠Pacific sleeper shark, hokkigai surf clam, 2 peat mosses, Madagascar jasmine, ⚠parasitic Squamanita fungus, Hachijo screw orchid, chimney murex, Christmas-tree tubeworm, hair jelly, red-billed starling + Chilean skua (rare), tintinnid plankton (microscopic — framed as microscope-only), duck mussel, silverbush (⚠seeds toxic).
- ~8 en-name fixes across the 2 batches. All sidecar-only, pushed each batch. DB fixes LOCAL (backups pre_A223/A224). `docs/` demo still far behind.

### 2026-06-14 (Claude/Opus) — np=1: A219–A222 (+104 →~6,758)
- 4 batches continuously (Rhododendron boninense → Simalura coerulea). 260 np=1 remain. **Next A223 from Sinanodonta calipygos** (skip deferrals + 4 genus stubs in Active #1).
- **NEW deferral `Rhombodera basalis`** (ja=カマキリ = generic "mantis" word on a specific exotic shield mantis; needs verified katakana).
- Highlights: ⚠Bonin azalea + Bonin needlewood (Ogasawara endemics), 4 rhododendrons ⚠toxic, ⚠decollate snail (invasive predator), ⚠orange mud crab, ⚠red-headed centipede, ⚠venomous rabbitfish (アイゴ), 2 rockfish ⚠spines, queenfish ⚠spines, 2 ⚠toxic earthballs, 4 Russula ⚠confusable, fox's-spear mulberry fungus, 4 sea slugs/nudibranchs, dragon swallowtail, yellowtail amberjack, chub mackerel/sardine, climbing goby, Whinchat + yellow-rumped warbler (rare vagrants), glassworts, snowrose, ホンゴウソウ mycoheterotroph.
- ~36 en-name fixes + 1 ja-name fix (Ribes rubrum romaji→アカスグリ) across the 4 batches. All sidecar-only, pushed each batch. DB fixes LOCAL (backups pre_A219…A222). `docs/` demo still far behind — regenerate + push when convenient (46 MB throttle, see Status).

### 2026-06-14 (Claude/Opus) — np=1: A217–A218 (+48 →6,654)
- 2 more batches (Pseudostellaria heterophylla → Rhodobryum roseum). 364 np=1 remain. **Next A219 from Rhododendron/next-alpha** (skip deferrals + 4 genus stubs in Active #1).
- **NEW deferral `Ptecticus aurifer`** (ja=黄色後架虻 Chinese-style name; needs katakana). Fixed ja `Quadrastichus erythrinae` デイゴ→デイゴヒメコバチ (it's the gall WASP, not the host coral tree).
- Highlights: many Ogasawara/island endemics & seabirds — ⚠Bonin flying fox (critically endangered), ⚠Bryan's shearwater (world's rarest seabird), Bonin petrel, Providence/Kermadec petrels, flesh-footed shearwater, Bonin brake ferns; ⚠cane toad (invasive/toxic), red-whiskered bulbul (invasive), black rat, Erythrina gall wasp (pest of Okinawa's deigo); guava, red oak, lady palms, ⚠Ramaria coral fungus (toxic), prickly fanfish (deep-sea).
- All sidecar-only, pushed each batch. DB fixes LOCAL (backups pre_A217, pre_A218). `docs/` demo still far behind — regenerate + push when convenient (46 MB throttle, see Status).

### 2026-06-14 (Claude/Opus) — np=1: A208–A216 (+231 →6,606)
- 9 more batches continuously (Orchestes hustachei → Pseudosperma rimosum). 412 np=1 remain. **Next A217 from Pseudostellaria/next-alpha** (skip the deferrals + 4 genus stubs in Active #1).
- **NEW: deferred `Primula vialii`** (ja=サクラソウ belongs to P. sieboldii — mislabel); added genus stubs `Platambus`, `Pseudogobio` to the permanent skip list.
- Highlights: Pallas's cat, rock hyrax, tiger, sheep, Reeves's muntjac (all zoo/park mammals); ⚠New Guinea flatworm (invasive snail-killer), ⚠Amur River corbula (invasive), striped eel catfish ⚠venom, blackfoot firefish ⚠venom; Ijima's leaf warbler (Izu endemic), Hachijo saw stag beetle (endemic), Bonin screw pine; jewel bug ニシキキンカメムシ, opium poppy ⚠banned, tomatillo, passion fruit, Japanese indigo アイ, 6 Polystichum holly ferns, 5 wild cherries, accentors, storm-petrels (earlier), bioluminescent bitter oysterling.
- All sidecar-only, pushed each batch. DB fixes LOCAL (backups pre_A208…A216). `docs/` demo still lagging far behind — regenerate + push when convenient (Status notes the 46 MB throttle).

### 2026-06-14 (Claude/Opus) — np=1: A204–A207 (+104 →6,375)
- 4 more batches continuously (Muntiacus reevesi → Orchestes horii). 643 np=1 remain. **Next A208 from Orchestes hustachei** (skip same 5 placeholders).
- Highlights: ⚠Reeves's muntjac (キョン, invasive deer), Siberian weasel, Japanese spider crab already done; 3 storm-petrels (Leach's/Matsudaira's/Swinhoe's), sooty tern, bristle-thighed curlew, 2 mantis shrimps (incl. sushi shako シャコ), 2 Ophiocordyceps, 2 brittle stars, mourning cloak, Hungarian glider, blue mussel, sea noodle alga, several bryophytes/lichens, Ogasawara endemics (Ochrosia nakaiana), ⚠toxic false garlic, Japanese devil's club.
- DB fixes LOCAL-only (/tmp/fixesA204–A207.py via param-binding; backups pre_A204…A207): ~34 en-name corrections (Snapper-type junk, lowercase, "Egg Bird"→Sooty Tern, "banana-plant"→Water Snowflake, "Wild Fennel"→Love-in-a-mist) + ja-disambig (none new). Sidecar pushed each batch; `docs/` demo still lagging.

### 2026-06-13 (Claude/Opus) — np=1: A199–A203 (+123 →6,271)
- 5 batches run continuously (Lucilia illustris → Mulloidichthys vanicolensis). 747 np=1 remain. **Next A204 from Muntiacus reevesi** — SKIP 5 placeholders at top (Latona/Lithospermum/Lunella deferred + genus stubs Meretrix/Monstera adansonii; see Active #1).
- **NEW DEFERRAL `Lunella coronata`** (A199): ja=カンギク (winter-chrysanthemum) wrong for a turban shell → mismatch. **Genus stubs Meretrix (ハマグリ属) / Monstera adansonii (モンステラ属) now skipped** (real species profiled instead).
- Highlights: Megaptera novaeangliae ザトウクジラ (humpback whale), Macrocheira kaempferi タカアシガニ (Japanese spider crab — world's largest arthropod), Micromys minutus カヤネズミ (harvest mouse, woven ball nest), Miniopterus fuliginosus (Asian bent-winged bat), Muntiacus-adjacent: Morus boninensis オガサワラグワ (⚠ critically endangered Ogasawara endemic), Mauremys sinensis ハナガメ (⚠ invasive turtle), Lybia tessellata キンチャクガニ (pom-pom crab), Misumena vatia (goldenrod crab spider), 3 Morchella morels (⚠ never raw), Monstera deliciosa, Lycoris × albiflora (⚠ toxic), Mitchella undulata (2 flowers→1 berry).
- DB fixes (LOCAL-only, /tmp/fixesA199–A203.sql, backups /tmp/parklife.db.pre_A199…A203): ~24 en-name corrections + 2 ja-name fixes (Maianthemum yesoense 広葉雪笹→ヒロハユキザサ; Mimachlamys crassicostata ヒオウギ→ヒオウギガイ scallop-disambig). Sidecar backfilled ~70 common_name_en + ~120 zh aliases across the 5 batches. All sidecar pushed; `docs/` demo still lagging (regenerate+push when convenient, see Status).

### 2026-06-10 (Claude/Opus) — np=1: A198 (+24 →6,148)
- A198 Liriomyza sativae→Lucilia ampullacea (24, sidecar-only). 870 np=1 remain. **Next A199 from Lucilia illustris — SKIP Latona bicolor + Lithospermum murasaki** (both deferred; see Active #1).
- **Deferred `Lithospermum murasaki`** (ja=ムラサキ, no iNat id): profiled the valid `L. erythrorhizon` (紫草/red gromwell dye plant) instead; murasaki is almost certainly its synonym → flagged for verify-and-merge next session. Substituted Lucilia ampublacea to keep batch at 24. (Latona bicolor still deferred too.)
- Highlights: Lissachatina fulica アフリカマイマイ (⚠️ INVASIVE giant African snail — rat-lungworm vector, no bare-hand warning), Lophura nycthemera ハッカン (Silver Pheasant, +eBird), Livistona boninensis オガサワラビロウ + Lobelia boninensis オオハマギキョウ (Ogasawara endemics), Lucanus gamunus ミクラミヤマクワガタ (Izu Is. Mikura stag beetle endemic), Lonicera morrowii キンギンボク (⚠️ toxic gourd-shaped red berries), Lobelia inflata (⚠️ toxic Indian tobacco), Liriomyza sativae (vegetable leafminer pest), 2 Lixus weevils, 2 Lottia limpets, Loxocalyx ambiguus マネキグサ.
- DB fixes: en — Lobelia inflata "Lowbelia"→Indian Tobacco, Lonicera morrowii "Fly Honeysuckle"→Morrow's Honeysuckle, Loxogramme "Oblong-sorus lance fern"→caps. Sidecar backfilled 15 common_name_en (Bonin Fan Palm, Bonin Lobelia, Mikura Stag Beetle, Manchurian Periwinkle, White-banded Carpenter Bee, Lindberg's/Fine-sculptured Limpets, Snout Moth, flea/click/cricket/geometer names, etc.) + 14 zh aliases (小笠原蒲葵, 大滨桔梗, 御藏深山锹甲, 满洲玉黍螺, 中紫斑螟, 斜萼草, 婆婆纳跳甲, 大扁脸蟋, 林氏/细雕花帽贝, 后纹黑波尺蛾, 斑/长尾筒喙象). All sidecar pushed; `docs/` demo lagging (~33 batches behind — regenerate+push when convenient, see Status).

### 2026-06-10 (Claude/Opus) — np=1: A197 (+24 →6,124)
- A197 Lespedeza tomentosa→Liquidambar styraciflua (24, sidecar-only). 894 np=1 remain. **Next A198 from Liriomyza sativae — SKIP Latona bicolor** (list in Active #1).
- **Two ja-name homonym fixes** (both DB rows wrongly shared a name): Lilium formosana テッポウユリ→タカサゴユリ (Taiwan lily, distinct from L. longiflorum テッポウユリ/Easter lily); Linaria purpurea ヒメキンギョソウ→ムラサキウンラン (purple toadflax, distinct from L. maroccana ヒメキンギョソウ/annual toadflax).
- Highlights: Lethrinus nebulosus ハマフエフキ (spangled emperor, Okinawa "tamin"), Leucaena leucocephala ギンネム (invasive white leadtree, mimosine-toxic), Leucophaeus pipixcan (Franklin's Gull, rare vagrant) + Leucosticte arctoa ハギマシコ (Asian Rosy Finch) + Limnodromus semipalmatus (Asian Dowitcher) = birds (+eBird), Liquidambar formosana フウ/styraciflua モミジバフウ (Chinese/American sweetgums, alternate maple-like leaves), Ligustrum micranthum + Lilium-formosana naturalised, Limosella aquatica キタミソウ (rare water mudwort), Linckia guildingi (comet star, single-arm regen), Lima vulgaris ミノガイ (swimming file clam), Lindera aggregata テンダイウヤク (medicinal 烏薬).
- DB fixes: en junk/caps — Lethrinus "blue emperor"→Spangled Emperor, Leucaena "vi-vi"→White Leadtree, Leucosticte "Rosy Finch"→Asian Rosy Finch, Lima "Pacific fileclam"→Pacific File Clam, Limnophila "ambulia"→Ambulia, Limosella "Mudwort"→Water Mudwort, Lindera sericea "Hairy spicebush"→Hairy Spicebush, Liquidambar "Formosan-gum"→Chinese Sweetgum & "redgum"→American Sweetgum, Lilium formosana "Taiwan lily"→Taiwan Lily, Linaria purpurea "Purple Linaria"→Purple Toadflax. Sidecar backfilled common_name_en (Orchard Spider, Golden-haired Bonnet, Japanese Cuckoo Wasp, Fringed Geometer Moth, Bonin Privet, Click Beetle) + zh (台湾百合, 金色小菇, 日本褶翅小蜂, 黄房姬尺蛾, 小花女贞, 紫姬金叩头虫). All sidecar pushed; `docs/` demo lagging (~32 batches behind — regenerate+push when convenient, see Status).

### 2026-06-10 (Claude/Opus) — np=1: A196 (+24 →6,100)
- A196 Lathraea japonica→Lespedeza japonica (24, sidecar-only). 918 np=1 remain. **Next A197 from Lespedeza tomentosa — SKIP Latona bicolor** (list in Active #1).
- **DEFERRED `Latona bicolor`**: ja=動物界 junk placeholder; confirmed it's a Donacidae bean clam (iNat 1610898) but couldn't find a verified katakana name → not profiled, substituted Lespedeza japonica to keep batch at 24. It now sits at the top of the np=1 query; needs a ja-name before profiling.
- Highlights: Lathraea japonica ヤマウツボ (chlorophyll-free root parasite), Lepidodactylus lugubris オガサワラヤモリ (mourning gecko — all-female parthenogenetic), Leptospermum scoparium ギョリュウバイ (mānuka), Lepas anatifera/anserifera (goose barnacles), Lepidium sativum コショウソウ (garden cress) + L.africanum (African peppercress), Lemmaphyllum microphyllum マメヅタ (bean-ivy epiphytic fern), 3 Lejeunea pounceworts (liverworts), ⚠️ Lepiota castanea/fusciceps = TOXIC dapperlings (amatoxin), Lathyrus aphaca (yellow vetchling, ODAP-toxic seeds).
- DB fixes: en junk/caps — Lavandula × intermedia lavandin→Lavandin, Lepidium africanum "Bird Weed"→African Peppercress, Lepidium sativum "Cress"→Garden Cress, Lepidodactylus "Sad Gecko"→Mourning Gecko, Lepidotrigla "Belalang"→Japanese Gurnard, Leptogium "blue skin lichen"→Blue Skin Lichen, Leptospermum "manuka"→Mānuka, Lemmaphyllum/Lathyrus quinquenervius caps; Lespedeza japonica alias シラハギ lang en→ja. Sidecar backfilled common_name_en (Japanese Toothwort, Bronze Jumping Spider, pouncewort names, dapperling/blewit/fern names, White Bush Clover) + zh (醒目薰衣草, 镰叶细鳞苔, 淡青阔吻象, 白花胡枝子). All sidecar pushed; `docs/` demo lagging (~31 batches behind — regenerate+push when convenient, see Status).

### 2026-06-10 (Claude/Opus) — np=1: A195 (+24 →6,076)
- A195 Lactarius chrysorrheus→Lateolabrax latus (24, sidecar-only). 942 np=1 remain. **Next A196 from Lathraea japonica** (list in Active #1).
- Highlights: Lactuca sativa チシャ (lettuce), Lagenaria siceraria ヒョウタン (bottle gourd), Lamprocapnos spectabilis ケマンソウ (bleeding heart, toxic), Larus glaucoides アイスランドカモメ (Iceland Gull, +eBird), 4 Lasius ants (incl. umbratus temporary social parasite), 2 Lagocephalus puffers (toxic, expert prep), Lactoria diaphana (spiny boxfish, skin toxin), Laportea cuspidata ミヤマイラクサ (mountain stinging nettle), Lateolabrax latus ヒラスズキ (blackfin sea bass), Laevicaulis alte (invasive leatherleaf slug), 3 milkcaps (acrid).
- DB fixes: en — Lagocephalus spadiceus "spade blaasop"→Half-smooth Golden Puffer, Laportea "Cuspidate girardinia"→Mountain Nettle. Sidecar backfilled common_name_en (Blackfin Sea Bass, Lanceleaf Jasmine prior) + zh (冰岛鸥). All sidecar pushed; `docs/` demo lagging (~30 batches behind — regenerate+push when convenient, see Status).

### 2026-06-10 (Claude/Opus) — np=1: A194 (+24 →6,052)
- A194 Japanocorus caeruleipennis→Lactarius camphoratus (24, sidecar-only). 966 np=1 remain. **Next A195 from Lactarius chrysorrheus** (list in Active #1).
- Highlights: Juglans regia カシグルミ (English walnut), 3 Jasminum (Spanish/pink jasmines fragrant), Kalanchoe pinnata セイロンベンケイ (air plant, plantlet-budding invasive), Koelreuteria bipinnata オオモクゲンジ (Chinese flame tree, pink lantern fruits), Kogia breviceps コマッコウ (pygmy sperm whale), Labroides dimidiatus ホンソメワケベラ (bluestreak cleaner wrasse), Kunugia undans (lappet moth, venomous-haired larva), Lactarius camphoratus (candy cap/curry milkcap), L.hirsutus=ルピナス属 (lupine, genus-level).
- DB fixes: en junk/foreign — Juglans "Ceviz"(Turkish)→English Walnut, Kogia "Lesser Cachalot"→Pygmy Sperm Whale, Kalanchoe "flopper"→Air Plant, Koelreuteria "Goldenrain"→Chinese Flame Tree, Labroides "Wrasse"→Bluestreak Cleaner Wrasse, Jasminum caps, Juncus fauriei multi-name→Faurie's Rush, Kaiwarinus caps. Sidecar backfilled common_name_en (Lanceleaf Jasmine) + zh (羽扇豆). All sidecar pushed; `docs/` demo lagging.

### 2026-06-10 (Claude/Opus) — np=1: A193 (+24 →6,028, under 1,000 remain)
- A193 Indocrassatella oblongata→Japananus hyalinus (24, sidecar-only). **990 np=1 remain (under 1,000).** **Next A194 from Japanocorus caeruleipennis** (list in Active #1).
- Highlights: Ipomoea alba ヨルガオ (moonflower, night-blooming), Ipomoea cairica モミジヒルガオ (mile-a-minute vine, invasive) + Ipomoea pes-caprae グンバイヒルガオ (beach morning glory) + Ipomoea quamoclit ルコウソウ (cypress vine), 2 Inosperma fibrecaps (TOXIC muscarine), Ixora chinensis サンタンカ (Okinawa flowering shrub), Ictalurus done A192, Indoplanorbis exustus (ramshorn snail, schistosome host), Japananus hyalinus (Japanese maple leafhopper, globally spread).
- DB fixes: Ipomoea alba romaji ja yakai-sō→ヨルガオ + "Lilac Bell"→Moonflower (+purged romaji zh 'yue guang hua'); en caps — Ipomoea cairica →Mile-a-minute Vine, Isognomon legumen →Pod Tree Oyster, Istiblennius →Rockskipper, Ixora →Chinese Ixora, Jacquemontia →Smallflower Morning-glory, Japananus "Leafhopper"→Japanese Maple Leafhopper, Irus macrophylla caps. Sidecar backfilled common_name_en (Beach Morning Glory). All sidecar pushed; `docs/` demo lagging.

### 2026-06-10 (Claude/Opus) — np=1: A192 (+24 →6,004, crossed 6,000)
- A192 Hypena indicatalis→Indocalamus tessellatus (24, sidecar-only). **Crossed 6,000 profiled species.** 1,014 np=1 remain. **Next A193 from Indocrassatella oblongata** (list in Active #1).
- Highlights: Hypomesus nipponensis ワカサギ (Japanese smelt, ice fishing), Ictalurus punctatus チャネルキャットフィッシュ (channel catfish — INVASIVE Alien Species warning), Hypholoma lateritium クリタケ (brick cap, vs poisonous sulphur tuft), Indigofera tinctoria ナンバンアイ (true indigo dye), 4 Ilex hollies (cornuta/geniculata/goshiensis/micrococca), Indocalamus tessellatus オオバヤダケ (ruo bamboo/zongzi wrap), Hyposidra talaca (black looper, citrus pest), 4 Idaea waves.
- DB fixes: en — Hypomesus "Wakasagi"→Japanese Smelt, Ictalurus "Catfish"→Channel Catfish, Indigofera "indigo"→True Indigo (+ja romaji nanban-ai→ナンバンアイ), Indocalamus caps. No sidecar backfills. All sidecar pushed; `docs/` demo lagging.

### 2026-06-10 (Claude/Opus) — np=1: A191 (+24 →5,980)
- A191 Hirundapus caudacutus→Hypena claripennis (24, sidecar-only). 1,038 np=1 remain. **Next A192 from Hypena indicatalis** (list in Active #1).
- Highlights: Hirundapus caudacutus ハリオアマツバメ (White-throated Needletail, fastest level-flight bird, +eBird), 2 Hosta (ギボウシ/ミズギボウシ), Hyacinthoides hispanica (Spanish bluebell), 2 Hylotelephium stonecrops (ベンケイソウ/ミツバベンケイソウ), Hymenophyllum oligosorum (filmy fern), Hydropsyche orientalis (net-spinning caddisfly, water-quality indicator), Hygophum reinhardtii (lanternfish), Heterodontus done A190, Hymenoscyphus fructigenus (Nut Disco on acorns).
- DB fixes: en caps/standard — Hirundapus "Spine-tailed Swift"→White-throated Needletail, Hyacinthoides "Spanish Squill"→Spanish Bluebell, Hygophum caps, Hylotelephium verticillatum caps, Hymenophyllum caps. No sidecar backfills this batch (NULL-en insects left without invented names). All sidecar pushed; `docs/` demo lagging.

### 2026-06-10 (Claude/Opus) — np=1: A190 (+24 →5,956)
- A190 Hemidactylus frenatus→Hippocampus coronatus (24, sidecar-only). 1,062 np=1 remain. **Next A191 from Hirundapus caudacutus** (list in Active #1).
- Highlights: Hemidactylus frenatus ホオグロヤモリ (common house gecko), Heterodontus japonicus ネコザメ (Japanese bullhead shark, spiral egg cases), Heterocentrotus mamillatus パイプウニ (slate pencil urchin), Hexabranchus lacer ミカドウミウシ (Spanish dancer), Hippocampus coronatus タツノオトシゴ (crowned seahorse), 4 Hibiscus (hamabo salt-marsh, arnottianus Hawaiian white, glaber Ogasawara endemic, tetraphyllus トロロアオイ washi-neri), Hernandia nymphaeifolia (sea hearse, peltate leaves).
- DB fixes: en junk/caps — Hemidactylus "Chichak"→Common House Gecko, Hemigrapsus "Pacific crab"→Brush-clawed Shore Crab, Hemigymnus →Barred Thicklip Wrasse, Hemitrichia →Salmon Eggs Slime Mould, Hermania orca "Animal"→NULL, Hernandia →Sea Hearse, Heteranthera →Kidneyleaf Mud-plantain, Heterocentrotus caps, Heterodontus "bull head"→Japanese Bullhead Shark, Hibiscus arnottianus →Hawaiian White Hibiscus, Hibiscus hamabo caps, Hippocampus →Crowned Seahorse. Sidecar backfilled common_name_en (Glossy-leaved Hibiscus) + zh (西班牙舞者海蛞蝓/黄蜀葵). All sidecar pushed; `docs/` demo lagging.

### 2026-06-08/10 (Claude/Opus) — np=1: A188–A189 (+48 →5,932)
- A188 Gonocephalum persimile→Halieutaea stellata, A189 Haloa japonica→Hemibarbus longirostris (24 each, sidecar-only; committed together). 1,086 np=1 remain. **Next A190 from Hemidactylus frenatus** (list in Active #1).
- Highlights: Grus monacha ナベヅル (Hooded Crane, Izumi) + Halcyon coromanda アカショウビン (Ruddy Kingfisher) = birds (+eBird), Grammistes sexlineatus ヌノサラシ (sixline soapfish, toxic grammistin mucus), 2 Goodyera jewel orchids, Guepinia helvelloides (apricot jelly), Gymnura japonica (butterfly ray), Halieutaea stellata アカグツ (starry handfish, fin-walker), Hedychium gardnerianum (Kahili ginger, invasive), Hebeloma sacchariolens (sweet poisonpie), Hemibarbus genus (gudgeons), Heliotropium arboreum モンパノキ (tree heliotrope).
- DB fixes: en junk/caps — Grammistes "Grouper"→Sixline Soapfish, Granulifusus caps, Grateloupia →Devil's Tongue Weed, Guepinia "Salmon Salad"→Apricot Jelly Fungus, Gymnura caps, Halieutaea "Batfish"→Starry Handfish, Hedychium "ginger-lily"→Kahili Ginger, Heliothrips "Thrips"→Greenhouse Thrips, Hemerocallis dumortieri caps; purged garbled zh aliases (Halenia '翡翠', Hemibarbus longirostris '长吻 鱼骨'). Sidecar backfilled common_name_en (Spurred Gentian, Stalked Jellyfish, Japanese Witch-hazel) + zh (圆叶金缕梅/仙台萱草). All sidecar pushed; `docs/` demo lagging.

### 2026-06-08 (Claude/Opus) — np=1: A186–A187 (+48 →5,884)
- A186 Freesia refracta→Gelochelidon nilotica, A187 Gempylodes ornamentalis→Gonocephalum coriaceum (24 each, sidecar-only; committed together). 1,134 np=1 remain. **Next A188 from Gonocephalum persimile** (list in Active #1).
- Highlights: Ficus religiosa/sacred fig handled in A185; here Fritillaria thunbergii バイモ (浙贝母 medicinal), Galium verum カワラマツバ (lady's bedstraw), Gavia adamsii ハシジロアビ (White-billed Diver) + 2 Gallinago snipe + Geokichla sibirica マミジロ (Siberian Thrush) = birds (+eBird), 3 Geastrum earthstars + Gelatinipulvinella (earthstar-parasite fungus), 2 Gloeophyllum mazegills, Glandirana genus (wrinkled frogs), Glechoma hederacea カキドオシ (ground ivy), 4 chromodorid nudibranchs.
- DB fixes: Geastrum mirabile kanji ja 雛土柿→ヒナツチガキ; en junk/caps — Festuca-era done; here Ficus religiosa-era done; Geastrum saccatum bowl→Bowl Earthstar, Gelochelidon "Marsh Tern"→Gull-billed Tern, Gavia "White-Billed Diver"→White-billed Diver, Fritillaria "fritillary"→Thunberg's Fritillary, Fromundus "Burrower bug"→Burrower Bug, Freesia caps, Gibasis "bridalveil"→Tahitian Bridal Veil, Ginebis Crump's Margarite, Glycymeris albolineata caps. Sidecar backfilled common_name_en (Necklace Sea Star, Egg Cockle, Smallscale Blackfish) + zh aliases (三裂老鹳草). All sidecar pushed; `docs/` demo lagging.

### 2026-06-08 (Claude/Opus) — np=1: A185 (+24 →5,836)
- A185 Felaniella usta→Fraxinus mandshurica (24, sidecar-only). 1,182 np=1 remain. **Next A186 from Freesia refracta** (list in Active #1).
- Highlights: Ficus religiosa インドボダイジュ (sacred fig/bo tree), Fragaria ananassa オランダイチゴ (garden strawberry), Forsythia viridissima シナレンギョウ (greenstem forsythia), 4 Festuca fescues, 3 Fissidens pocket mosses (incl. translucent F. hyalinus), Fraxinus mandshurica ヤチダモ (Manchurian ash), Ficopomatus enigmaticus (invasive Australian tubeworm), Frankliniella intonsa (flower thrips pest), Fistulobalanus albicostatus (white-ribbed barnacle).
- DB fixes: 2 romaji ja→katakana (Festuca heterophylla→ハガワリトボシガラ, Fragaria ananassa→オランダイチゴ); en junk/caps — Festuca ovina "Ovina"→Sheep's Fescue, Ficus religiosa "botree"→Sacred Fig, Ficopomatus "tube worm"→Australian Tubeworm, Fraxinus mandshurica "Japanese ash"→Manchurian Ash, Frankliniella "Thrips"→Flower Thrips, Fragaria "Strawberry"→Garden Strawberry, +2 fescue caps. Sidecar backfilled 2 zh aliases (异叶羊茅/草莓). All sidecar pushed; `docs/` demo lagging.

### 2026-06-08 (Claude/Opus) — np=1: A184 (+24 →5,812)
- A184 Eupithecia subfumosa→Falagria caesa (24, sidecar-only). 1,206 np=1 remain. **Next A185 from Felaniella usta** (list in Active #1).
- Highlights: Eutrema japonicum ワサビ (wasabi), Euploea mulciber ツマムラサキマダラ (Striped Blue Crow, range-expanding), Eurystomus orientalis ブッポウソウ (Oriental Dollarbird, nest-box conservation, +eBird), Euprymna morsei ミミイカ (Morse's bobtail squid, luminous bacteria), Exallias brevis セダカギンポ (leopard blenny, coral-eating), Euwallacea validus (ambrosia beetle), Evernia mesomorpha (air-quality indicator lichen), 2 Exolaternula lantern clams.
- DB fixes: en caps/junk — Euprymna morsei "Mimika bobtail"→Morse's Bobtail Squid (+purged garbled romaji zh alias), Eutrema japonicum wasabi→Wasabi, Evernia mesomorpha ring lichen→Ring Lichen, Exallias brevis "pao'o kauila"→Leopard Blenny, Falagria caesa "Staph beetle"→Rove Beetle, Eurystomus orientalis Dollarbird→Oriental Dollarbird. Sidecar backfilled 1 common_name_en (Striped Blue Crow). All sidecar pushed; `docs/` demo lagging.

### 2026-06-08 (Claude/Opus) — np=1: A183 (+24 →5,788)
- A183 Eubrianax granicollis→Eupithecia clavifera (24, sidecar-only). 1,230 np=1 remain. **Next A184 from Eupithecia subfumosa** (list in Active #1).
- Highlights: 4 Euhadra Japanese land snails (クチベニマイマイ red-lipped etc.), Eumeta variegata オオミノガ giant bagworm (declined via introduced parasitic fly) + Eumeta minuscula チャミノガ tea bagworm, Eumicrotremus awae ダンゴウオ lumpsucker (diver favourite), Euonymus boninensis ヒメマサキ (Ogasawara endemic spindle), Euphorbia ebracteolata マルミノウルシ (toxic latex spurge, root=狼毒), Euops pustulosus blue leaf-rolling weevil, 2 water-penny beetles (Eubrianax).
- DB fixes: stripped "(広義)" from 2 Euhadra ja-names (Euhadra eoa→ヒラマイマイ, Euhadra sandai→コガネマイマイ; note half-width parens slip past the full-width `（` query filter); en caps Euonymus sachalinensis→Siberian Spindletree, Euphorbia ebracteolata→Red Spurge. Sidecar backfilled 3 common_name_en + 2 zh aliases (茶蓑蛾/小笠原卫矛). All sidecar pushed; `docs/` demo lagging.

### 2026-06-08 (Claude/Opus) — np=1: A182 (+24 →5,764)
- A182 Epipterygium tozeri→Eubranchus inabai (24, sidecar-only). 1,254 np=1 remain. **Next A183 from Eubrianax granicollis** (list in Active #1).
- Highlights/hazards: Eranthis pinnatifida セツブンソウ (spring ephemeral, toxic Ranunculaceae), Erythrina variegata デイゴ (Okinawa floral emblem, seeds toxic-alkaloid), Eruca vesicaria キバナスズシロ (arugula/rocket), Ericerus pela イボタロウムシ (Chinese white wax scale), Eriocaulon heleocharioides コシガヤホシクサ (once extinct-in-wild, reintroduced), Eriocheir ogasawaraensis (Ogasawara endemic mitten crab), Etrumeus micropus ウルメイワシ (round herring), Epipterygium tozeri アカスジゴケ (red-stemmed moss).
- DB fixes: en caps/junk — Ericerus pela "pela scale"→Chinese White Wax Scale, Erronea errones "mistaken cowrie"→Mistaken Cowrie, Erythrina variegata "coraltree"→Indian Coral Tree; ja kanji→katakana Erysimum cheiranthoides 蝦夷スズシロ→エゾスズシロ. Sidecar backfilled 6 common_name_en + 3 zh aliases (烟草跳甲/羽叶菟葵/小笠原绒螯蟹). All sidecar pushed; `docs/` demo lagging.

### 2026-06-08 (Claude/Opus) — np=1: A181 (+24 →5,740)
- A181 Elysia marginata→Epipremnum aureum (24, sidecar-only). 1,278 np=1 remain. **Next A182 from Epipterygium tozeri** (list in Active #1).
- Highlights/hazards: Elysia marginata コノハミドリガイ (kleptoplasty + self-decapitation/regeneration), Engina mendicaria bumblebee snail, Epipomponia nawai セミヤドリガ (cicada ectoparasite moth, parthenogenetic), Epipremnum aureum ポトス/golden pothos (calcium-oxalate toxic), Entoloma murrayi キイボカサタケ (pinkgill — not eaten), Ephemera strigata モンカゲロウ (fly-fishing model mayfly), Enkianthus cernuus シロドウダン (nodding enkianthus).
- DB en fixes: Emilia javanica "Irish Poet"(cultivar junk)→Tassel Flower; Engina mendicaria "striped engina"→Striped Engina. Sidecar backfilled 7 common_name_en + 2 zh aliases (日本胡桃蛤/薄壳胡桃蛤). All sidecar pushed; `docs/` demo not regenerated this batch (lagging per Status).

### 2026-06-06 (Claude/Opus) — EXPORT SPLIT: de-inline demo data (git-bloat fix)
- `export_html` no longer embeds the dataset in `docs/index.html` (was 46 MB, regenerated every batch → 2.2 GB `.git`). Now writes a stable ~85 KB `index.html` that `fetch()`es a sibling `docs/parklife-data.json` (~46 MB). Wrapped client JS in `boot(DATA)`; verified `node --check` + JSON valid + HTTP 200 served.
- Dropped the two **unreferenced** dumps `parklife.json`/`park_species.ndjson` from publish (`deploy.py`); still produced locally by `export_json`. Per-commit churn ~96 MB → ~46 MB and index.html stops churning.
- ⚠️ Trade-off: `file://` open no longer works (browsers block local fetch) — preview via `cd docs && python -m http.server`. The 2.2 GB already in history is untouched (future-only fix).

### 2026-06-06 (Claude/Opus) — np=1: A178–A180 (+72 →5,716)
- A178 Dichomeris tostella→Distoleon nigricans (porcupinefish/toxic-berry warnings), A179 Ditrema temminckii→Dryopteris maximowiczii (incl. アマガエル Japanese tree frog, beard lichens, Drupa rock snails), A180 Dryopteris mayebarae→Eleusine coracana (7 Dryopteris ferns, Duranta toxic, Globe Thistle, Finger Millet). 1,302 np=1 remain. **Next A181 from Elysia marginata.** All sidecar pushed clean; `docs/index.html` still uncommitted/lagging (see Status). **Session A157–A180 = +574 profiles (5,142→5,716).**

### 2026-06-05 (Claude/Opus) — np=1: A175–A177 (+71 →5,644) + Davidia typo-dup merge + sidecar workflow
- A175 Crotalaria sessiliflora→Cyclosa laticauda, A176 Cyclosa maritima→Delosperma cooperi (skip Davidia ivolucrata typo-dup, merged into Davidia involucrata), A177 Delta pyriforme→Dichomeris acuminatus (+23). 1,374 np=1 remain. **Next A178 from Dichomeris tostella.**
- Adopted **`/tmp/finish_np1_sidecar.sh`** (commit sidecar JSON only per batch → reliable small pushes). `docs/index.html` regenerated through A176 but its 43.9 MB push kept failing; left uncommitted in working tree for the next good window (see Active #1). All sidecar data pushed clean.
- Hazards flagged: Zebra Lionfish + Long-spined Sea Urchin (Diadema) + Pine caterpillar (urticating) venomous; Daphne/Crotalaria/Cuscuta toxic; Coconut/Cucumber/Melon/Winter Squash/Quince/Carnation crops; Black Swan, Blue Tit, Dove Tree/Handkerchief Tree, Oleander Hawkmoth.

### 2026-06-05 (Claude/Opus) — np=1: A173–A174 (+48 →5,573) + resolved push outage
- A173 Conocephalus halophilus→Corgatha pygmaea (10 venomous cone snails, warnings), A174 Cornus hongkongensis→Crossorhombus kobensis (6 Corydalis + Crinum/Croomia toxic). 1,446 np=1 remain. **Next A175 from Crotalaria sessiliflora.**
- **PUSH OUTAGE RESOLVED:** the 5 stacked commits (A171–A174 + HANDOFF) packed to **77 MB** because `docs/index.html` (43.5 MB) was re-committed each batch; throttled upload kept disconnecting on both port 22 and 443 (ls-remote fine → upload-only problem). Fixed by **splitting**: pushed sidecar JSON+HANDOFF first (small, instant), then `docs/index.html` separately (succeeded on ~3rd retry). History squashed to 9021755 (data) + 44830f5 (index.html). **Adopted `/tmp/finish_np1_sidecar.sh` (sidecar-only commits) + push index.html once per session — see Active #1 PUSH LESSON.**

### 2026-06-05 (Claude/Opus) — np=1 tier continued: A169–A172 (5,430→5,525, +95)
- A169 Chamaegastrodia shikokiana→Chonostropheus chujoi, A170 Choreutis japonica→Citrullus lanatus, A171 Citrus unshiu→Cobitis magnostriata (+23, see below), A172 Coccinula crotchi→Conocephalum salebrosum. 1,494 np=1 remain. **Next A173 = Conus-heavy (10 cone snails, venom warnings) — list in Active #1.**
- **Merged a typo-duplicate species:** `Cocccinella septempunctata` (3 c's, ja=テントウムシ, id 2499) was a misspelled dup of real `Coccinella septempunctata` (ナナホシテントウ, id 851). Repointed its 1 observation to 851, deleted the typo row + aliases + park_species, re-ran `scripts.dedupe` (park_species rebuilt to 126,812). Backup at `/tmp/parklife.db.pre_merge2499`. A171 therefore = +23 not +24.
- Highlights/hazards: Autumn Crocus/Colchicum (DEADLY colchicine — fatal misident warnings), Blueberry Hermit Crab (Natural Monument, protected), Lady Amherst's Pheasant, Cattle Egret/Daurian Jackdaw/Northern Bobwhite, Satsuma Mandarin/Watermelon/Coconut/Quinoa/Cinnamon, Chanterelle, butterflyfishes/parrotfishes, Tongue-biter isopod. DB fixes ~40 en/ja.
- **⚠️ GitHub push outage** from ~A171 onward: pushes time out (`pushed_exit=128`). A171 (20cf213) + A172 (b2c6b68) committed locally but NOT on remote. **Next agent must `git push` until clean.**

### 2026-06-04/05 (Claude/Opus) — np=1 tier continued: A163–A168 (5,286→5,430, +144; all pushed)
- A163 Bipalium kewense→Brassica oleracea, A164 Bromus sterilis→Caldesia parnassifolia, A165 Calidris mauri→Carangoides equula, A166 Caranx ignobilis→Carex pseudocyperus (18 Carex sedges), A167 Carex pyrenaica→Centrocolumna striata, A168 Ceramium kondoi→Chamaecrista nictitans (all +24 verified). 1,590 np=1 remain. **Next A169 from Chamaegastrodia shikokiana (list in Active #1).**
- Highlights/hazards flagged: Bipalium kewense (toxic invasive land planarian, TTX), Carybdea box jellyfish (painful sting), Sand Tiger Shark (intrauterine cannibalism), Marsh Marigold/Bupleurum longiradiatum/Brunfelsia (toxic), Giant/Vagabond/Wrought-iron butterflyfishes, Jindai-akebono & Kanzan & Takato cherries, Chanterelle/Dryad's Saddle (edible/notable fungi), Tongue-biter isopod (タイノエ), Cattle Egret/Temminck's Stint/Western Sandpiper (birds), Papaya/Quinoa.
- DB fixes (~35): en junk/caps (Catbird already done; "Pope"→Papaya, "Go bok"→Cattle Egret, "Raggie"→Sand Tiger Shark, "Bagwhale"→Minke, "Morrone"→Napier Grass, "tocalote"→Maltese Star-thistle, "Pande"…); ja kanji→katakana (仁王藪苧麻→ニオウヤブマオ, 深山紅猪口→ミヤマベニイグチ, 小鰯鯨→ミンククジラ from earlier); purged romaji zh aliases ('zui yu cao','wu la cao'). All A163–A168 pushed clean (no push retries needed).

### 2026-06-04 (Claude/Opus) — np=1 tier continued: A157–A162 (5,142→5,286, +144; 6 commits, all pushed)
- A157 Ainsliaea cordifolia→Aphaenogaster osimenseis, A158 Aphareus rutilans→Argiope boesenbergi, A159 Argopistes tsekooni→Aseroe rubra, A160 Asimina triloba→Asyneuma japonicum, A161 Atergatis integerrimus→Axylia putris, A162 Azalea jodogawa→Bidens parviflora (all +24 verified). 1,734 np=1 candidates remain. **Next A163 from Bipalium kewense (list + DB-junk fixes in Active #1).**
- Highlights: many toxic/dangerous flagged — Amanita death-caps (hongoi/kotohiraensis/sphaerobulbosa), Green Anole (invasive), White-spotted & Striped Puffers (TTX), Red Egg Crab (PSP/TTX), Northern Pacific Seastar (invasive), Sea Poison Tree, Wormwood/absinthe, Pawpaw, Giant Reed, Bird's-nest & Hart's-tongue ferns, Minke Whale, Golden Shower Tree, White Muscardine Fungus, Star Stinkhorn, Brown Noddy/Purple Heron/Little Swift (birds). Lots of Athyrium lady ferns (A161) and Asplenium spleenworts (A160).
- DB fixes (en caps/precision, ja kanji→katakana): Anous stolidus Catbird→Brown Noddy, Apus affinis "LISW"→Little Swift, Apodemus speciosus German junk→Large Japanese Field Mouse, Balaenoptera 小鰯鯨→ミンククジラ +Bagwhale→Common Minke Whale, Beauveria "Icing sugar fungus"→White Muscardine, Auxis rochei Bonito→Bullet Tuna, Aulostomus Trumpet→Chinese Trumpetfish, +~12 more. **NEW push flakiness: 43 MB push timed out once (A162) → manual `git push` retry; see Active #1 PUSH NOTE.**

### 2026-05-31 (Claude/Opus) — np=2 tier continued: A139–A142 (4859→4911, +52 net; commits + pushes)
- A139 Russula bella→Scudderocoris (+24), A141 Scyra quadridens→Solenomphala debilis (+24). **A140 AND A142 were fabricated-candidate misfires** (built from invented/misremembered names, not the real query rows): A140 +0 (cleaned via `/tmp/fix_a140_keys.py`); A142 only +3 of 15 (the new ROBUST normalize-rekey step auto-dropped 5 fabricated orphans + 7 were already profiled — so no pollution, but mostly wasted). 110 np=2 candidates remain. **Next A143: copy the verified clean 24-name list in Active #1 EXACTLY.** Demo 41.8→41.9 MB.
- Highlights: Sakura Shrimp, Christmas Tree Worm, Coast Redwood, Japanese Squirrel, Tiger Keelback (venomous), Castor Bean (ricin), Potato/solanine, Carolina Horsenettle, Tall Goldenrod, Bur Cucumber, Hijiki, lots of whelks/false-limpets/spikemosses/slime moulds.
- DB fixes: en caps/junk (Japanese Squirrel from Spanish "Ardilla de Japén", Milk Thistle from "Disseld", Foxtail Millet, Chinese Pond Mussel, Bezoar Rapa Whelk, Luna Lionfish, etc.); Sargassum fusiforme taxon_group ''→algae. **Adopted the ROBUST BATCH METHOD (normalize-rekey, see Active #1) which finally stopped the recurring hidden-char/orphan breakage.**

### 2026-05-31 (Claude/Opus) — np=2 tier continued: A133–A138 (4762→4859, +~97 net; 6 commits + pushes)
- A133 Plagodis→Polystichum kiyozumianum, A134 Polystichum mashikoi→Pseudocneorhinus obesus, A136 Pseudogaleomma→Rapana bezoar, A138 Regimbartia→Rudarius ercodes (real, +24 each). **A135 & A137 were fabricated-candidate misfires** (names not in DB → no profiles; orphans purged in A136/A138). ~160 np=2 candidates remain.
- DB fixes: many en cap/quality (Branching Hump Coral, Luna Lionfish, Black-legged Kittiwake, Japanese Greater Horseshoe Bat, Chinese Sumac, etc.); ja kanji→katakana (コノテガシワ, オオヤマザクラ); generic ja トカゲ→フトアゴヒゲトカゲ; purged garbled/（误） zh aliases.
- **Restored 4 species corrupted by a bad-id UPDATE** (Fissidens taxifolius/Baeochila horvathi/Aphis gossypii/Entoloma atrum) from `bak_pre_cleanup_20260529`. See ⚠️ PROCESS LESSON in Status. Demo 41.5→41.7 MB.

### 2026-05-31 (Claude) — np=2 tier continued: A130–A132 (4690→4762, +72; 3 commits + pushes)
- A130 Orthotylus gotohi→Paradarisa, A131 Paraglenurus→Persicaria viscosa, A132 Petrolisthes→Plagiomnium. 256 np=2 candidates remain. **Next A133 from Plantago/Platanus onward.** Demo 41.3→41.5 MB.
- Highlights: Rice (イネ), Akoya Pearl Oyster, Nameko, Tigertail Spruce, Dark-spotted Frog, Kamchatka Leaf Warbler (eBird), Trap-jaw/scorpionfly, Green Nettle Caterpillar Moth (stinging, warned), Veiled Chiton (carnivorous), 6 smartweeds (Persicaria), porcelain/hermit crabs, surfgrass.
- **DB fixes**: stripped "(広義)" from `Pedicularis resupinata`; en caps/precision — "zebra prawn"→Green Tiger Prawn, "Japanese pearl-oyster"→Akoya Pearl Oyster, "Rockweed"→Artillery Plant, "paradoxa grass"→Hood Canary-grass, "flammed bonnet"→Flame Bonnet, "swanky sweeper"→Schwenk's Sweeper.

### 2026-05-31 (Claude) — np=2 tier continued: A122–A129 (4498→4690, +192; 8 commits + pushes)
- A122 Hypselodoris→Laguncula, A123 Lampranthus→Leymus, A124 Libellula→Macrosemia, A125 Macrothelypteris→Meleonoma, A126 Melibe→Monocentris, A127 Monodonta→Nematogmus, A128 Nematopogon→Nymphaea alba, A129 Nymphaea tetragona→Orthotylus. 328 np=2 candidates remain. **Next A130 from Oryzias/Osmunda onward.** Demo 40.7→41.3 MB.
- Highlights: Steller's Sea Eagle/Bluethroat/Marsh Grassbird/Tristram's Storm-petrel (eBird), Japanese Marten, Coypu+Rainbow Trout (invasive), Reishi, Yesso Scallop, East Asian Common Octopus, Sweet Potato, Olive, Bitter Melon, Daffodil/Forget-me-not/Sensitive Plant, Trap-jaw Ant, blister/oil beetles (cantharidin warned), Pineconefish, Sargassum Frogfish, Schistosome snail (ミヤイリガイ).
- **Data-error fixes**: `Macoma tokyoensis` ja-name was ゴイサギ (a heron!) → ユウシオガイ; `Oncomelania hupensis` garbled "ヒューペミゾヒダニナ" → ミヤイリガイ; `Ninox scutulata` "フーアアオバズク" → アオバズク; `Larix kaempferi`/`Myosotis` romaji ja → katakana. ~50 en placeholders/junk fixed ("Species code"/"Animal"/"Arthropod"/"Bow"/"Tabacco"/"kaphal"/"Yam"+pinyin junk aliases). Homonym watch: ヒイラギ (Nuchequula = ponyfish ≠ holly), クロサギ, ビワガイ.

### 2026-05-31 (Claude) — np=2 tier continued: A118–A121 (4402→4498, +96; 4 commits + pushes)
- A118 Eunaticina→Gabala, A119 Gagea→Gonitis, A120 Gortyna→Heracleum, A121 Heriaeus→Hypoponera. 520 np=2 candidates remain. **Next A122 from Hypsipyla / Hypsizygus onward.** Demo 40.4→40.7 MB.
- Highlights: Steller's Sea Eagle + Lesser Frigatebird + Red Junglefowl (eBird/Wikipedia), Reishi/Lingzhi (霊芝), Japanese Beech (ブナ), Sargassum Frogfish, Sago-palm cousins, Dragonfly Club Fungus (ヤンマタケ, cordyceps on dragonflies), Long-tailed Braconid (ウマノオバチ, giant ovipositor), Horse Dung Sea Urchin, 4 seagrasses, 3 Helvella saddles, climbing/oakleaf hydrangea, Petty Spurge (toxic latex), Common Hogweed (phototoxic sap), Giraffe.
- **DB fixes**: ~30 en placeholders/junk fixed — "Species code: Hp/Hu" seagrass→Pineneedle/Narrowleaf Seagrass, "Algae"→Gracilaria, "Animal"→Pond Skaters, "Fungi"→White Saddle (also ja `菌界`→ノボリリュウタケ), "Chicken"→Red Junglefowl, "Giraffa"→Giraffe, "Coronet"→Bluespotted Cornetfish, typos (splindletree, pearlbrush). zh alias fixes: 努比亚长颈鹿→长颈鹿, 锯足软腹懈→锯足软腹蟹.

### 2026-05-31 (Claude) — np=2 tier continued: A112–A117 (4258→4402, +144; 6 commits) [details in git log]
- A112 Centaurium→Cicurina, A113 Cimex→Coptis, A114 Coptis quinquefolia→Cycas, A115 Cymodocea→Dipterygina, A116 Dischissus→Enkianthus, A117 Enkianthus subsessilis→Eumyias.
- Highlights: deadly Textile Cone + Basking Shark + Green Sea Turtle, Sago Palm/Datura/Scotch Broom (toxic), Tsuchi-akebi orchid, Pallas's Bunting + Western House Martin (eBird), verditer flycatcher, several seagrasses/cones/Cordyceps.
- **DB fixes**: romaji ja placeholders → katakana (o-hashibami/furorida/sotetsu); junk en "Species code: Cr/Ea"→Round/Tape Seagrass, Cetorhinus 'Homer'→Basking Shark, Chelonia 'Black Turtle'→Green Sea Turtle, etc.; purged garbled zh `菊苣 ju ju`. **A113 process note: first pass wrote 23 — always verify ENTRIES = 24.**

### 2026-05-31 (Claude) — np=2 tier continued: A110–A111 (4210→4258, +48; 2 commits + pushes)
- A110 Bolbitius→Camponotus, A111 Campylopus→Centaurea. ~760 np=2 candidates remain. **Next A112 from Centipeda onward.** Demo crossed 40.0 MB.
- Highlights: Spoon-billed Sandpiper (critically endangered) + Cackling Goose + Bulwer's Petrel + Curlew/Broad-billed Sandpiper + Pallas's Rosefinch (eBird), Fanwort + Yellow Star-thistle (invasives), Madagascar Periwinkle (toxic, anticancer source), bombardier beetle, 2 skeleton shrimps, Snow Camellia, Tsukubane (hemiparasite), Yellow Catalpa, Goat.
- **DB en fixes**: Calendula Ruddles→Pot Marigold, Calidris pygmaea Spoonbill Sandpiper→Spoon-billed Sandpiper (A110); Caprella scaura Amphipod→Skeleton Shrimp, Catharanthus Oldmaid→Madagascar Periwinkle, Centaurea geeldissel(Dutch)→Yellow Star-thistle (A111). **Reminder: include all 24 query rows even when ~10 already have en+zh — they still lack profiles.**

### 2026-05-31 (Claude) — np=2 tier continued: A107–A109 (4138→4210, +72; 3 commits + pushes)
- A107 Anthonomus→Argyrodes, A108 Arichanna→Atractomorpha, A109 Auletobius→Blattella. ~808 np=2 candidates remain. **Next A110 from Blechnum onward.**
- Highlights: Red-throated Pipit + Ring-necked Duck + a Pochard×Ferruginous-Duck hybrid (eBird), Greater Argonaut (paper nautilus), Heike firefly, Tropical Milkweed (toxic), Strawberry Tree, Tatarian Aster, German Cockroach, velvet ants (painful sting), brooding water bug (Appasus), Longfin Waspfish (venomous), 4 Astropecten sand stars, several lady ferns / boletes / nudibranchs / moths.
- **DB en fixes**: Anthus cervinus tree pipit→Red-throated Pipit (A107); Aythya collaris Moon-bill→Ring-necked Duck, Basella alba Spinach→Malabar Spinach, Blattella germanica Crotonbug→German Cockroach (A109). Demo 39.6→39.8 MB.

### 2026-05-31 (Claude) — np=2 tier opened: A104–A106 (4066→4138, +72; 3 commits + pushes)
- Started the np=2 long-tail (952 candidates), alphabetical from A. A104 Abbottina→Aegista, A105 Aeshna→Amata, A106 Amelanchier→Anthemis. ~880 remain.
- Highlights: 3 deadly/toxic fungi (アケボノドクツルタケ amatoxin death cap, シロオニタケ, ナカグロモリノカサ), 2 invasive aliens (alligator weed, sessile joyweed), Death's-head Hawkmoth, European Peacock, Snow/Swan/Tundra Bean Goose (eBird), Century Plant, kiwi, Feijoa, Chives, cockroach-hunting jewel wasp (Ampulex), cedar tiger longhorn (forestry pest), Chinese jumping worm, ~10 sea spiders/molluscs/jewel beetles.
- **Code**: `scripts/seed_species_profiles.py` GENERIC_EN_NAMES += `animal`,`plant` (corrects text-mining "Animal" en placeholders). DB cleanups: purged junk aliases `竣蜓`+`Lian Zi Cao`; fixed `Aglais io` en `Paecock`→`European Peacock`. Demo 39.5→39.6 MB. **Next A107 from Anthocharis onward.**

### 2026-05-31 (Claude) — np=3 tier CLEARED: A83–A103 sweep (3576→4066, +490; 21 commits + pushes)
- 21 batches alphabetically swept Abies homolepis → Ziphius cavirostris; **np≥3 coverage now 100%** (0 candidates remaining, verified). Crossed 4,000 profiled species.
- Highlights: Loggerhead Sea Turtle, ドクウツギ/Japanese Coriaria + ハシリドコロ/Japanese Belladonna + トウゴマ/Castor Bean (ricin) (all highly poisonous — strong warnings), Aromia bungii/桃红颈天牛 (invasive cherry pest), Grass Carp + Golden Apple Snail + Muskrat + Florida Redbelly Turtle + Amur Hedgehog (invasives), カツオノエボシ/Portuguese Man o' War (dangerous sting), スベスベマンジュウガニ (TTX-toxic crab), ゴンズイ/イソカサゴ/ニザダイ (venomous fish). Birds (eBird): Black Wood Pigeon, Rhinoceros Auklet, 3 Calidris, Caspian Tern, Chinese Egret, Yellow Bunting, 2 snipe, Chinese Grey Shrike, Herring Gull, Water Rail, Red-footed Booby, Japanese Murrelet, Izu Thrush, Hoopoe, Desert Wheatear. Plus red sea bream, abalone, Cuvier's Beaked Whale, ミズバショウ, オニバス, ルリモンハナバチ (blue cuckoo bee), many nudibranchs/molluscs/slime moulds/sedges/ferns/inverts.
- Local DB fixes: alias purges `Jin-Wu-Zéi`→金乌贼, `杏 xing`→杏; romaji ja `tama-no-kanzashi`→タマノカンザシ, `kuro-ezo`→エゾマツ. ~13 placeholder en names fixed (Cannon→Indian Shot, mud carp→Grass Carp, Madai→Red Sea Bream, Puput→Eurasian Hoopoe, Pelopèe→Mud Dauber, Wond→Castor Bean, etc.). Demo 38.1 → 39.5 MB. **np=2 tier next (A104).**

### 2026-05-30 (Claude) — np=4 tier CLEARED: A82 closeout (3546→3576, +30; committed + pushed)
- Batch A82 swept Taeniogonalos → Yucca (30 entries), closing the np=4 tier. **np≥4 coverage now 100%** (verified 0 remaining).
- Highlights: Tulip (郁金香), Feverfew + Tansy (小白菊/菊蒿), Bodhi Linden (ボダイジュ/南京椴, temple tree), Japanese Trillium (延龄草), Hiba (アスナロ/罗汉柏), Banded Houndshark (ドチザメ/皱唇鲨), By-the-wind Sailor (カツオノカンムリ/帆水母), Elephant Mosquito (トワダオオカ, predatory non-biting), Golden Bladderwort (carnivorous aquatic), Yucca, 吴茱萸.
- Demo 38.0 → 38.1 MB. **np=3 tier next: 490 candidates, restart alphabetically from Abies homolepis. A83 onward.**

### 2026-05-30 (Claude) — np=4 tier A81 (3522→3546, +24; committed + pushed)
- Batch A81 swept Sialis → Tachysurus. Highlights: Thread-sail Filefish (カワハギ/丝背细鳞鲀), Common Lilac (ライラック/欧丁香), Cannonball Fungus (タマハジキタケ, spore-shooting), アケボノソウ (dawn-sky gentian/獐牙菜), White Mustard, 2 bagrid catfish (ギギ/ギバチ), trap-jaw scale ant.
- Verified Sialis/センブリ属 = alderfly genus (insect), not the gentian. Caught + fixed a bad zh alias mid-batch (`斑足出尾蕈甲近似种`→`斑足水边大眼隐翅虫`; 出尾蕈甲 is wrong family, ハネカクシ=隐翅虫). Demo 37.9 → 38.0 MB. 30 np=4 remain. Next A82 finishes the tier.

### 2026-05-30 (Claude) — np=4 tier A80 (3498→3522, +24; committed + pushed)
- Batch A80 swept Pteris → Shaka. Highlights: Pied Avocet (反嘴鹬), Five-leaf Azalea (シロヤシオ/五叶杜鹃), Wineberry (エビガライチゴ), Green Brittlegill (アイタケ/变绿红菇, edible russula), Fortune's Saxifrage (ダイモンジソウ, "大"-character flower), Wild Radish, Japanese Cedar Longhorn, red-gilled aeolid.
- Verified Sabia conica/キクスズメ is a mollusk (hoof snail) via taxon_group before writing. Demo 37.8 → 37.9 MB. 54 np=4 candidates remain. Next A81 from Sialis.

### 2026-05-30 (Claude) — np=4 tier A79 (3474→3498, +24; committed + pushed)
- Batch A79 swept Pinellia → Pteraeolidia. Highlights: Indianmeal Moth (印度谷螟), Moss Rose (松叶牡丹), Blue Dragon Nudibranch (蜈蚣鳃海蛞蝓), Ruler Damselfly (モノサシトンボ), Chinese Cinquefoil (委陵菜), 3 holly ferns (イノデ属), leafless haircap moss.
- Demo stable 37.8 MB. 78 np=4 candidates remain. Next A80 from Pteris pseudosefuricola.

### 2026-05-30 (Claude) — np=4 tier A78 (3450→3474, +24; committed + pushed)
- Batch A78 swept Oryctolagus → Pilosabia. Highlights: European Rabbit, Tree Peony (牡丹), Short-tailed Albatross (アホウドリ, endangered), Blue Bat Star (海燕), Aizoon Stonecrop (費菜), boxfish, lantern slime mould.
- 2 ja-name DB fixes (local-only): `Phedimus aizoon` 麒麟草→キリンソウ, `Phillipsia domingensis` 肉厚紅皿茸→ニクアツベニサラタケ.
- Demo 37.0 → 37.8 MB. 102 np=4 candidates remain. Next A79 from Pinellia tripartita.

### 2026-05-30 (Claude) — np=5 tier cleared: A54–A67 sweep (2901→3219, +318; committed + pushed)
- 14 batches (A54–A67) alphabetically swept Abelia → Zaranga; np≥5 now 0 candidates (verified).
- Highlights: Japanese skink, Tanuki-zone fauna, giant flying squirrel, Baer's Pochard + Ferruginous Duck + Bufflehead + Brant + Shelduck (rare ducks), Star Magnolia, water caltrop relatives, waterwheel plant (carnivorous), Dead Man's Fingers, many shore molluscs/fish/hermit crabs.
- Fixed 3 zh-Hans typos mid-sweep (大阪绿步甲, 近缘牙甲, 蚊子草). `R.arborea`/シャクナゲ kept as genus-level rhododendron (mangled sci name, left as-is).
- Demo 36.2 → 37.0 MB. **np≥5 coverage 100%.** Next: np=4 tier (~450 candidates).

### 2026-05-29 (Claude) — np=6 tier cleared: A45–A53 sweep (2694→2901, +207; committed + pushed)
- 9 batches (A45–A53) alphabetically swept Acanthochitona → Weigela; np≥6 now 0 candidates (verified). +2 stragglers (ヌマムツ, オカヒジキ) in A53b.
- Highlights: Tanuki-adjacent fauna, Christmas Rose, Poison Hemlock (toxic), panthercap + destroying-angel Amanitas (toxic), Crested Kingfisher, Japanese Scops Owl, Laysan Albatross, Japanese Hare, Rugosa Rose, water caltrop, mistletoe, sea hibiscus, razor clam, turban shell.
- Fixed placeholder ja-name `Bucephala albeola` カモ→ヒメハジロ (DB UPDATE, local-only). Fixed 2 zh-Hans typos mid-sweep (大阪绿步甲, 近缘牙甲).
- Cumulative ~110 common_name_en + ~50 zh-Hans aliases backfilled. Demo 35.6 → 36.2 MB. **np≥6 coverage 100%.**

### 2026-05-29 (Claude) — np=7 tier cleared: A38–A44 sweep (2536→2694, +158; commits this session)
- 7 consecutive batches alphabetically swept Acanthosoma → Vibidia; np≥7 now 0 candidates remaining (verified).
- **A38** (+24): Acanthosoma→Buxus. Honshu Maple, Grey Burrowing Snake (Achalinus), Many-sepal anemone, Snake Amanita (toxic).
- **A39** (+24): Calidris→Eleutherococcus. Ruff, Taiwan Cherry + Kawazu-zakura cultivar, Strawberry conch, Green-eyed Robber Fly.
- **A40** (+24): Endotricha→Lepista. Harlequin Duck, Silver Carp, Fall Webworm, Festive Sea Slug (Aoumiushi), Blewit.
- **A41** (+24): Lichenophanes→Ophioglossum thermale. Tanuki (Nyctereutes), Dawn Redwood, Tallow tree, Sword fern, two adder's-tongue ferns.
- **A42** (+24): Ophioglossum vulgatum→Rattus. Red Phalarope, Moss-pink, Samurai slave-making ant (Polyergus), Flower crab, Rapa whelk.
- **A43** (+24): Rondibilis→Sympetrum. Rosalia batesi (iconic azure longhorn), Pussy willow, Korean stewartia, Red-veined Darter.
- **A44** (+14): Tachycines→Vibidia CLOSEOUT. Greenhouse Camel Cricket, Baldcypress, Horned Turban (Sazae), 12-Spot fungus-eating ladybird.
- Cumulative sidecar backfills ~75 common_name_en + ~38 zh-Hans aliases. Demo 35.2 → 35.6 MB. **np≥7 coverage 100%.**

### 2026-05-29 (Claude) — data-quality cleanup sweep (no commit-tracked DB; manual_species.json + docs re-exported)
- **Bufo fragmentation fixed**: `Bufo japonicus` (id=9929, romaji ja-name "Nihon Hikigaeru", np=84, no tid) was a split-off dup of `Bufo formosus`/アズマヒキガエル (id=820). iNat treats "Bufo japonicus" as a deprecated name split into formosus (Kanto) + praetextatus (W.Japan). 820 already held 92 "Bufo japonicus" raw obs via its sci alias → merged 9929→820 (now 176 + 353 obs). Romaji entry gone.
- **3 sakura cultivars resolved** (were NULL-sci): カワヅザクラ (id=106, ヅ-variant) merged into existing カワズサクラ (id=558, `Cerasus × kanzakura 'Kawazu-zakura'`); コブクザクラ (id=175) → `Cerasus 'Kobuku-zakura'`; ジンダイアケボノ (id=234) → `Cerasus × yedoensis 'Jindai-akebono'`. All 3 added to `data/manual_species.json` for future-proofing.
- **2 non-taxa deleted**: コミュニケーション (text-mining junk) + タケノコ (bamboo shoot, not a taxon).
- Net: species 9770→9766; null-sci visible 28→23; romaji-ascii visible species 6→5 (remaining 5 lack iNat ja-name/tid, documented). Ran `dedupe`, re-exported (35.2 MB), synced docs/. Integrity re-verified all-green.
- **Backups pruned**: 9 → 2 (kept `bak_pre_tsutsuji_sawara` 5/27 + `bak_pre_cleanup_20260529` today); freed ~742 MB.

### 2026-05-28 (Claude) — np=8 tier cleared: A30–A37 sweep (2371→2537, +144; commits `0067a12` → `cce7b66`)
- 8 consecutive batches (A30–A37) alphabetically swept Acer → Zoothera. Final batch A37 (12 entries) closed the tier; np≥8 now 0 candidates remaining.
- **A30** (`0067a12`, +22): Acer→Bassia. Greater White-fronted Goose, Tamagotake, Sweet Annie, Thale Cress, Kochia.
- **A31** (`cd39fed`, +22): Bristowia→Coprinellus. Sharp-tailed Sandpiper, Siberian Rubythroat, Japanese Serow, Eurasian Treecreeper, Peppery Bolete, Bamboo Borer.
- **A32** (`17d098d`, +22): Clytus→Eurema. Japanese Quail, Constable Butterfly, Chinese Grosbeak, Bird-dropping mimic Cyrtarachne, Tasmanian Blue Gum.
- **A33** (`004e6ca`, +22): Eutonia→Lactuca. Beefsteak Fungus, Witches' Butter, Senegal Tea, Asian Shore Crab, 13-spot Ladybird, Panicle Hydrangea.
- **A34** (`748a8fe`, +22): Laportea→Nippancistroger. Japanese Bell Cricket, Smallmouth + Largemouth Bass, Magic Lily, Southern Magnolia, Burgundydrop Bonnet, Japanese Green Hairstreak.
- **A35** (`6492684`, +22): Nipponobuprestis→Pyracantha. Masu Salmon, Crested Honey Buzzard, Russet Sparrow, Spangle Swallowtail, Japanese Bombardier Beetle, Misty Cherry, White-browed Laughingthrush.
- **A36** (`c7980f4`, +22): Rhantus→Tartessus. Floating Fern (Salvinia), Goat Willow, Ancient Murrelet, Hamabō Hibiscus, East Asian Swertia, Yellow Dung Fly.
- **A37** (`cce7b66`, +12): Ternstroemia→Zoothera closeout. Wakame, Crimson Glory Vine, Scaly Thrush, Mokkoku, Mulberry Tiger Longhorn.
- Cumulative sidecar backfills ~74 common_name_en + ~40 zh-Hans aliases. Demo 34.8 → 35.2 MB. **np≥8 coverage 100%.**

### 2026-05-28 (Claude) — np≥9 tier cleared: A25–A29 sweep (2279→2371, +93; commits `5a4f2fd` → `f3e7804`)
- **A25** (`5a4f2fd`, +22): Dichomeris→Matricaria. Highlights: Merlin, Tokyo Salamander, Glandirana reliquia frog, Geastrum+Lycoperdon fungi.
- **A26** (`07e5661`, +25): Echinolittorina→Palaemon. Highlights: 5 jewel/spittle/sesiid insects, Hiroses' damselfly Mortonagrion (Red List EN-IB), Tetrapanax planthopper, both Palaemon prawns.
- **A27** (`8efde9e`, +25): Parasa→Spicantopsis. Highlights: Phalaropus phalarope, Parmotrema lichen (air-quality indicator), Quercus dentata (kashiwa-mochi oak), Sphrageidus moth (with urticating hairs warning).
- **A28** (`a4cc515`, +18): Simulatacalles→Yezoterpnosia. Highlights: Copper Pheasant, Spring Cicada, Spotted Redshank, Mushak Cupid (recent invader from China).
- **A29** (`f3e7804`, +2): closeout. **Malus toringo had romaji ja `ko-nashi`** → fixed in-DB to `ズミ` (canonical), `ko-nashi` preserved as ja-romaji alias, mis-tagged `en`→`ja` alias corrected, pinyin `san ye hai tang` zh-Hans alias purged. Macrogerris (subgenus 亜属) profiled following ツツジ属 precedent.
- Cumulative: 8 batches over 2026-05-27/28, en backfills ~41 + zh-Hans alias backfills ~23. Demo 34.5→34.8 MB. **np≥9 coverage 99.9% → 100%.**

### 2026-05-27 (Claude) — ツツジ + Sawara: genus entry + homonym disambig (commit `d76b147`)
- ツツジ (id=270, was NULL-sci np=25): set `scientific_name='Rhododendron sp.'`, `common_name_ja='ツツジ属'`, `taxon_group=shrub`; added zh-Hans alias `杜鹃花属`. Existing `ツツジ` ja/ja-kana aliases preserved for resolver. **First time the schema uses genus-only `sci sp.` notation** — works fine with seed/export pipelines.
- Sawara homonym (Wikipedia-style kanji disambig): id=3417 tree → `サワラ（椹）`, id=588 fish → `サワラ（鰆）`. New ja/ja-kana aliases for both disambiguated forms. Wrongly-tagged `en`-lang `サワラ` alias on tree row dropped. `SKIP_IDS` guard in `fix_romaji_ja_names.py` kept (now dead code, but documents the regression risk).
- DB integrity unchanged; species/pairs totals stable at 9,583 / 126,491.

### 2026-05-26 (Claude) — system audit + merge_duplicate_species pass
- Integrity scan: 0 orphan rows across alias/profile/photo/observation; 0 dead parks; 0 dupe `scientific_name`; only 2 NULL-species observations (descriptive labels, harmless). Healthy.
- Ran `scripts.merge_duplicate_species` (hadn't been run in weeks): 6 bogus `inat_taxon_id` collisions (each shared by 13–32 unrelated species) NULLed; 55 legitimate synonym pairs/triples (Vitex agnus/agnus-castus, Botaurus sinensis/Ixobrychus sinensis, Anoplophora chinensis/malasiaca, etc.) collapsed. Visible species 9640 → 9583.
- HANDOFF restructured: Active list rewritten with 6 concrete items, periodic-maintenance task added as #2 (run `merge_duplicate_species` + `fix_romaji_ja_names` after every iNat/GBIF/eBird ingestion), Recent sessions trimmed from ~60 entries to ~15.

### 2026-05-26 (Claude) — romaji ja-name placeholder fix (62 species)
- New `scripts/fix_romaji_ja_names.py`: queries iNat `locale=ja` to recover canonical katakana for species whose `common_name_ja` was a romaji placeholder (`Yahazu-endo` → `ヤハズエンドウ`, `Oni-tabirako` → `オニタビラコ`, ...). 60 renamed; 2 sibling-typo duplicates merged via `merge_species_pair` (Paraprenanthes sororia/sorosia, Cephalotaxus harringtonii/harringtonia); 1 homonym (`Sawara` tree vs `サワラ` fish, id=3417) skipped via SKIP_IDS. Strips trailing `(広義)` for consistency with prior cleanup.
- These 60+ species were previously invisible to the profile-batch query (filter `SUBSTR(common_name_ja,1,1) NOT BETWEEN 'A' AND 'Z'`) and many had very high spread — combined np ≈ 1,400 park_species pairs that now display in proper Japanese AND are eligible for future profile batches. `Yahazu-endo` alone was np=140 (wider than ソメイヨシノ post-merge).
- Remaining 7 ASCII-prefix species: 2 truly lack a ja name in iNat (Polyphylla laticollis, Streptococcus agalactiae — the latter actually has a ja name starting with 'B'); 1 Sawara homonym; 4 without inat_taxon_id (low np).

### 2026-05-26 (Claude) — A23 + sakura placeholder merge
- A23 batch: +22 profiles (np≥10 remainder + start of np=9). 2,236 → 2,258. 7 common_name_en + 6 zh-Hans aliases backfilled. Cleared the post-cleanup np≥10 sedges + Brassica rapa + Hypericum perforatum.
- New `scripts/merge_species_pair.py`: single-pair version of `merge_duplicate_species`, takes `--from`/`--to` species ids and repoints observations + aliases + photos + profiles; preserves dst common_name_ja; inserts src common_name_ja as a ja alias. Used to merge `サクラ` (NULL-sci, id=181, np=42) into `ソメイヨシノ` (id=252) — combined np now 51, making it the top-spread plant in the demo.
- `ツツジ` (np=25) NOT merged: source pages (akirudai, oizumi-chuo, koganei) explicitly list it alongside サツキ as distinct items, so it's used as a broad genus category and there's no default cultivar to redirect into. Left as NULL-sci.

### 2026-05-26 (Claude) — species_alias partial-unique migration (Active #4 done)
- Dropped `species_alias UNIQUE(raw_name, lang)`; replaced with partial unique index `uniq_alias_resolver` covering only `('ja','ja-kana','sci','en')`. Migration script `scripts/migrate_alias_partial_unique.py` is idempotent; row count preserved (31967→31967). DB backed up to `parklife.db.bak_pre_alias_partial` before run.
- Rationale: `species_alias` was conflating two roles (resolver lookup vs display vernacular). Only resolver langs need uniqueness; display langs (zh-Hans/zh-Hant) are correctly many-to-one per species — `export_html.py:187` uses `setdefault` keyed by species_id, so multiple species sharing a Chinese name causes no display issue.
- Re-ran `scripts.seed_species_profiles` → 27 aliases inserted (the 26 previously collision-blocked + 1 from A22). zh-Hans coverage on profiled species now 98% (51 missing — all genuinely lacking a published Chinese vernacular, no further collisions). `parklife/db.py` schema updated to match. Re-exported docs (34.5 MB) and published to `docs/`.

### 2026-05-26 (Claude) — placeholder-name cleanup + A22 (2231→2236)
- New `scripts/fix_placeholder_names.py`: 9 per-species fixes (hybrid notations cleaned to `マガモ×カルガモ雑種` form; romaji `tsuno-hashibami` → `ツノハシバミ`, `yama-rakkyō` → `ヤマラッキョウ`; genus placeholder `オオハンゴンソウ属` → `アラゲハンゴンソウ`; nakaguro transliterations → canonical katakana). Bulk-stripped 53 redundant `（romaji）` / `（広義）` annotations on Carex etc. Idempotent.
- A22 sidecar batch (+5 profiles) for the originally-noted 5 hybrid/romaji/genus species, all with `common_name_en` + `aliases.{zh-Hans}` fields.

### 2026-05-26 (Claude) — UI chrome i18n: switch full page on language change (commit `a8173a5`)
- Added `CHROME_LABELS` dict (4 langs) covering `<title>`, header label texts, all select option lists, search placeholder, stats template, modal close aria-label, and `<html lang>` attribute.
- New `renderChrome()` JS function called on init + every language change. Header labels marked with `<span class="lbl" data-lbl="…">` so JS can rewrite text without re-rendering full options; select values preserved through option rebuilds.
- Map tile labels NOT switched — OSM default tiles use native script and there's no standard tile-language switch.

### 2026-05-26 (Claude) — profile curation A14–A21: np≥10 tier cleared (2053→2231)
- +178 profiles across 8 batches (commits `d84f817`, `8f16d45`, `a540c28`), sidecar format with `common_name_en` + `aliases.{zh-Hans}`.
- A14 np=12 (bass, Glacial Apollo, Mukashitombo dragonfly, Fire-bellied Newt, European Mantis, Purseweb Spider); A15–A18 np=11→10 (Black Cutworm, Pearly Everlasting, Jimsonweed, Eurasian Curlew, Morel, Forest Cricket); A19–A21 np=10 sweep (Stejneger's Scoter, Fringed Water-lily, Painted-snipe, Eurasian Nuthatch, Wild Boar, Japanese Shrew-mole, Forest Green Tree Frog).
- np≥10 tier exhausted modulo 5 unprofileable placeholders (later cleaned by `fix_placeholder_names`).

### 2026-05-25 (Claude) — profile curation A1–A13 + name sync pipeline (1778→2053)
- +275 profiles across 13 batches A1–A13 (commits `9be2185` → `7ec538a`). Covered np=14–12 tier: national butterfly Sasakia, pomegranate, satsuki, Black-tailed Godwit, Tundra Swan, witch hazel, bigfin reef squid, Macrocilix mysticata bird-dropping mimic, 秋の七草, sedges/mushrooms/inverts, etc.
- Name-sync pipeline (commits `08b8514` → `bbfa587`): closed gap between profiles and species name fields. `common_name_en` NULL 609→18; zh-Hans missing 239→37. Toolchain: regex from profile summaries, iNat `/v1/taxa/{id}?locale=…` (new `scripts/inat_localized_names.py`), Wikidata residual (new `scripts/wikidata_residual_names.py`), manual batches.
- Sidecar format extended (`f124c86`): `species_profiles_extra.json` entries now carry optional `common_name_en` + `aliases.{zh-Hans,zh-Hant}` fields written idempotently by `seed_species_profiles.apply_extra_aliases()`.

### 2026-05-25 (Claude) — P13 URL pass 3 + parking reclassify (1500→1778)
- P13 URL pass 3 (commits `d82c2b2` → `e009cd3`): 124/142 NULL P13 parks filled via 4 parallel research agents + manual JSON; 18 small 緑地/河川敷 confirmed `__no_url__`. Apply script `scripts/apply_manual_park_urls.py`. **Lesson**: instruct agents to Write incremental JSON after EACH park, not at end.
- Parking reclassify (`44b6ce2`, `e009cd3`): new `scripts/fetch_parking_followups.py` + `scripts/reclassify_parking.py`. For each `parking_info LIKE 'OSM:%'` park, refetch official + 4 intra-host links, re-run classify. 91 parks moved OSM-heuristic → text-confirmed; 13 verdict flips on missed lots >300m from centroid.
- +291 profiles across 20 batches: 1487 → 1778 distinct species.

### 2026-05-21 (Claude) — park-specific gallery photos shipped
- New `scripts/park_species_photo.py` + `park_species_photo` table. Per-(park, species) gallery from already-cached iNat + GBIF data (tier-0 ≤600 m, tier-1 ≤5 km) — no new network calls.
- GBIF cache was the unlock: ~80% of GBIF records carry `media[]` array with image + creator + license + lat/lon already park-scoped.
- Full sweep: 122,283 visible pairs → 57,406 (47%) got park-local photos. 46,816 photo rows. Export wiring in `scripts/export_html.py`: per-pair `li` array; `speciesPhotos(sp, pair)` preserves species hero at slot 0 then inserts park-local. Demo 17.1 → 30.3 MB.

### 2026-05-16 (Claude) — TODO #6/3/7/5 batch (P13 expansion + parking + photo + sort)
- P13 expansion (`scripts/p13_seed.py`, commit `e1f93b2`): downloads 国土数値情報 P13 GML zips for 4 pref, filters biodiv park types ≥5 ha, dedupes 500 m radius. +273 parks (209 → 482). Full geo enrichment: iNat +21,792, GBIF +66,406, eBird +4,042 obs.
- OSM parking (`scripts/osm_parking.py`, `6a9ca7d`): Overpass `amenity=parking` within 300 m. Parking NULL 318 → 0.
- Photo gallery rebuild (`scripts/repopulate_species_photos.py` + `wikicommons_hero.py`, `87f5553`): diversity-deduped iNat + Commons P18. 34,979 iNat + 5,822 hero rows. `species_photo.source_url` column added.
- Sort fix (`a85c1ba`): freq sort now `pair.oc` desc primary, `sp.n` tiebreaker (was global-only).

### 2026-05-14/15 (Claude) — Chinese name coverage + sp2000 import (3 sessions)
- Display fallback fixed (`15cbd4c`): zh/zhT users see English with `.name-fallback` dotted-underline, not silently fall back to Japanese.
- `wikidata_zh_broad.py` (P1843 + altLabel SPARQL) + `wikipedia_zh_direct.py` (zh.wiki redirect): +87 aliases.
- `cleanup_zh_aliases.py` removed 48 pinyin aliases; `sp2000_import.py` ingested Catalogue of Life China 2023 DwC-A (288k taxa) → +231 zh-Hans aliases. Visible zh coverage 4,619 → 4,948.

### Older sessions
Pre-2026-05-14 history (initial DB build, prefecture scrapers, Wikipedia normalizer, GBIF/eBird/iNat first passes, modal+photo MVP, repo consolidation to GitHub Pages, Codex collaboration setup) lives in git log. Key infrastructure commits: `9f3add9` (repo init), `e1f93b2` (P13), `87f5553` (photo gallery), `f124c86` (sidecar profile format), `6c9a6b8` (alias partial-unique migration), `bad6563` (romaji fix), `a057858` (dupe-merge pass).
